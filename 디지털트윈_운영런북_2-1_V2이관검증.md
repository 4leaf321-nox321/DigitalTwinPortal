# 운영서버 런북 2-1 — V2 테이블 생성 · 이관 · 동일성 검증

> 대상: **운영서버에서 직접 실행** (AI 없음 · 인터넷 없음)
> 근거: [디지털트윈_대시보드_실행계획.md](./디지털트윈_대시보드_실행계획.md) Phase 2
> 선행: Phase 1 완료 (통합런북)
> 소요: **약 1~1.5시간**
> 서비스 영향: **없음.** 사용자는 평소대로 쓰면 된다. 공지 불필요

---

## 이 작업이 하는 것

**기존 구조를 그대로 둔 채, 과제를 "행"으로 저장하는 새 테이블을 만들고 데이터를 복사해
"원본과 완전히 같다"를 증명한다.**

```
dashboard_data (기존)  ──읽기만──▶  dt2_projects 등 7개 신규 테이블
                                          │
                                     검증 스크립트로 왕복 비교 
```

## 이 작업이 하지 않는 것

```
✗ 화면 변경          사용자는 아무 차이도 느끼지 못한다
✗ 저장 방식 변경     V1 이 계속 정본이다
✗ 전환/컷오버        V2 는 "증명된 복사본"으로 대기만 한다
✗ 기존 테이블 수정   dashboard_data 는 읽기만 한다
```

**언제든 없던 일로 만들 수 있는 구간이다.** `flask db downgrade` 한 줄이면 테이블이 사라진다.

## 왜 하는가

원래 목표가 **"AI(MCP)가 과제를 수정하되 과제별로 권한을 나누는 것"** 이었다.
지금은 ①모든 쓰기가 싱글톤 한 행 전체를 다시 써서 권한을 걸 **대상**이 없고
②`과제PL` 이 자유 텍스트라 권한을 판단할 **주체**가 없다.
V2 의 `owner_user_id` · `row_version` 이 그 둘을 만든다.

---

## 이 런북을 쓰는 법

- 명령은 **그대로 복사해서 붙여넣는다.**
- **"이 결과가 나와야 정상"** 과 다르면 **거기서 멈춘다.**
- **판단하지 않는다.** 예상과 다르면 화면을 캡처하고 담당자에게 전달한다.
- ★ 는 건너뛰면 안 되는 검증 지점이다.

---

# A. 준비

## A-1. 변수 설정 — **경로와 비밀번호만 수정**

```powershell
$PROJ   = "C:\경로를_여기에\52_KnowledgeGraphProject"
$BACKUP = "D:\dt_backup"
$PGBIN  = "C:\Program Files\PostgreSQL\17\bin"
$DB     = "dxdigitaltwin"
$env:PGPASSWORD = "여기에_비밀번호"
```

이어서 그대로 실행:

```powershell
$STAMP  = Get-Date -Format "yyyyMMdd_HHmm"
$COREBK = (Get-ChildItem $BACKUP -Filter "*.dump" -ErrorAction SilentlyContinue | Sort-Object LastWriteTime -Descending | Select-Object -First 1).FullName
Write-Host "작업 스탬프 : $STAMP"
Write-Host "최근 백업   : $(if($COREBK){$COREBK}else{'(없음 — B 에서 생성)'})"
```

## A-2. 현재 상태 기록 ★ 나중에 대조할 숫자다

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT (SELECT version FROM dashboard_data) AS v1_version, (SELECT json_array_length(projects) FROM dashboard_data) AS projects, (SELECT json_array_length(performances) FROM dashboard_data) AS performances, (SELECT count(*) FROM project_attachments) AS attachments;" | Tee-Object -FilePath "$BACKUP\phase2_baseline_$STAMP.txt"
```

**화면의 숫자를 적어둔다.** 마지막에 이 값과 대조한다.

---

# B. 백업

Phase 1 이후 데이터가 작아져 **몇 분이면 끝난다.**

```powershell
& "$PGBIN\pg_dump.exe" -h localhost -U postgres -d $DB `
  --format=custom --compress=6 --no-owner --no-privileges `
  --exclude-table-data=public.dashboard_snapshots `
  -f "$BACKUP\core_$STAMP.dump"
Write-Host "종료코드: $LASTEXITCODE"
$COREBK = "$BACKUP\core_$STAMP.dump"
Get-Item $COREBK | Select-Object Name, @{n='MB';e={[math]::Round($_.Length/1MB,1)}}
```

**정상** — 종료코드 `0`, 크기 0 아님.

> 이 작업은 기존 테이블을 건드리지 않지만, 되돌릴 지점은 항상 만들어 둔다.

---

# C. 코드 배포

## C-1. 백엔드 중지

## C-2. 기존 파일 보관

```powershell
New-Item -ItemType Directory -Force -Path "$BACKUP\code_before_p2_$STAMP" | Out-Null
Copy-Item "$PROJ\backend\app\modules\digital_twin_dashboard\__init__.py" "$BACKUP\code_before_p2_$STAMP\" -Force
Get-ChildItem "$BACKUP\code_before_p2_$STAMP" | Select-Object Name
```

## C-3. 새 파일 배포

```
backend\app\modules\digital_twin_dashboard\models_v2.py              (신규)
backend\app\modules\digital_twin_dashboard\__init__.py               (교체 — models_v2 import 추가)
backend\migrations\versions\fba9e7125df9_add_dt2_normalized_tables_phase_2.py  (신규)
backend\scripts\dt2_import.py                                        (신규)
backend\scripts\dt2_verify.py                                        (신규)
backend\scripts\dt_snapshot_cleanup.py                               (교체 — Phase 1 VACUUM 버그 수정본)
```

> **프론트엔드는 변경이 없다.** `dist` 를 건드리지 않는다.

## C-4. 배포 확인

```powershell
Test-Path "$PROJ\backend\app\modules\digital_twin_dashboard\models_v2.py"
Test-Path "$PROJ\backend\migrations\versions\fba9e7125df9_add_dt2_normalized_tables_phase_2.py"
Test-Path "$PROJ\backend\scripts\dt2_import.py"
Test-Path "$PROJ\backend\scripts\dt2_verify.py"
Select-String -Path "$PROJ\backend\app\modules\digital_twin_dashboard\__init__.py" -Pattern "models_v2" | Select-Object LineNumber, @{n='Line';e={$_.Line.Trim()}}
```

**정상** — 앞의 4개가 전부 `True`, 마지막에 `models_v2` 줄이 보임.

## C-5. 서비스 재시작 후 확인

```powershell
Invoke-WebRequest -Uri "http://localhost:5174/api/digital-twin-dashboard/health" -UseBasicParsing | Select-Object StatusCode
```

**정상** — `200`. 기동 로그에 `[Frontend] 빌드 자산 확인 완료` 도 보여야 한다.

**실패 시** — `$BACKUP\code_before_p2_$STAMP\__init__.py` 를 되돌리고 재시작한다.

---

# D. 테이블 생성

```powershell
cd "$PROJ\backend"
.\venv\Scripts\activate
$env:FLASK_APP = "run.py"
python -m flask db upgrade
```

**정상 출력**

```
INFO  [alembic.runtime.migration] Running upgrade e707bfa26eeb -> fba9e7125df9, add dt2 normalized tables (Phase 2)
```

확인:

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT c.relname AS table_name FROM pg_class c JOIN pg_namespace n ON n.oid=c.relnamespace WHERE n.nspname='public' AND c.relkind='r' AND c.relname LIKE 'dt2%' ORDER BY 1;"
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT count(*) AS dt2_projects FROM dt2_projects;"
```

**정상** — 테이블 **7개**, `dt2_projects` **0행**.

```
dt2_change_proposals
dt2_performances
dt2_project_attachments
dt2_project_dependencies
dt2_project_editors
dt2_project_performance
dt2_projects
```

> 이 마이그레이션은 **새 테이블을 만들기만 한다.** 기존 테이블 변경 구문이 하나도 없다.
> 되돌리기: `python -m flask db downgrade`

## D-1. 기존 데이터가 그대로인지 ★

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT (SELECT version FROM dashboard_data) AS v1_version, (SELECT json_array_length(projects) FROM dashboard_data) AS projects, (SELECT json_array_length(performances) FROM dashboard_data) AS performances;"
```

**A-2 와 완전히 같아야 한다.** (사용자가 그 사이 저장했다면 version 만 올라간다)

---

# E. 이관 계획 확인 (아무것도 쓰지 않음)

```powershell
python scripts\dt2_import.py
```

**정상 출력 예시**

```
── 원본 (V1 vNNN) ──
  과제 396건 / 성과 596건

── 참조 데이터 ──
  사용자 NN명 / 활성 사업부 8개 / 보고확인 항목 NN건

── 이관 계획 ──
  성과 596건 처리
  과제 396건 처리
  소유자 해석 — knoxId NN / 과제PL NN / 작성자 NN / **미해결 NN**
  과제→성과 참조 NNN건 중 NNN건 연결
  고아 참조(성과 없음) : N건  ← 건너뜀
  ...
 계획만 확인했습니다. 아무것도 쓰지 않았습니다.
```

**확인할 것**

| 항목 | 판정 |
|---|---|
| 과제·성과 건수 | **A-2 와 일치** |
| 활성 사업부 | 8개 |
| `[확인] 활성 사업부에 없는 값` | 나오면 **적어두고 담당자에게 알림** (진행은 가능) |
| `[정보] extra_fields 로 보존한 키` | 나오면 **적어둔다** (유실은 아님) |
| 소유자 **미해결** | 숫자를 적어둔다. 나중에 지정하면 됨 |

**멈춰야 하는 경우** — 오류가 나거나 과제·성과 건수가 A-2 와 다르면 중단한다.

---

# F. 이관 실행

```powershell
python scripts\dt2_import.py --commit
```

**정상 출력**

```
[OK]   커밋 완료

── V2 현황 ──
  dt2_projects             : 396
  dt2_performances         : 596
  dt2_project_performance  : NNN
  dt2_project_dependencies : N
  dt2_project_attachments  : N
```

**판정** — `dt2_projects` · `dt2_performances` 가 **A-2 의 건수와 일치**.

**실패 시** — 스크립트가 **자동 롤백**한다. `[FAIL]` 이면 데이터는 변경되지 않은 상태다.
`scripts\out\dt2_import_*.log` 를 담당자에게 전달한다.

> 원본 `dashboard_data` 는 읽기만 했다. 이 단계에서 기존 데이터는 바뀌지 않는다.

---

# G. 동일성 검증 ★★ Phase 2 의 핵심 관문

```powershell
python scripts\dt2_verify.py
```

**통과 기준 — 5개 항목 전부 `[OK]`**

```
 판정 요약
  [OK]   건수
  [OK]   과제 필드
  [OK]   성과 필드
  [OK]   과제-성과 연결
  [OK]   참조(이미지·선행·첨부)

 결과: [OK] V2 가 원본과 논리적으로 동일합니다.
```

**특히 이 세 줄이 0 이어야 한다**

```
  불일치 필드                     : 0개
  원본에 있는데 V2 없음            : 0건
  extra_fields 로도 보존 안 된 키  : 0개
```

## `[FAIL]` 이 나오면

**중단한다.** 데이터는 안전하다 — V1 은 그대로이고 V2 는 복사본일 뿐이다.

상세를 확인해 담당자에게 전달한다. **값은 출력되지 않는다**(과제 code·필드명·건수만).

```powershell
python scripts\dt2_verify.py --detail
```

> 이 검증 스크립트는 개발 환경에서 **9종의 훼손을 일부러 심어 전부 검출되는 것을 확인**했다.
> `[OK]` 는 "검사를 안 해서 통과"가 아니라 "실제로 대조해서 같다"는 뜻이다.

---

# H. 최종 확인

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT (SELECT version FROM dashboard_data) AS v1_version, (SELECT json_array_length(projects) FROM dashboard_data) AS v1_projects, (SELECT count(*) FROM dt2_projects) AS v2_projects, (SELECT count(*) FROM dt2_performances) AS v2_performances;"
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT count(*) FILTER (WHERE NOT is_deleted AND NOT is_permanently_deleted) AS active, count(*) FILTER (WHERE is_deleted OR is_permanently_deleted) AS deleted, count(owner_user_id) AS owner_resolved, count(*) - count(owner_user_id) AS owner_unresolved, count(*) AS total FROM dt2_projects;"
```

**정상 조건**

| 항목 | 기대 |
|---|---|
| `v1_projects` | **A-2 와 동일** ★ 원본 무손상 증거 |
| `v2_projects` | `v1_projects` 와 동일 |
| `active` / `deleted` | 초기 스캔 기준 활성 352 / 삭제 44 근처 |
| `owner_unresolved` | 적을수록 좋음. 초기 스캔 기준 **1건 예상** |

## H-1. 화면 확인

```
□ 대시보드 정상 표시
□ 과제 하나 저장 → 정상
□ 보고서 이미지 정상
```

**아무 변화도 없어야 정상이다.** V2 는 아직 화면과 연결되지 않았다.

---

# 완료 체크리스트

```
□ A-2   기준값 기록 (phase2_baseline_*.txt)
□ B     core_*.dump 생성 (종료코드 0)
□ C-4   배포 파일 4개 True + models_v2 import 확인
□ C-5   재시작 후 health 200
□ D     dt2 테이블 7개 생성, dt2_projects 0행
□ D-1   기존 데이터 A-2 와 동일 ★
□ E     이관 계획 확인, 건수 A-2 와 일치
□ F     이관 커밋, V2 건수 일치
□ G     검증 5항목 전부 [OK] ★★
□ H     v1_projects 가 A-2 와 동일 ★
□ H-1   화면 정상 (변화 없음)
```

**11개 항목이 모두 체크되면 Phase 2 이관·검증 완료.**

---

# 되돌리기

| 어디까지 갔든 | 방법 | 손실 |
|---|---|---|
| D (테이블 생성) 이후 | `python -m flask db downgrade` | 없음 |
| F (이관) 이후 | `python -m flask db downgrade` 또는 `python scripts\dt2_import.py --commit --truncate` 로 재이관 | 없음 |
| G (검증 실패) | 그냥 멈춘다 | 없음 |
| 코드까지 되돌리려면 | `$BACKUP\code_before_p2_$STAMP\__init__.py` 복원 후 재시작 | 없음 |

**원본 `dashboard_data` 를 한 번도 쓰지 않으므로, 어느 지점에서 중단해도 서비스 영향이 0 이다.**

---

# 문제가 생겼을 때

1. **더 이상 아무 명령도 실행하지 않는다**
2. `$PROJ\backend\scripts\out\` 의 최신 `dt2_import_*.log` / `dt2_verify_*.log` 확보
3. 화면 캡처
4. 담당자에게 전달

> F 의 이관은 **단일 트랜잭션**이다. 중간에 실패하면 전부 되돌려진다.

---

# 자주 나오는 상황

| 상황 | 답 |
|---|---|
| 화면에 아무 변화가 없다 | **정상.** V2 는 아직 화면과 연결되지 않았다 |
| `소유자 미해결` 이 있다 | 정상. 나중에 지정하면 된다. Phase 3 전까지만 채우면 됨 |
| `활성 사업부에 없는 값` 이 나온다 | 진행 가능. `division_id` 만 NULL 로 들어간다. 적어두고 알린다 |
| `extra_fields 로 보존한 키` 가 나온다 | **유실이 아니다.** 매핑에 없는 키를 통째로 보관한 것. 적어두면 나중에 컬럼으로 승격 가능 |
| `고아 참조` 가 있다 | 존재하지 않는 성과를 지목한 참조. 건너뛰는 것이 맞다 |
| 이관을 두 번 실행했다 | **안전하다.** uuid 기준 upsert 라 결과가 같다 |
| 검증에서 `[FAIL]` | **중단.** V1 은 안전하다. `--detail` 결과를 전달한다 |
| 사용자가 그 사이 저장했다 | 괜찮다. 이관을 다시 돌리면 최신으로 맞춰진다 (몇 초) |

---

# 이 런북의 검증 이력

개발 환경(PostgreSQL 17.5)에서 **운영 DB 복제본 + 예외를 심은 합성 데이터**로 전 과정 실행 (2026-07-28).

| 항목 | 결과 |
|---|---|
| 마이그레이션 | `create_table` 7개 + 인덱스뿐. 기존 테이블 변경 구문 **0개** |
| 이관 (복제본) | 과제 328 / 성과 432 / 연결 227 / 선행 2 / 첨부 1 |
| 검증 (복제본) | 과제 필드 7,337개 · 성과 필드 7,074개 대조 → **불일치 0**, 전항 `[OK]` |
| **훼손 시험 9종** | 제목·불리언·숫자 변경, 행 삭제, 연결 삭제, 기여도 변경, 선행 삭제, 이미지 훼손 → **전부 검출, exit 1** |
| 재실행 안전성 | 두 번 실행해도 건수 동일 |
| 훼손 후 복구 | 재이관만으로 원상복구 → 재검증 전항 `[OK]` |
| **합성 데이터 예외 14종** | 모르는 키 / 레거시 참조 3종 / 고아 / 중복 / 타입 흔들림 / 삭제·영구삭제 / `updatedAt` 결측 / 사업부 미매칭 / 선행 고아 / 이미지 참조 / 성과 소프트삭제 / `실적_N월` / UI 런타임 상태 → **전부 의도대로 처리, 전항 `[OK]`** |
| 원본 개발 DB | **v572 / 과제 328 그대로** — 시험은 전부 별도 DB |

> 운영은 데이터가 더 크고 소유자 매핑률이 높아(99.7%) **소요 시간과 미해결 건수만 다르고 절차·판정 기준은 동일**하다.
