"""Phase 5 — 사내 LLM(GLM-5.2)이 MCP 도구를 직접 쓰는 에이전트.

🚦 **한 층 위 `../ai_tools.py` 와 헷갈리지 말 것** — 그건 '필드 안내' 고 여기는 '에이전트' 다.
   이름이 비슷한 AI 갈래가 프로젝트에 여섯이다 → 루트 `디지털트윈_AI기능_지도.md`


세 층으로 나뉜다. 층을 섞지 않는 것이 요점이다.

    llm.py          OpenAI 호환 `/v1/chat/completions` 어댑터. **모델과만 얘기한다.**
    agent_tools.py  도구 스키마 + 실행. **판단하지 않는다** — REST 로 넘긴다.
    agent.py        루프. tool_calls → 실행 → 되먹임 → 최종 답변.

왜 이렇게 나누나
    도구의 판단(권한·위험도·확인 절차)은 이미 `routes_v2.py` + `permissions.py` 에 있고
    MCP 서버도 그걸 REST 로 부른다. 여기서 규칙을 다시 쓰면 **같은 규칙이 세 곳**이 되고,
    셋은 반드시 갈린다. 그래서 이 폴더에는 규칙이 한 줄도 없다.

개발서버에서 어디까지 되나
    LLM 접근이 없어도 **어댑터와 루프는 전부 검증된다** —
      · `scripts/llm_stub.py`      GLM 과 같은 응답 모양을 흉내내는 서버(어댑터 검증)
      · `scripts/dt3_test_agent.py` `chat` 을 각본으로 대체(루프·도구 실행 검증)
    운영에서만 확인되는 것은 **"GLM 이 실제로 옳은 도구를 고르는가"** 한 줄뿐이다.
"""
