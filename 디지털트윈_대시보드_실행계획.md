# 디지털 트윈 대시보드 — 실행 계획 (확정본)

> 작성일: 2026-07-28
> 배경 문서: [저장성능 개선계획](./디지털트윈_대시보드_저장성능_개선계획.md) · [MCP 권한분리 계획](./디지털트윈_대시보드_MCP_권한분리_계획.md) · [정규화 검토](./디지털트윈_대시보드_정규화_검토.md)
>
> **목표**: AI(MCP)가 과제를 안전하게 수정할 수 있는 구조를 만든다. 부수적으로 저장 성능도 정상화된다.
> **최대 제약**: **운영 중인 서버다. 데이터가 꼬이면 안 된다.** 이 제약이 아래 모든 설계를 지배한다.

---

## ★ 운영 실측 결과 (2026-07-28, `dt_scan.py` Phase 0-1 완료)

**운영 스캔이 완료되어 아래 수치가 확정됐다. 개발 데이터로 세운 가정과 크게 다르다.**

| 항목 | 개발 DB (가정) | **운영 DB (실측)** | 배수 |
|---|---|---|---|
| **저장 1회 payload** | 1.2 MB | **37.4 MB** | **31배** |
| **저장 1회 왕복(업+다운)** | 2.4 MB | **74.9 MB** | **31배** |
| **이미지 인라인** | 3과제 / 489 kB / 64% | **23과제 / 33.9 MB / payload의 94.4%** | **71배** |
| **스냅샷 누적** | 358건 / 논리 160 MB / 디스크 41.7 MB | **4,896건 / 논리 28.8 GB / 디스크 25.2 GB** | **600배** |
| 활성 과제 | 224 | **352** | |
| 삭제 상태 과제 | 104 | 44 | |
| 양방향 연결 불일치 | 26 / 6 | **53 / 36 (합 89건)** | |
| 첨부파일 고아 | 0 | **6건** | |
| 성과 신규 키 | 0 | **16개** | |
| **`updatedAt` 결측** | 199건 (60.7%) | **0건 (0.0%)** ✅ | 문제 없음 |
| **소유자 자동 매핑** | 21/224 (9.4%) | **351/352 (99.7%)** ✅ | 현업 확인 **1건** |
| `departments.division_id` | 백필 필요 | **백필 필요** | 동일 |

### 이 수치가 말하는 것

**① "느려졌다"의 원인이 완전히 설명된다.**
과제 하나의 진행률만 바꿔도 **75 MB가 왕복**한다. 그리고 서버는 같은 순간에 디스크로도 **약 75 MB를 쓴다**
(자동 스냅샷 37.4 MB + 싱글톤 37.4 MB). 느린 게 아니라 **이 규모로는 느릴 수밖에 없다.**

**② 범인은 이미지다 — 추정이 아니라 확정이다.**
payload 37.4 MB 중 **33.9 MB(94.4%)가 base64 이미지**다. 나머지 실제 업무 데이터는 3.5 MB뿐이다.
과제 23건이 전체 저장 성능을 지배하고 있다.

**③ 스냅샷 25.2 GB는 긴급 사안이다.**
저장할 때마다 37.4 MB짜리 전체 복사본이 한 건씩 쌓인다. 이미 4,896건 = **디스크 25.2 GB**.
**지금도 계속 늘어나고 있다.** 디스크 여유를 즉시 확인해야 한다.

**④ 일정이 단축된다 — 임계경로였던 Phase 0-2가 사실상 사라졌다.**
소유자 자동 매핑이 **99.7%**다. 현업 회람 대상이 **1건**뿐이다.
`updatedAt` 결측도 **0건**이라 Phase 0-3(백필)이 **불필요**하다.
→ 개발 데이터로는 "몇 주짜리 현업 대기"로 보였던 구간이 **몇 시간**으로 줄었다.

**⑤ 새로 확인해야 할 것**
- **첨부파일 고아 6건** — 과제와 연결되지 않은 첨부
- **양방향 연결 불일치 89건** — 개발(32건)보다 많다. 이관 정책 결정의 무게가 커짐

### 성과 신규 키 16개의 정체 (확인 완료)

| 키 | 개수 | 의미 | 조치 |
|---|---|---|---|
| `_deleted` `_deletedAt` `_deletedBy` `_deletedByName` | 4 | **성과에도 소프트 삭제가 실제로 쓰이고 있다.** 개발 DB엔 삭제 성과가 0건이라 키 자체가 안 보였다 | `dt2_performances`에 `is_deleted`/`deleted_at`/`deleted_by`/`deleted_by_name` 컬럼 추가 (반영 완료) |
| **`실적_1월` ~ `실적_12월`** | 12 | **월별 실적의 두 번째 표현.** 기존 `월별실적`(배열)·`월별실적여부`와 **같은 값을 두 곳에 담고 있다** | ⚠️ **정본 결정 필요** — 아래 참조 |

#### ✅ `실적_N월` 정본 결정 — 완료 (2026-07-28)

**운영 대조 결과 (성과 596건 기준)**

| 구분 | 건수 |
|---|---|
| 둘 다 값 있음 | **0** |
| `월별실적` 배열만 값 있음 | **16** |
| `실적_N월` 평면 키만 값 있음 | **0** |
| 둘 다 비어 있음 | 580 |

**프론트 코드 확인 결과 — `실적_N월`은 저장 필드가 아니라 입력/교환 포맷이다.**

| 위치 | 역할 |
|---|---|
| `BulkAddModal.jsx:462-473` | 일괄 등록 **입력 컬럼** 정의 (`실적_1월`…`실적_12월`) |
| `DigitalTwinDashboardApp.jsx:1923-1927`, `2223-2238` | 저장 직전 **`실적_N월` → `월별실적` 배열로 변환** |
| `importExportUtils.js` | CSV 내보내기/가져오기 컬럼 |

> **결정: 정본은 `월별실적`(배열). `실적_N월`은 컬럼을 만들지 않는다.**
> 운영 값이 0건이고, 저장 시 배열로 변환되는 입력 포맷일 뿐이다.
> 잔재로 남은 12개 키는 **`extra_fields`가 자동 흡수**하므로 별도 처리가 필요 없고 유실도 없다.
> → 앞서 추가했던 `실적_월별_평면 JSONB` 컬럼은 **불필요하므로 제거**한다.

#### ⚠️ 이 조사에서 발견된 잠재 버그 (별건, 이관과 무관)

`DigitalTwinDashboardApp.jsx:1920`

```js
currentPerformances[existingIndex] = { ...currentPerformances[existingIndex], ...perfData };
```

일괄 등록에서 **uuid가 있는 행(= 기존 성과 수정)** 은 입력 객체를 통째로 spread 한다. 그래서

1. `실적_1월`~`실적_12월` 원본 키가 성과 객체에 그대로 섞여 저장된다 → **운영에 12개 빈 키가 남은 원인**
2. 이 경로는 **`실적_N월` → `월별실적` 변환을 하지 않는다** (신규 추가 경로 `:1923`에만 있음)
   → 일괄 수정으로 월별 실적을 입력하면 **저장은 되지만 화면에 표시되지 않는다**

현재는 값이 0건이라 무해하지만, 일괄 수정 기능을 쓰는 순간 조용히 어긋난다.
**Phase 1에서 함께 고치는 것을 권장**한다(입력 필드 화이트리스트 + 변환 적용).

---

> 참고: `GET /data`가 성과의 `_deleted`를 걸러내므로 **삭제된 성과는 프론트에 내려가지 않는다.**
> 운영 성과 596건 중 일부는 이미 삭제 상태이며, 이관 시 `is_deleted`로 함께 옮긴다.
> 또한 월별 실적을 실제로 쓰는 성과는 **596건 중 16건뿐**이다 (기능 사용률이 낮음).

---

## 1. 확정된 결정사항

| # | 항목 | 결정 |
|---|---|---|
| 1 | **AI 쓰기 방식** | **저위험 필드는 즉시 반영, 핵심 필드는 제안 후 사람 승인** |
| 2 | **권한 모델** | **소유자(과제PL) + 같은 부서 manager + admin** |
| 3 | **착수 순서** | **Phase 0(소유자 매핑표 + updatedAt 백필) 먼저** |
| 4 | **마이그레이션 방식** | **V2 병행 + Additive-only** (아래 2장) |

---

## 2. 마이그레이션 방식 — 무엇을 어떤 순서로 하는가

### 2.0 한 문장 요약

> **기존 테이블은 그대로 둔 채, 옆에 새 테이블을 만들어 데이터를 "복사"하고,
> 두 벌이 논리적으로 같다는 것을 자동 비교로 증명한 뒤에야 쓰기를 새 쪽으로 옮긴다.**

기존 `dashboard_data`를 **변형하지 않는다.** ALTER도, 데이터 이동도 없다. 읽기만 한다.

> ⚠️ 흔한 오해: "테이블을 하나씩 차례대로 만든다"는 뜻이 아니다.
> 테이블 6개는 **Alembic 마이그레이션 한 번에 같이 생성**한다.
> "차례대로"인 것은 **테이블이 아니라 단계(Stage)** — 만들기 → 복사 → 검증 → 읽기 전환 → 쓰기 전환 → 옛것 폐기.

### 2.1 시간 순서로 본 상태 변화

```
[지금]
┌────────────────────────────────────────────┐
│ dashboard_data (1행, JSON 통짜)             │ ◀── 프론트가 읽고 쓴다 = 정본
│   projects[328]   performances[432]        │
└────────────────────────────────────────────┘


[Phase 2-1]  빈 테이블만 추가 — 데이터와 무관, 위험 0
┌────────────────────────────────────────────┐
│ dashboard_data (1행)                        │ ◀── 여전히 정본. 아무 변화 없음
└────────────────────────────────────────────┘
┌────────────────────────────────────────────┐
│ dt2_projects(0행)  dt2_performances(0행)    │ ◀── 빈 껍데기만 생성
│ dt2_project_performance(0행)  …             │
└────────────────────────────────────────────┘


[Phase 2-2]  복사 — 원본은 "읽기"만 당한다
   dashboard_data ────읽기────▶ dt2_projects(328행), dt2_performances(432행), …
   ※ 이 시점부터 데이터가 두 벌. 그러나 정본은 여전히 dashboard_data


[Phase 2-3]  두 벌이 같다는 것을 증명
   V1 GET /data 응답   vs   V2 어댑터가 조립한 응답   →   필드 단위 diff = 0 이어야 통과
   (통과 못 하면 여기서 멈춘다. 원본은 아직 아무 손상 없음)


[Phase 3 컷오버]  쓰기를 옮긴다 — 유일하게 동결이 필요한 순간 (수십 분 목표)
   dashboard_data ──읽기 전용 동결──▶ 마지막 증분 복사 ──▶ 재검증 ──▶ dt2_* 가 정본이 됨


[컷오버 이후 1~2주]
   dashboard_data 를 읽기 전용으로 그대로 보존 (롤백 지점)

[그 뒤]
   안정 확인 후 dashboard_data 폐기
```

### 2.2 왜 "복사"인가 — 인플레이스 변환과의 비교

| | 인플레이스 변환 (기존 테이블을 직접 바꿈) | **복사 방식 (채택)** |
|---|---|---|
| 원본 | ALTER + 데이터 이동으로 **변형됨** | **한 번도 안 건드림** (읽기만) |
| 중간 실패 시 | 원본이 이미 변형된 상태 → **백업 복원 외 방법 없음** | `DROP TABLE dt2_*` 한 줄이면 없던 일 |
| 복원 시 손실 | 백업 시점 이후 사용자 입력분 **전부 소실** | 손실 없음 (원본이 계속 정본) |
| 검증 시점 | 변환한 뒤에야 확인 가능 (이미 늦음) | 전환 **전에** 두 벌 비교로 증명 |
| 비용 | 낮음 | 전환 기간 동안 코드 두 벌 유지 |

**핵심**: 복사 방식은 **되돌릴 수 없는 순간이 컷오버 한 번뿐**이고, 그 직전까지 모든 검증이 끝나 있다.
인플레이스는 변환이 시작되는 순간부터 되돌릴 수 없다.

### 2.3 자주 나오는 질문

**Q. 두 벌이면 서로 어긋나지 않나?**
어긋날 수 없다. **쓰기가 한 쪽에서만 일어나기 때문이다.** 컷오버 전까지 사용자의 모든 입력은
`dashboard_data`에만 기록되고, `dt2_*`는 그것을 복사해 받기만 한다.
어긋난 것 같으면 복사를 다시 돌리면 된다 — 복사 스크립트는 재실행 안전(idempotent)이라
몇 번을 돌려도 같은 결과가 된다. (→ 원칙 2·3)

**Q. 복사하는 동안 서비스는 어떻게 되나?**
정상 동작한다. 사용자는 아무것도 느끼지 못한다. 화면도 저장도 그대로다.
복사는 백그라운드에서 원본을 읽기만 하기 때문이다. **중단(down time)은 Phase 3 컷오버 때 수십 분뿐이다.**

**Q. 프론트엔드는 언제 바뀌나?**
Phase 3-5까지 거의 바뀌지 않는다. 서버가 새 테이블의 행들을 읽어 **기존과 똑같은 JSON 배열 모양으로
조립해서** 내려주기 때문이다(어댑터). 프론트 입장에선 응답 형태가 그대로라 바뀐 것을 모른다.

**Q. 데이터가 두 벌이면 용량이 두 배 아닌가?**
과제 328건 + 성과 432건이 1.2 MB 수준이라 두 벌이어도 미미하다.
(오히려 정리 대상인 스냅샷 41 MB 쪽이 훨씬 크다. → Phase 1-3)

**Q. 두 벌 유지의 실제 비용은?**
**백엔드 코드가 두 벌**이라는 것이다. 이것이 이 방식의 유일한 실질 비용이고,
**전환 기간을 짧게 가져가는 것이 유일한 방어책**이다. (→ Phase 3 컷오버 런북)

**Q. 더 조심스럽게 쪼갤 수는 없나?**
`dt2_projects`(과제)만 먼저 만들고 성과는 나중에 하는 것도 가능하다.
다만 과제↔성과 참조 때문에 어댑터가 복잡해지므로, **과제·성과·연결 3개를 한 번에 가는 쪽을 권한다.**

### 2.4 되돌리기 지점 정리

| 단계 | 되돌리는 방법 | 데이터 손실 |
|---|---|---|
| Phase 2-1 (테이블 생성) | `flask db downgrade` → `DROP TABLE dt2_*` | 없음 |
| Phase 2-2 (복사) | `TRUNCATE dt2_*` 후 재복사, 또는 전체 DROP | 없음 (원본 무손상) |
| Phase 2-3 (검증 실패) | 그냥 멈춘다. 서비스 영향 없음 | 없음 |
| Phase 3 컷오버 직후 | V1을 다시 정본으로 되돌림 (V1 보존 중) | 컷오버 이후 입력분만 |
| Phase 3 + 1~2주 경과 | 백업 복원 | 백업 시점 이후 |

> **Phase 2 전체가 "언제든 없던 일로 만들 수 있는" 구간이다.**
> 진짜 되돌리기 어려운 지점은 컷오버 이후뿐이고, 그래서 V1을 1~2주 보존한다.

---

## 2.5 운영 DB 안전 원칙 — 이 5가지를 어기지 않는다

### 원칙 1. 기존 테이블은 끝까지 건드리지 않는다 (Additive only)

```
신규 생성만(8개):  dt2_projects, dt2_performances, dt2_project_performance,
                   dt2_project_dependencies, dt2_project_attachments,
                   dt2_project_editors, dt2_change_proposals, dt2_report_images
읽기만:            dashboard_data, dashboard_snapshots, project_attachments,
                   dashboard_activity_logs, users, divisions, departments
ALTER/DROP:        없음
```

→ Alembic 마이그레이션이 **전부 `create_table`뿐**이다. 실패해도 기존 데이터는 물리적으로 손댈 수가 없다.
`downgrade()`는 `drop_table`뿐이라 되돌리기도 자명하다.

> 예외 하나: `departments.division_id` 백필(Phase 0-4)은 기존 테이블 UPDATE다.
> 단 **현재 전 행이 NULL**이라 덮어쓸 값이 없다 → 정보 손실 위험 0. 그래도 백업 후 수행.

### 원칙 2. 데이터 이관은 "이동"이 아니라 "복사"다

- **컷오버 전까지 V1(`dashboard_data`)이 계속 정본(source of truth)이다.** V2는 파생본.
- 복사 스크립트는 **재실행 안전(idempotent)** — uuid 기준 upsert. 몇 번 돌려도 같은 결과.
- **V2가 꼬이면 V2를 통째로 비우고 다시 복사하면 끝.** 이게 가장 강력한 안전장치다.
  운영 데이터는 V1에 그대로 있으므로 어떤 실수도 복구 가능하다.

### 원칙 3. 쓰기 방향은 항상 한 쪽뿐 — 양방향 동기화 금지

```
Stage A   V1 쓰기(정본)  →  V2 복사              [V2는 읽기 전용, 검증만]
Stage B   V1 쓰기(정본)  →  V2 복사              [V2 API 읽기 검증 추가]
── 컷오버 (짧은 동결 창) ──
Stage C   V2 쓰기(정본)      V1 읽기 전용 보존    [롤백용]
```

**양방향 쓰기는 하지 않는다.** 데이터가 꼬이는 사고의 대부분이 양방향 동기화에서 나온다.

### 원칙 4. 검증 스크립트를 코드보다 먼저 만든다

`V1 GET /data` 응답과 `V2 GET /data` 응답을 자동 비교하는 diff 스크립트를 **API를 만들기 전에** 작성한다.
매 복사 직후 자동 실행하고, **불일치가 0이 아니면 다음 단계로 진행하지 않는다.**

### 원칙 5. 되돌릴 지점을 매 단계 앞에 만든다 — 그리고 복원을 실제로 해본다

> ⚠️ **현재 최신 DB 덤프가 `database_dump_20251125_160756.sql` — 8개월 전이다.**
> Phase 0 이전에 새 덤프를 뜨는 것이 이 프로젝트의 0번 작업이다.

- 각 Phase 시작 전 `python export_database.py` 실행 → 타임스탬프 덤프 확보
- **`dxdigitaltwin_test`에 복원해서 실제로 되살아나는지 확인한다.** 백업이 있는 것과 복원되는 것은 다르다.
- 코드는 zip 스냅샷(`fullstack###.zip`) 관례를 유지 — Phase 시작 전 스냅샷

---

## 2.6 운영서버 제약 — AI 없음 · 인터넷 없음

### 2.6.1 제약 정의

| | 개발서버 (이 PC) | **운영서버** |
|---|---|---|
| AI 도구 | ✅ 사용 가능 | ❌ **사용 불가** |
| 인터넷 | ✅ | ❌ **완전 오프라인** (`backend/packages`에 wheel 69개 사전 반입 방식) |
| 코드 작성·디버깅 | ✅ | ❌ 하지 않는다 |
| 시행착오 | ✅ 얼마든지 | ❌ **한 번에 성공해야 함** |

### 2.6.2 지배 원칙

> **운영서버에서는 "판단"을 하지 않는다. "실행"과 "확인"만 한다.**
> 판단이 필요한 모든 것은 개발서버에서 미리 끝내고, 운영서버에는 **결과물(스크립트·데이터 파일·체크리스트)만** 가져간다.

운영서버에서 사람이 하는 일은 세 가지뿐이다:

```
1. 정해진 명령어를 순서대로 복붙해서 실행한다
2. 화면에 나온 결과가 "기대값"과 같은지 확인한다  (기대값은 런북에 미리 적혀 있다)
3. 다르면 → 즉시 중단하고, 준비된 롤백 명령을 실행한 뒤, 로그 파일을 개발서버로 가져온다
```

**SQL을 즉석에서 짜거나, 오류 메시지를 보고 판단하거나, 스크립트를 수정하는 일은 없어야 한다.**

### 2.6.3 ⚠️ 결정적 제약 — 운영 데이터를 개발서버로 가져올 수 없다

**운영 DB 덤프 반출이 불가능하다.** 따라서:

- 개발서버에서 **진짜 데이터로 리허설할 수 없다**
- 운영서버에서는 **AI도 없고 시행착오도 불가능하다**
- → 개발·운영 양쪽 모두에서 "실제 데이터를 보며 고치는" 방법이 막혀 있다

이 조합을 정면으로 뚫는 것이 아래 **3대 설계 변경**이다.
(이것 없이는 이관 스크립트가 운영서버에서 실패할 때 대응할 방법이 없다.)

---

#### 변경 1 ★★★ `extra_fields` catch-all — 유실을 구조적으로 불가능하게 만든다

인벤토리(2-0)는 **개발 DB만 보고 만든 것**이다. 운영 데이터에 내가 모르는 키가 있을 수 있고,
그러면 이관 시 **조용히 유실**된다. 반출이 안 되므로 사전에 확인할 방법도 없다.

**해법: 스키마에 매핑되지 않은 모든 키를 통째로 보관하는 컬럼을 둔다.**

```sql
ALTER ... dt2_projects      ADD extra_fields JSONB NOT NULL DEFAULT '{}';
ALTER ... dt2_performances  ADD extra_fields JSONB NOT NULL DEFAULT '{}';
```

```python
# 이관 규칙: 알려진 키는 컬럼으로, 나머지는 전부 extra_fields로
known = INVENTORY['projects']            # 설정 파일에서 로드 (변경 2 참조)
row.extra_fields = {k: v for k, v in project.items() if k not in known}
```

| | catch-all 없이 | **catch-all 있으면** |
|---|---|---|
| 모르는 키를 만나면 | 유실 (조용히) 또는 이관 중단 | **자동 보존.** 이관 계속 진행 |
| 어댑터 응답 | 원본과 달라짐 | `extra_fields`를 펼쳐 **원본과 100% 동일** |
| 사후 발견 시 | 재이관 필요 | `extra_fields`에서 컬럼으로 승격만 하면 됨 |

> **이 한 가지로 "인벤토리 누락 = 데이터 손실"이라는 최대 위험이 사라진다.**
> 인벤토리는 여전히 중요하지만, 틀려도 **치명적이지 않게** 된다.

---

#### 변경 2 ★★ 리허설을 운영서버 안에서 한다 (반출 아님)

데이터를 밖으로 못 내보낼 뿐, **운영서버 안에서 복제하는 것은 반출이 아니다.**

```
[운영서버 내부]
  dxdigitaltwin              (운영 정본 — 절대 안 건드림)
        │ pg_dump | psql      ← 서버 밖으로 나가지 않음
        ▼
  dxdigitaltwin_stg          (리허설용. 여기서 이관 스크립트를 몇 번이든 실행)
        │
        └─ 실패해도 운영에 영향 0. DROP 후 재생성하면 끝
```

- `dxdigitaltwin_test`가 이미 존재하므로 **관례상 무리 없다**
- 스크립트는 `--db` 인자로 대상 DB를 받아, **staging과 운영에 같은 코드**를 쓴다
- staging에서 `[OK]`가 나온 뒤에만 운영에 적용한다

---

#### 변경 3 ★★ 검증을 "왕복(round-trip) 비교"로 — 결과 숫자만 반출

값을 반출할 수 없으니 개발서버에서 비교할 수 없다. **비교를 운영서버 안에서 끝낸다.**

```
운영 JSON 원본  ──이관──▶  dt2_* 행  ──어댑터로 재조립──▶  JSON
        └──────────────── 정규화 후 완전 비교 ────────────────┘

반출하는 것: "과제 328건 중 328건 일치, 불일치 0" ← 숫자와 키 이름뿐. 값은 나가지 않음
```

`extra_fields`(변경 1)가 있으면 **이 왕복 비교가 100% 일치할 수 있다.**
일치하지 않는다 = 이관 로직에 버그가 있다는 뜻이므로, 판정이 명확해진다.

---

#### 개발서버에서는 무엇으로 개발하나 — 합성 데이터

개발서버에는 **구조만 같고 값은 가짜인 데이터셋**을 만들어 쓴다.

| 만들 것 | 근거 |
|---|---|
| 과제 300여 건 (활성/삭제/영구삭제 혼재) | 2-0 인벤토리의 키·타입·보유율 |
| 레거시 포맷 혼재 (`성과항목UUID`/`성과항목ID`/`id` 3종) | 2-0 조사 결과 |
| 고아 참조, `updatedAt` NULL, 양방향 불일치 등 **예외 케이스를 의도적으로 심는다** | 실제 데이터에서 발견된 패턴 |
| 모르는 키 (`__unknown_test__`) | **catch-all 동작 검증용** |

> 합성 데이터로는 **로직**을 검증하고, 실제 데이터로는 **운영서버 staging에서** 검증한다.
> 역할을 나누면 반출 없이도 양쪽이 성립한다.

---

#### 그래서 실제 작업 루프는 이렇게 된다

```
① 개발서버   스크립트 작성 + 합성 데이터로 검증          (AI 사용)
      │ 스크립트 파일만 반입 (데이터 아님)
      ▼
② 운영 staging   --dry-run 실행 → [OK] / [FAIL]         (AI 없음, 사람이 실행)
      │ [FAIL]이면 진단 메시지만 반출 (값 없이 키 이름·건수·타입만)
      ▼
③ 개발서버   원인 수정 → 합성 데이터에 해당 케이스 추가   (AI 사용)
      │ 재반입
      ▼
②로 복귀 … [OK] 나올 때까지
      ▼
④ 운영 정본   같은 스크립트를 --commit 으로 실행
```

**설계 목표는 ②↔③ 왕복 횟수를 줄이는 것이다.** 그래서 변경 1(catch-all)이 중요하다 —
모르는 키 때문에 왕복이 발생하는 일 자체가 없어지기 때문이다.

> ⚠️ **본 문서의 모든 수치(과제 328·성과 432·이미지 3건·`reportConfirmations` 등)는 개발 DB 기준이다.**
> 운영 수치는 Phase 0-1의 스캔 스크립트를 **운영서버에서 실행해** 확인한다(결과는 숫자·키 이름뿐이라 반출 부담이 낮다).

### 2.6.4 운영서버 반입 산출물 요구사항

모든 스크립트는 아래 7가지를 **반드시** 갖춘다. 하나라도 없으면 운영 반입 금지.

| # | 요구사항 | 이유 |
|---|---|---|
| 1 | **단일 진입점** `python dt2_migrate.py --step N` | 사람이 SQL을 짜지 않게 한다 |
| 2 | **`--dry-run` 모드** — 무엇이 바뀔지만 출력, 쓰기 없음 | 실제 실행 전 반드시 1회. 결과를 런북 기대값과 대조 |
| 3 | **사전조건 자동 검사** — 안 맞으면 **실행 거부** | 백업 존재? alembic head 일치? 과제 수 예상 범위? 이미 실행됨? |
| 4 | **단일 트랜잭션 + 실패 시 자동 롤백** | 중간 상태가 남지 않게. 사람이 판단할 여지를 없앤다 |
| 5 | **롤백 스크립트 동봉** `python dt2_rollback.py --step N` | 즉석 대응 금지. 되돌리기도 미리 만들어 간다 |
| 6 | **로그 파일 출력** `logs/dt2_migrate_YYYYMMDD_HHMM.log` | 개발서버로 가져와 분석. 화면만 보고 판단하지 않는다 |
| 7 | **기대 출력값을 스스로 검증** — `[OK]` / `[FAIL]` 로 단정적 출력 | "이게 정상인가?"를 사람이 해석하지 않게 |

**출력 예시 (사람이 해석할 필요 없게):**

```
[1/6] 사전조건 검사
      백업 파일          : database_dump_20260728_0930.sql  [OK]
      alembic head       : d8e2c4f51a93 일치                [OK]
      과제 수            : 328 (예상 320~340)               [OK]
      dt2_projects 상태  : 비어 있음                        [OK]
[2/6] 과제 이관 (dry-run)
      신규 328 / 갱신 0 / 스킵 0                            [OK]
      미매핑 키          : 0개                              [OK]
      owner_user_id NULL : 0건                              [OK]
─────────────────────────────────────────────
결과: [OK] 6/6 통과. 실제 실행하려면 --commit 을 붙이세요.
```

### 2.6.5 오프라인 패키지 대응

인터넷이 없으므로 **신규 파이썬 패키지는 wheel을 사전 반입**해야 한다.

| Phase | 신규 패키지 | 대응 |
|---|---|---|
| 1-4 | `Flask-Compress` (gzip) | wheel + 의존성(`brotli` 등)을 `backend/packages`에 사전 반입 |
| 1-2 | 이미지 처리 | **가급적 신규 패키지 없이** 프론트 canvas로 처리 (서버측 Pillow 도입 회피) |
| 2~4 | 이관·검증 스크립트 | **표준 라이브러리 + 기존 설치분(SQLAlchemy/psycopg)만 사용** |

> 원칙: **이 프로젝트에서 신규 서버 패키지 도입은 `Flask-Compress` 하나로 제한한다.**
> 그마저도 반입이 번거로우면 **리버스 프록시(nginx/IIS)의 gzip 설정으로 대체** 가능 — 패키지 0개.

### 2.6.6 단계별 실행 위치

| Phase | 개발서버에서 | 운영서버에서 (AI 없이 사람이) |
|---|---|---|
| **0-0** | 복원 리허설, 검증 | `export_database.py` 실행 → **덤프 파일 반출** |
| **0-1** | 스캔 스크립트 작성·검증 | `dt_scan.py` 실행 → 결과 md 반출 |
| **0-2** | 매핑표 자동생성 + 현업 회신 반영 → **CSV 확정** | (없음 — CSV는 개발서버에서 완성) |
| **0-3** | 백필 스크립트 작성 + 복제본에서 리허설 | `dt_backfill.py --dry-run` → 확인 → `--commit` |
| **0-4** | 조직 정리 스크립트 작성 + 리허설 | `dt_org_fix.py --dry-run` → 확인 → `--commit` |
| **1** | 코드 수정 전부 | **빌드 산출물(zip) 배포 + 서비스 재시작만** |
| **2-1** | 마이그레이션 파일 작성·검증 | `flask db upgrade` (테이블 생성) |
| **2-2** | 이관 스크립트 완성 (복제본에서 N회 반복) | `dt2_import.py --dry-run` → `--commit` |
| **2-3** | 검증 스크립트 완성 | `dt2_verify.py` 실행 → **[OK] 아니면 즉시 중단** |
| **3** | API·프론트 개발, 컷오버 런북 작성 | 런북대로 배포 + 동결 + 전환 |
| **4** | MCP 서버 개발 | 배포 |

### 2.6.7 운영서버 실행 런북 형식 (모든 단계 공통)

```
□ 단계 N: <무엇을 하는지 한 줄>

  1) 실행할 명령 (그대로 복사)
     cd C:\...\backend
     venv\Scripts\activate
     python scripts\dt2_import.py --step 2 --dry-run

  2) 이 결과가 나와야 정상
     결과: [OK] 6/6 통과

  3) [OK]가 아니면
     → 아무것도 더 실행하지 말 것
     → logs\ 폴더의 최신 .log 파일을 개발서버로 전달
     → 담당자 연락

  4) [OK]면 실제 실행
     python scripts\dt2_import.py --step 2 --commit

  5) 되돌리려면
     python scripts\dt2_rollback.py --step 2
```

### 2.6.8 이 제약이 일정에 주는 영향

| 항목 | 영향 |
|---|---|
| 개발 공수 | **증가** — dry-run·사전조건검사·롤백·로그를 전부 만들어야 함 (Phase당 +0.5~1일) |
| 운영 리스크 | **감소** — 시행착오가 원천 차단됨 |
| 전제조건 추가 | **운영 덤프 반입**이 Phase 0-0에 추가 (없으면 리허설이 무의미) |
| 신규 패키지 | `Flask-Compress` 1개로 제한, 또는 프록시 gzip으로 0개 |

> **총 일정 약 3주 → 약 3.5~4주로 조정.** 스크립트 견고화 비용이 추가된다.
> 다만 이것은 낭비가 아니라, **AI 없이 한 번에 성공해야 하는 환경에서 유일하게 합리적인 투자**다.

---

## 3. 사전 점검에서 나온 추가 문제 (계획에 반영됨)

> ⚠️ **아래는 전부 개발 DB 관찰이다.** 운영에서 같은 문제가 같은 규모로 있는지는 Phase 0-1 스캔으로 확인한다.
> "구조" 열이 ✅면 코드에서 비롯된 것이라 **운영에도 반드시 존재**하고, ❌면 데이터 품질 문제라 **운영은 다를 수 있다.**

| # | 발견 (개발 DB 기준) | 구조? | 영향 | 반영 |
|---|---|---|---|---|
| A | `과제PL`이 자유 텍스트 → users와 연결 없음. 매칭 4/224건(1.8%) | ✅ 연결 부재는 구조<br>❌ 1.8%는 테스트값 탓 | 권한의 주체가 없음 | Phase 0-2 |
| B | `updatedAt` 199/328건 NULL → 병합이 무조건 덮어쓰기 | ✅ 비교 로직은 구조<br>❌ NULL 비율은 미지 | 조용한 데이터 손실 | Phase 0-3 |
| C | `divisions` 중복 24행 (실제 8개) | ❌ 데이터 | FK id 특정 불가 | Phase 0-4 |
| D | `departments.division_id` 전 행 NULL | ❌ 데이터 | "같은 부서 manager" 계산 불가 | Phase 0-4 |
| E | 최신 DB 덤프가 8개월 전 | ❌ 운영 백업 주기는 별도 확인 | 롤백 지점 없음 | Phase 0-0 |
| F | 보고서 이미지 base64 인라인 (개발에선 payload의 55%) | ✅ **저장 방식이 구조** | AI 컨텍스트 낭비 + 저장 지연 | Phase 1 |
| G | `linkedProjects` 역방향 참조 존재 | ✅ 구조 | 단방향 이관 시 연결 유실 | 2-0 판단사항 라 |
| H | `reportConfirmations`가 과제 uuid 키로 `module_settings`에 존재 | ✅ 구조 | 미이관 시 보고확인 상태 소실 | 2-0 판단사항 바 |
| I | `isEditing`·`_idChanged` 등 UI 임시 상태가 DB에 저장됨 | ✅ 구조 | 불필요 데이터 이관 | 2-0 판단사항 마 |

**운영에서 다를 가능성이 높은 것 (좋은 쪽으로)**: A의 매칭률.
개발 데이터엔 `PL0`~`PL9`(각 20건), `test`, `TTTT`, `SDFSDF` 등 테스트 과제가 다수다.
운영은 실제 담당자 이름일 테니 **`과제PL` 이름 매칭률이 훨씬 높을 수 있고, 그러면 Phase 0-2가 크게 짧아진다.**

**운영에서 더 나쁠 가능성이 있는 것**: F의 이미지.
개발엔 3건뿐이지만 운영에서 보고서 기능을 활발히 썼다면 훨씬 많을 수 있다. → Phase 0-1 스캔의 최우선 확인 항목.

> **C·D는 결정 2("같은 부서 manager")를 실행하려면 반드시 먼저 고쳐야 한다.**
> 과제의 `사업부` 값 자체는 8종(MX/VD/DA/NW/의료기기/GTR/SR/CS)으로 깨끗하고 divisions 이름과 100% 매칭된다.
> 문제는 divisions 쪽 중복과 departments 연결 누락이다.

---

## 4. Phase 0 — 기반 데이터 정리 (2~3일 + 현업 확인 대기)

> **코드 배포가 없는 단계다.** 스크립트와 데이터만 다룬다. 여기서 실패해도 서비스에 영향이 없다.

### 0-0. 백업 확보 (30분) ★ 최우선

> **데이터는 운영서버 밖으로 나가지 않는다.** 백업도 복원 리허설도 전부 운영서버 안에서 끝낸다. (→ 2.6.3 변경 2)

| 실행 위치 | 작업 | 산출물 |
|---|---|---|
| **운영서버** | `python export_database.py` 실행 | `database_dump_20260728_*.sql` (**서버 내부에 보관**) |
| **운영서버** | 덤프를 `dxdigitaltwin_stg`로 복원 → 복원 성공 확인 | 리허설용 staging DB |
| **운영서버** | 이후 모든 이관 리허설을 `_stg`에서 수행 | — |
| 개발서버 | 합성 데이터셋 생성 (구조만 동일, 값은 가짜) | 로직 검증 환경 |
| 개발서버 | 코드 zip 스냅샷 | `fullstack101.zip` |

**검증 게이트**:
- `dxdigitaltwin_stg`에서 `dashboard_data` 행이 살아나고 과제가 조회된다 → **백업이 실제로 복원됨을 증명**
- staging DB 생성으로 운영 정본에 아무 영향이 없음을 확인

> ⚠️ **덤프는 반출하지 않는다.** 운영서버 안에서 뜨고, 운영서버 안에서 복원하고, 운영서버 안에서 리허설한다.
> 개발서버는 합성 데이터로 로직만 검증한다. (역할 분담 → 2.6.3)

### 0-1. 운영 현황 스캔 (0.5일) ★★ 본 문서 모든 수치의 기준선

> 개발 데이터로 세운 가정을 **운영 실측으로 교체하는 단계.** 이 결과 없이는 Phase 2 규모를 확정할 수 없다.

읽기 전용 스크립트 `scripts/dt_scan.py`를 **운영서버에서** 실행한다.

| 확인 항목 | 왜 |
|---|---|
| **키 인벤토리 (과제·성과)** | 2-0의 55/31개 목록에 **없는 키가 운영에 있는지**. 있으면 목록에 추가 (없어도 `extra_fields`가 받아냄) |
| **base64 이미지 보유 과제 수·총량** ★ | 개발 3건. 운영이 훨씬 많으면 **Phase 1의 우선순위·공수가 달라진다** |
| 과제·성과 건수, payload 크기 | 이관 규모 산정 |
| `updatedAt` NULL 건수 | Phase 0-3 백필 범위 |
| **`과제PL`/`작성자`/`knoxId` → users 매칭률** ★ | **Phase 0-2(임계경로)의 실제 길이를 결정** |
| `divisions` 중복·`departments.division_id` NULL 여부 | Phase 0-4 필요 여부 |
| 스냅샷 건수·용량, 첨부파일 건수·용량 | Phase 1-3 정리 범위 |
| 고아 성과 참조, `linkedProjects` 양방향 불일치 건수 | 이관 예외 규모 |
| `module_settings`의 과제 uuid 종속 키 | `reportConfirmations` 외에 더 있는지 |

**출력 형식**: 숫자·키 이름·비율만. **값은 출력하지 않는다** (반출 부담 최소화).

**산출물**: `dt_scan_YYYYMMDD.md` → 이 문서의 2-0 인벤토리와 3장 수치를 **운영 기준으로 갱신**한다.

> 이 단계에서 나올 수 있는 결과 두 가지:
> - **좋은 쪽**: `과제PL` 매칭률이 높다 → Phase 0-2가 며칠 단축된다
> - **나쁜 쪽**: 이미지 보유 과제가 수십 건이다 → Phase 1-2를 **Phase 1의 최우선**으로 올린다

### 0-2. 과제 소유자 확정 ★ 이 프로젝트의 진짜 임계경로

**자동으로 정할 수 없다. 사람이 결정해야 한다.**

**매핑 우선순위** (2-0 인벤토리 조사에서 발견된 knoxId 경로를 최우선으로 시도):

```
1순위  과제참여인력목록[].knoxId  →  users.email 로컬파트      (42개 과제 보유, 결정론적)
       예: yjtwin.park → yjtwin.park@samsung.com, nox321 → nox321@naver.com
2순위  과제PL 이름               →  users.name                (활성 224건 중 4건만 매칭)
3순위  작성자 이름               →  users.name                (21건)
4순위  현업 회신
5순위  admin 임시 소유
```

| 단계 | 내용 |
|---|---|
| a | 위 1~3순위를 자동 적용한 **매핑표 CSV** 생성 (근거 순위를 열로 함께 기록) |
| b | 남은 미매칭 목록을 현업에 회람 → 담당자 지정 |
| c | 회신을 매핑표에 반영, 미확정분은 **admin 소유로 임시 지정**(업무 안 막힘) |
| d | 매핑표를 `dt2_projects.owner_user_id` 이관 스크립트 입력으로 사용 |

> **c가 중요하다.** "전부 확정될 때까지 대기"하면 프로젝트가 멈춘다.
> 미확정은 admin 소유로 두고 순차 이관하면 Phase 2~4를 병렬로 진행할 수 있다.

**검증 게이트**: 활성 과제 224건 **전부**가 유효한 `owner_user_id`를 가진다(admin 임시 포함).

### 0-3. `updatedAt` 백필 (0.5일) — MCP와 무관하게 지금 위험

NULL 199건을 채운다. 우선순위: `updated_at` → `createdAt` → 활동로그의 마지막 변경 시각 → 고정 기준시각.

```python
# 원칙: 실제보다 과거로 넣는다. 미래로 넣으면 이후 정상 수정이 스킵된다.
fallback = '2020-01-01T00:00:00.000Z'
```

**주의**: V1 `dashboard_data`를 직접 UPDATE하는 유일한 작업이다.
→ 백업 후, `dxdigitaltwin_test`에서 먼저 실행해 결과를 확인하고 운영에 적용한다.

**검증 게이트**: `updatedAt` NULL 0건. 과제 수 328 불변. `projects` 배열 길이 불변.

### 0-4. 조직 데이터 정리 (0.5일)

| 작업 | 내용 |
|---|---|
| a | `divisions` 중복 24행 → 정규 8행 확정 (나머지는 `is_active=false`, **삭제하지 않음**) |
| b | `departments.division_id` 백필 (30개 부서 → 8개 사업부) — **현업 확인 필요** |
| c | `users.department` 미기입자 확인 |

**검증 게이트**: `departments.division_id` NULL 0건. 과제의 `사업부` 8종이 전부 정규 division에 매핑됨.

### Phase 0 롤백

- 0-3, 0-4만 데이터를 쓴다. 둘 다 백업에서 해당 테이블만 복원하면 원복.
- 나머지는 읽기 전용이라 롤백 대상 아님.

---

## 5. Phase 1 — 성능 개선 (1.5~2일, Phase 0과 병렬 가능)

> Phase 0의 현업 확인을 기다리는 동안 진행한다. **MCP 준비 작업이기도 하다.**

### 1-1. 보고서 이미지 압축 (0.5일) — 즉시 출혈 차단

`DetailInfoModal.jsx:663-670`

```js
// 현재: reader.readAsDataURL(file)          → 원본 그대로
// 개선: canvas 리사이즈(최대 1600px) + toDataURL('image/jpeg', 0.75)
//       + 원본 10MB 초과 거부
```

**신규 업로드에만 적용**되므로 위험이 없다. 기존 3건은 1-2에서 처리.

### 1-2. 보고서 이미지 → 파일 저장 분리 (1일)

```
현재:  과제JSON.이미지_좌측 = [{ dataUrl: "data:image/jpeg;base64,…134kB", caption, fileName }]
개선:  과제JSON.이미지_좌측 = [{ imageId: 123, caption, fileName }]     ← ~80 B
       실체: dt2_report_images 테이블 + backend/uploads/digital-twin-dashboard/
       조회: GET /report-images/<id>
```

- 신규 테이블 1개 추가(Additive) + 기존 3개 과제의 JSON 치환
- **이관 스크립트는 idempotent**: 이미 `imageId` 형태면 건너뜀
- **롤백**: 원본 base64를 `dt2_report_images.original_json`에 함께 보관 → 되돌리기 가능

**검증 게이트**: 이관 후 보고서 화면에서 7개 이미지가 전부 보인다. `projects` payload가 888 kB → 400 kB 이하.

### 1-3. 자동 스냅샷 정책 변경 + 정리 (0.5일)

- `routes.py:1087` upsert 자동 스냅샷 제거 (덮어쓰기 `/data/upload`에만 유지)
- 보존 정책: `auto` 최근 30개만. `manual`/`upload` 타입은 **전량 보존**
- 기존 358건 중 정리 대상만 삭제 → 디스크 41 MB 회수

**주의**: 삭제 전 덤프 필수. 스냅샷은 사실상 백업이므로 신중히.

### 1-4. gzip + `ensure_ascii=False` (0.5시간)

```python
from flask_compress import Compress
Compress(app)
app.json.ensure_ascii = False
```

**검증 게이트**: `GET /data` 응답 Content-Encoding이 gzip, 크기 1/8 이하.

### 1-5. 강제 덮어쓰기 방어 (0.5일) — 고아 첨부·데이터 증발의 유력 원인 차단

`routes.py:695, 702` 의 `forceUpload=true` 경로는 **버전 충돌 검사를 건너뛰고 전체 배열을 통째로 덮어쓴다.**
오래된 스냅샷을 가진 클라이언트가 이걸 쓰면 **그 사이 다른 사람이 만든 과제가 통째로 증발**한다.
(고아 첨부 6건 = "과제는 사라지고 첨부만 남은" 흔적으로 추정)

| 조치 | 내용 |
|---|---|
| 서버 | `forceUpload` 시 **덮어쓰기로 사라지는 uuid 목록을 계산해 경고 로그 + 활동로그에 기록** |
| 서버 | 사라지는 과제가 N건 이상이면 **거부**하고 별도 확인 플래그를 요구 |
| 서버 | 첨부가 달린 과제가 사라지는 경우 **반드시 차단** |
| 프론트 | 강제 업로드 확인 창에 **"과제 N건이 삭제됩니다"** 를 실제 건수로 표시 |

> Phase 3(부분 저장 전환)이 끝나면 전체 배열 업로드 자체가 사라져 근본 해결되지만,
> 그때까지 **몇 주간 노출된 채로 두지 않기 위해** Phase 1에서 먼저 막는다.

### 1-6. 저장 후 전체 재다운로드 제거 (0.5일)

`App.jsx:629, 826, 1271, 1391, 1551`의 `executeServerDownload({silent:true})` 제거,
upsert 응답의 `version`만 반영.

**주의**: 다중 사용자 반영이 목적이었다면 "서버 데이터 불러오기" 버튼은 유지한다.

---

## 6. Phase 2 — V2 정규화 (4~6일)

> 전체 그림과 되돌리기 지점은 [2장](#2-마이그레이션-방식--무엇을-어떤-순서로-하는가) 참조.
> **이 Phase 전체가 "언제든 없던 일로 만들 수 있는" 구간이다.** 원본 `dashboard_data`는 읽기만 당한다.

### 2-0. 전체 필드 인벤토리 — 운영 데이터 55개 키 전수 대조 ★ 누락 방지의 핵심

> **초안 스키마에서 8개 필드군이 빠져 있었다(2026-07-28 전수 조사로 발견).** 아래가 정정된 완전 목록이다.
> 이관 스크립트는 **이 표를 기준으로 작성**하고, 표에 없는 키가 운영 데이터에서 나오면 **이관을 중단**한다.

#### `projects` 배열에 실재하는 키: **55개**

> ⚠️ **아래 "보유 과제수" 는 개발 DB(과제 328건) 실측이다.** 운영은 396건이고 분포가 다르다.
> 키 목록 자체는 운영에서도 유효했다 — 2026-07-29 이관에서 매핑 밖 키는 12종이 나왔고
> 전부 `extra_fields` 로 보존됐다. 다만 **비율은 운영과 크게 다를 수 있다.**
> 실측 예: `진행률` 개발 39/328(12%) vs **운영 373/396(94%)**.

| UI 섹션 | JSON 키 | 보유 과제수 | 타입 | V2 반영 |
|---|---|---|---|---|
| **기본정보** | `uuid` / `id` | 328 / 328 | string | PK / UNIQUE |
| | `과제명` | 328 | string | 컬럼 |
| | `사업부` | 328 | string | 컬럼 + `division_id` FK |
| | `프로세스` `과제구분` `진행상태` | 328 | string | 컬럼 |
| | `과제영역` | 129 | string | 컬럼 |
| | `과제년도` | 328 | number | 컬럼 |
| | **`시작` `종료`** | 328 | **number(월 1~12)** | ⚠️ **DATE 아님. INTEGER로 정정** |
| | `진행률` | 39 | number | 컬럼 |
| | `과제상세설명` | 203 | string | TEXT |
| | `PoC과제여부` `중점과제여부` | 328 | boolean | 컬럼 |
| | **`사업부내공개여부`** | 13 | boolean | ⚠️ **초안 누락 → 추가** |
| **담당정보** | `과제PL` | 302 | string | 컬럼(원문 보존) + `owner_user_id` |
| | `작성자` | 328 | string | 컬럼 |
| | `담당부서목록` | 328 (값 42) | array | JSONB |
| | `과제참여인력목록` | 328 (값 42) | array | JSONB (`{knoxId, 부서, 이름, 순번}`) |
| | **`관리자`** | 31 | string | ⚠️ **초안 누락 → 추가** |
| | **`담당자`** | 31 | array | ⚠️ **초안 누락 → 추가** |
| | **`담당부서`** (레거시 단수) | 31 | string | ⚠️ **초안 누락 → 추가** |
| | **`과제참여인력`** (레거시 단수) | 31 | string | ⚠️ **초안 누락 → 추가** |
| **과제 성과** | `성과목록` | 328 (값 217) | array | → `dt2_project_performance` |
| **액션아이템** | `액션아이템목록` | 328 (값 23) | array | JSONB |
| **이슈사항** | `이슈목록` | 18 (값 8) | array | JSONB |
| **첨부파일** | *(JSON 아님 — `project_attachments` 테이블)* | 1건 | — | ⚠️ **초안 누락 → `dt2_project_attachments` 추가** |
| **상세과제정보** | `상세정보_과제개요` `_추진배경` `_과제목표` `_상세내용` `_성과` `_산출물` `_향후계획` | 15 | object | JSONB 7개 |
| | `상세정보_입력완료` | 15 | boolean | 컬럼 |
| | `이미지_좌측` `_우측` `_개요그림` `_상세내용그림` `_향후계획그림` | 15 | array | → `dt2_report_images` 참조 (Phase 1-2) |
| | `이미지_그룹1_카테고리` `_그룹2_카테고리` | 15 | string | 컬럼 |
| | **`선행과제목록`** | 15 (값 1) | array | ⚠️ **초안 누락 → `dt2_project_dependencies` 추가** |
| **월간진척현황** | `월간진척현황` | 14 (값 2) | object | JSONB `{"1":"텍스트", …}` |
| **시스템/삭제** | `createdAt` `updatedAt` | 128 / 129 | string | TIMESTAMP (Phase 0-3 백필) |
| | `_deleted` `_deletedAt` `_deletedBy` `_deletedByName` | 104 / 104 / 32 / 32 | — | soft-delete 컬럼 |
| | **`_permanentlyDeleted` `_permanentlyDeletedAt` `_permanentlyDeletedBy` `_permanentlyDeletedByName`** | 104 | — | ⚠️ **초안 누락 → 추가** |
| | **`_canceledAt`** | 1 | string | ⚠️ **초안 누락 → 추가** |

#### 운영 DB `performances` 배열에 실재하는 키: **31개** (성과 432건 기준)

> 초안 `dt2_performances`는 **17개만 반영**하고 있었다. 아래가 정정된 완전 목록.

| JSON 키 | 보유 | 타입 | 비고 |
|---|---|---|---|
| `uuid` / `id` | 432 | string | PK / UNIQUE |
| `성과항목` `대분류` `소분류` `단위` `설명` | 432 | string | 컬럼 |
| `현재수준` `목표수준` | 432 | number | 컬럼 |
| `실적수준` | 432 | string | ⚠️ **성과 본체에도 존재** (조인 속성과 별개) |
| **`성과년도`** | 432 | number | ⚠️ **초안 누락** |
| `월별실적` | 432 | array | 컬럼(JSONB) |
| **`월별실적여부`** | 432 | boolean | ⚠️ **초안 누락** |
| **`displayName`** | 421 | string | ⚠️ **초안 누락** |
| **`linkedProjects`** | 421 (43 kB) | array | ⚠️ **초안 누락 · 아래 "역방향 참조" 참조** |
| **`isAchievementType`** | 214 | boolean | ⚠️ **초안 누락** |
| **`조치사항` `조치사항목록`** | 214 | string/array | ⚠️ **초안 누락** |
| `성과항목UUID` | 200 | string | 레거시 자기 참조 |
| `createdAt` `updatedAt` | 29 / 33 | string | TIMESTAMP |
| `isActive` `isFromSample` | 29 | boolean | 컬럼 |
| **`보고현황목록`** | 14 | array | ⚠️ **초안 누락** |
| `계산로직` `디지털트윈기여도` `디지털트윈기여도여부` `로직입력여부` | 15 | — | 컬럼 |
| **`성과평가`** | 1 | string | ⚠️ **초안 누락** |
| **`isEditing` `_idChanged`** | 16 / 15 | boolean | ⚠️ **UI 임시 상태가 DB에 저장됨 → 이관 제외 권장** |

> ⚠️⚠️ **`linkedProjects` — 가장 중요한 누락.**
> 과제→성과(`성과목록`)뿐 아니라 **성과→과제(`linkedProjects`) 역방향 참조가 따로 존재**한다. 421개 성과가 보유(43 kB).
> 조인 테이블 이관 시 **양방향을 모두 읽고 대조**해야 하며, **둘이 어긋나는 경우의 처리 방침이 필요**하다.
> 초안은 `성과목록` 한 방향만 보고 있었다 — 그대로 갔으면 연결이 유실됐을 수 있다.

#### 중첩 구조 원소의 키

| 구조 | 원소 키 | 초안 대비 |
|---|---|---|
| `성과목록[]` (조인) | `과제기여도`(230) `성과항목ID`(205) `성과항목UUID`(199) `단위`·`성과항목`·`현재수준`·`대분류`·`실적수준`·`목표수준`·`소분류`(각 34) `id`·`uuid`·`설명`(25) **`소분류ID`·`대분류ID`(9)** `월별실적여부`·`월별실적`(4) **`성과UUID`(4)** | ⚠️ 18개. 초안은 2개만 상정 |
| `액션아이템목록[]` | `완료여부`·`제목`(68) `id`·`월별내용`(64) **`세부항목목록`(59)** `완료일`(59) `createdAt`(58) `목표일`(57) `순번`(7) | ⚠️ **`세부항목목록` 초안 누락** |
| `이슈목록[]` | `id`·`제목`·`등록일`·`코멘트`·**`해결일`**·**`해결여부`**(각 9) `_sample`(8) | ⚠️ **`해결일`·`해결여부` 초안 누락** |
| `과제참여인력목록[]` | `knoxId`·`부서`·`이름`(59) `순번`(8) | ✓ |
| `이미지_*[]` | `caption` `fileName` `dataUrl` | ✓ (Phase 1-2에서 `imageId`로 치환) |
| `월간진척현황{}` | 월 번호 문자열 `"1"`~`"12"` → 텍스트 | ✓ |

#### ⚠️ 과제 JSON 밖에 있는 과제별 데이터 — 초안에 전혀 없던 영역

**`module_settings` 테이블에 `digital_twin_dashboard` 설정이 10건 있고, 그중 일부는 과제 uuid를 키로 삼는다.**

| settings_key | 크기 | 최종수정 | 과제 종속성 |
|---|---|---|---|
| **`reportConfirmations`** | 783 B | **2026-07-28** | ⚠️ **키가 과제 uuid** `{"10c5e5cd-…": {at, by, byName, comment, status}}` |
| `issueSecretariatComments` | 383 B | 2026-07-02 | 이슈 참조 가능성 — 확인 필요 |
| `executiveReportSettings` | 576 B | 2026-06-19 | 없음(추정) |
| `groupedViewColumnSettings` `pivotViewSettings` `logicTemplates` `unitConversions` `defaultActiveConversions` `performanceEvaluations` `reportStatuses` | 각 ~수백 B | ~2026-07-27 | 없음(추정) |

> **`reportConfirmations`가 위험하다.** 과제 uuid를 키로 하는 맵이므로:
> - 과제를 삭제해도 이 맵에 찌꺼기가 남는다 (현재도 그렇다)
> - V2 전환 시 **함께 옮기지 않으면 보고 확인 상태가 통째로 사라진다**
> - 어제(2026-07-28)도 갱신됐다 = **활발히 사용 중**
>
> **처리 방침(안)**: `dt2_projects`에 `report_confirmation JSONB` 컬럼을 두어 과제 행으로 흡수.
> 나머지 설정 9건은 과제 종속성이 없으므로 `module_settings`에 그대로 둔다(이관 불필요).

#### 기타 확인 사항

| 항목 | 현황 | 방침 |
|---|---|---|
| **`dashboard_data.data_metadata`** | `{version:"2.0", createdAt, updatedAt, projectCount:224, performanceCount:432, lastBackupDate, settings:{viewMode}}` | 파생값(집계)이라 V2에서 **재계산**. 이관 불필요. 단 `settings.viewMode`는 보존 |
| `auth.role_module_permissions` | `{user/viewer/manager: {"/meeting-management-edit": false}}` — **라우트 단위 권한이 이미 존재** | Phase 3 `can_edit_project()`와 **레이어가 다름**(모듈 접근 vs 과제 편집). 병존시키고 충돌 없게 문서화 |
| 성과 `_deleted` | 현재 **0건** (키 자체가 없음) | 스키마엔 `is_deleted` 유지 |

#### 이관 시 판단이 필요한 사항

| # | 사항 | 발견 | 처리 방침(안) |
|---|---|---|---|
| 가 | **샘플 데이터 혼입** | `이슈목록`에 `_sample: true` 항목이 **7개 과제**에 존재 | 이관하되 플래그 보존. 또는 제외 — **결정 필요** |
| 나 | **`성과목록` 원소에 성과 본체가 복제됨** | 원소 18개 키 중 관계 속성은 `과제기여도`/`실적수준`뿐이고 나머지는 성과 본체 복사본 | 조인 테이블엔 관계 속성만. **복제본과 본체가 다른 경우를 검증 스크립트에서 리포트** |
| **라** | ~~연결이 양방향으로 이중 저장됨~~ → **✅ 결정 완료** | `linkedProjects`는 **저장 데이터가 아니라 렌더 시 계산된 파생 캐시**가 새어 들어간 것 (아래 상세) | **정본 = `성과목록`. `linkedProjects`는 컬럼화하지 않음** (`extra_fields`가 보존). 데이터 손실 0 |
| **마** | **UI 임시 상태가 DB에 저장됨** | 성과에 `isEditing`(16) `_idChanged`(15), 과제에도 유사 흔적 | 이관 **제외** 권장 (런타임 상태) |
| **바** | **`reportConfirmations`가 과제 uuid를 키로 module_settings에 존재** | 783 B, 2026-07-28 갱신 = 사용 중 | `dt2_projects.report_confirmation` 컬럼으로 흡수 — **결정 필요** |
| **사** | **고아 첨부파일 6건** → **✅ 결정 완료** | 과제가 배열에서 사라졌는데 첨부 레코드만 잔존 | **`project_uuid`를 NULL로 두고 이관.** 정보 손실 0, `uploads/` 파일도 유지. 관리 화면에 "미연결"로 표시해 나중에 판단 |

---

#### ✅ 판단사항 라 상세 — `linkedProjects`가 파생 캐시인 근거

`AllPerformancesView.jsx`

| 위치 | 내용 |
|---|---|
| `:1410-1420` | `performanceProjectMap`을 **과제의 `성과목록`을 훑어서** 생성 (성과→과제 역방향 맵) |
| `:1412-1413` | ⚠️ 맵 생성 범위가 **현재연도 −2 ~ +1년 과제로 제한** → 저장된 `linkedProjects`는 **애초에 부분집합** |
| `:1493-1501` | 성과별로 맵에서 조회 |
| `:1505-1509` | `{ ...perf, displayName, linkedProjects }` — **렌더용 객체에 계산값을 붙임.** 이 객체가 저장 경로를 타면 계산값이 DB에 굳는다 |

**결정적 증거**: 개발 DB에서 `displayName` 421건과 `linkedProjects` 421건이 **개수가 정확히 일치**한다.
둘 다 위 한 줄에서 같이 붙는 필드다.

**불일치 89건이 전부 설명된다**
- 정방향 전용 53건 = 연결 생성 후 성과 화면을 안 열어 캐시 미갱신, 또는 연도 범위 밖
- 역방향 전용 36건 = 과거에 굳은 캐시인데 그 사이 연결이 끊김

→ **파생값이므로 버려도 손실이 없다.** 정본은 `성과목록` 하나다.
| 다 | **`knoxId`가 users와 매칭됨** | `과제참여인력목록[].knoxId`가 `users.email` 로컬파트와 일치 (`yjtwin.park`→`yjtwin.park@samsung.com`, `nox321`→`nox321@naver.com`). **42개 과제 보유** | Phase 0-2 소유자 매핑에서 **`과제PL` 이름 매칭(1.8%)보다 우선 시도** |

> **다는 Phase 0-2를 크게 도와준다.** 이름 문자열 매칭은 4건뿐이지만 knoxId는 이메일과 결정론적으로 이어진다.
> 매핑 우선순위: `knoxId → email` → `과제PL 이름 → users.name` → 현업 회신 → admin 임시.

---

### 2-0.1 새 필드를 추가할 때 — 컷오버 후 유지보수 비용

지금(V1)은 화면에 입력칸을 하나 추가하면 끝이다. JSON 덩어리를 통째로 저장하니 서버는 새 필드가
생긴 줄도 모른다. **컷오버 후에는 표에 칸이 없으면 저장할 곳이 없다.** 정규화의 대가다.

**편집 가능한** 필드를 하나 늘릴 때 손댈 곳:

| # | 파일 | 내용 |
|---|---|---|
| 1 | `models_v2.py` | 컬럼 추가 |
| 2 | alembic | 마이그레이션 1개 |
| 3 | `field_maps.py` | `'한글키': 'column_name'` **한 줄** — 이관·동기화·재조립이 전부 이걸 본다 |
| 4 | `permissions.py` | `LOW_RISK_FIELDS` 또는 `CORE_FIELDS` 에 컬럼명 등록 |

재조립(`assemble.py`)은 `field_maps.py` 를 뒤집어 쓰므로 **손댈 필요 없다.**

빠뜨렸을 때 드러나는 방식 — 조용히 사라지는 경우는 없다:

| 빠뜨린 곳 | 증상 |
|---|---|
| 4 (권한 분류) | 저장 시 **400** "분류표에 없는 필드". 즉시 드러난다 |
| 3 (매핑) | 값이 `extra_fields` 로 가고, 화면의 그 칸은 늘 비어 있다 |
| 1·2 (컬럼) | 마이그레이션/기동 단계에서 실패 |

**보여주기만 할 필드는 아무것도 안 해도 된다.** 매핑에 없는 키는 `extra_fields` 로 자동 보존된다
(운영 이관에서 매핑 밖 키 12종이 그렇게 살아 있다). 단 `extra_fields` 는 `IMMUTABLE_FIELDS` 라
**PATCH 로 고칠 수 없다** — "표시는 되는데 저장이 안 되는" 상태가 된다. 이게 가장 헷갈리는 함정이다.

> **2026-07-30 변경 — 3번이 원래 두 곳이었다.** `models_v2.py` 와 `scripts/dt2_import.py` 가
> 각자 사본을 들고 있었다. 스크립트가 운영서버에서 Flask 앱 없이 돌아야 해서였는데,
> 사본은 갈렸을 때 조용히 필드가 사라진다(해시·SKIP_KEYS·필드맵에서 세 번 물렸다).
>
> 매핑만 **의존성이 하나도 없는** `field_maps.py` 로 떼어내고, 스크립트는 `history_hash.py` 와
> 같은 방식으로 그 파일만 경로로 직접 읽는다. 앱은 여전히 딸려오지 않는다.
> `field_maps.py` 에 **import 를 추가하면 이 구조가 깨진다.**
> `dt2_compare_api.py` 첫 단계가 사본 재발을 감시한다(값 일치 + 로더 경로 + 재조립 동일 객체).

### 2-1. 스키마 생성 (전부 신규 테이블 · 마이그레이션 3개 파일로 9개 생성)

> 2026-07-29 보강: 시계열 테이블 2개(`dt2_performance_history`, `dt2_project_history`)를 추가했다.
> 값이 직전 기록과 다를 때만 1행씩 쌓는 append-only 테이블이다.
> **과거는 소급 생성이 불가능**하므로 Phase 2 시점에 같이 시작한다.

```sql
CREATE TABLE dt2_projects (
  uuid              VARCHAR(64)  PRIMARY KEY,
  id                VARCHAR(32)  UNIQUE NOT NULL,        -- "VD-1"

  -- 기본정보
  과제명            VARCHAR(300) NOT NULL,
  사업부            VARCHAR(50),
  division_id       INTEGER REFERENCES divisions(id),    -- ★ 권한용
  프로세스          VARCHAR(100),
  과제영역          VARCHAR(100),
  과제구분          VARCHAR(100),
  진행상태          VARCHAR(50),
  과제년도          INTEGER,
  시작              INTEGER,      -- ⚠️ 월 번호(1~12). DATE 아님
  종료              INTEGER,      -- ⚠️ 월 번호(1~12)
  진행률            INTEGER,
  과제상세설명      TEXT,
  PoC과제여부       BOOLEAN DEFAULT false,
  중점과제여부      BOOLEAN DEFAULT false,
  사업부내공개여부  BOOLEAN DEFAULT false,               -- ★ 정정 추가

  -- 담당정보
  과제PL            VARCHAR(100),                        -- 표시용 원문 보존
  작성자            VARCHAR(100),
  관리자            VARCHAR(100),                        -- ★ 정정 추가
  담당부서          VARCHAR(200),                        -- ★ 정정 추가 (레거시 단수)
  과제참여인력      VARCHAR(300),                        -- ★ 정정 추가 (레거시 단수)
  담당자            JSONB DEFAULT '[]',                  -- ★ 정정 추가
  담당부서목록      JSONB DEFAULT '[]',
  과제참여인력목록  JSONB DEFAULT '[]',                  -- {knoxId, 부서, 이름, 순번}

  -- 액션아이템 / 이슈 / 월간진척  (1차엔 행 내 JSON 유지)
  액션아이템목록    JSONB DEFAULT '[]',   -- {id, 제목, 순번, 완료여부, 목표일, 완료일, 월별내용{1..12}}
  이슈목록          JSONB DEFAULT '[]',   -- {id, 제목, 등록일, 코멘트, _sample?}
  월간진척현황      JSONB DEFAULT '{}',   -- {"1":"텍스트", "2":"텍스트", …}

  -- 상세과제정보
  상세정보_과제개요     JSONB,   -- {enabled, items[{text, children[]}]}
  상세정보_추진배경     JSONB,
  상세정보_과제목표     JSONB,
  상세정보_상세내용     JSONB,
  상세정보_성과         JSONB,
  상세정보_산출물       JSONB,
  상세정보_향후계획     JSONB,
  상세정보_입력완료     BOOLEAN DEFAULT false,
  이미지참조            JSONB DEFAULT '{}',   -- Phase 1-2 결과: {이미지_좌측:[{imageId,caption,fileName}], …}
  이미지_그룹1_카테고리 VARCHAR(50),          -- ★ 정정 추가
  이미지_그룹2_카테고리 VARCHAR(50),          -- ★ 정정 추가

  -- 권한 / 동시성 / 이력
  owner_user_id     INTEGER REFERENCES users(id),        -- ★ 권한의 근거
  row_version       INTEGER NOT NULL DEFAULT 1,          -- ★ 과제별 낙관적 락
  created_at        TIMESTAMP NOT NULL DEFAULT now(),
  updated_at        TIMESTAMP NOT NULL DEFAULT now(),
  updated_by        INTEGER REFERENCES users(id),

  -- 삭제 / 취소 상태  (★ 영구삭제 4종·취소 정정 추가)
  is_deleted                  BOOLEAN NOT NULL DEFAULT false,
  deleted_at                  TIMESTAMP,
  deleted_by                  INTEGER REFERENCES users(id),
  deleted_by_name             VARCHAR(100),
  is_permanently_deleted      BOOLEAN NOT NULL DEFAULT false,
  permanently_deleted_at      TIMESTAMP,
  permanently_deleted_by      INTEGER REFERENCES users(id),
  permanently_deleted_by_name VARCHAR(100),
  canceled_at                 TIMESTAMP,

  -- ★ 정정 추가: module_settings.reportConfirmations 흡수 (과제 uuid 키 맵이었음)
  report_confirmation         JSONB,     -- {at, by, byName, comment, status}

  -- ★★★ catch-all: 위 컬럼에 매핑되지 않은 모든 키를 통째로 보관 (2.6.3 변경 1)
  --     운영에만 있는 미지의 키로 인한 유실을 구조적으로 차단한다
  extra_fields                JSONB NOT NULL DEFAULT '{}'
);
CREATE INDEX ON dt2_projects (owner_user_id);
CREATE INDEX ON dt2_projects (division_id);
CREATE INDEX ON dt2_projects (is_deleted, is_permanently_deleted, 진행상태);

-- ★ 정정 추가: 선행과제 (과제 간 참조)
CREATE TABLE dt2_project_dependencies (
  project_uuid     VARCHAR(64) REFERENCES dt2_projects(uuid) ON DELETE CASCADE,
  depends_on_uuid  VARCHAR(64) REFERENCES dt2_projects(uuid) ON DELETE CASCADE,
  PRIMARY KEY (project_uuid, depends_on_uuid)
);

-- ★ 정정 추가: 첨부파일 (기존 project_attachments의 project_id를 FK로 승격)
CREATE TABLE dt2_project_attachments (
  id                SERIAL PRIMARY KEY,
  project_uuid      VARCHAR(64) REFERENCES dt2_projects(uuid) ON DELETE CASCADE,
  original_filename VARCHAR(255) NOT NULL,
  stored_filename   VARCHAR(255) NOT NULL,   -- 실체는 uploads/ 폴더 (파일 이동 없음)
  file_size         INTEGER DEFAULT 0,
  mime_type         VARCHAR(100),
  uploaded_by       INTEGER REFERENCES users(id),
  uploaded_by_name  VARCHAR(100),
  created_at        TIMESTAMP DEFAULT now()
);
CREATE INDEX ON dt2_project_attachments (project_uuid);

CREATE TABLE dt2_performances (              -- 실재 키 31개 전부 반영 (2-0 인벤토리 기준)
  uuid                  VARCHAR(64) PRIMARY KEY,
  id                    VARCHAR(64) UNIQUE NOT NULL,
  성과항목              VARCHAR(300),
  displayName           VARCHAR(300),                    -- ★ 정정 추가 (421건)
  대분류                VARCHAR(200),
  소분류                VARCHAR(200),
  단위                  VARCHAR(50),
  성과년도              INTEGER,                         -- ★ 정정 추가 (432건)
  현재수준              NUMERIC,
  목표수준              NUMERIC,
  실적수준              VARCHAR(100),                    -- ★ 정정 추가 (본체에도 존재)
  월별실적              JSONB DEFAULT '[]',
  월별실적여부          BOOLEAN DEFAULT false,           -- ★ 정정 추가 (432건)
  isAchievementType     BOOLEAN DEFAULT false,           -- ★ 정정 추가 (214건)
  조치사항              TEXT,                            -- ★ 정정 추가 (214건)
  조치사항목록          JSONB DEFAULT '[]',              -- ★ 정정 추가 (214건)
  보고현황목록          JSONB DEFAULT '[]',              -- ★ 정정 추가 (14건)
  성과평가              VARCHAR(200),                    -- ★ 정정 추가 (1건)
  계산로직              JSONB,
  로직입력여부          BOOLEAN DEFAULT false,
  디지털트윈기여도      VARCHAR(50),
  디지털트윈기여도여부  BOOLEAN DEFAULT false,
  설명                  TEXT,
  성과항목UUID          VARCHAR(64),                     -- 레거시 자기 참조 보존
  is_active             BOOLEAN DEFAULT true,
  is_from_sample        BOOLEAN DEFAULT false,

  -- ★ 운영 스캔(2026-07-28)에서 확인 — 개발 DB엔 삭제 성과가 0건이라 안 보였던 필드
  is_deleted            BOOLEAN NOT NULL DEFAULT false,
  deleted_at            TIMESTAMP,
  deleted_by            INTEGER REFERENCES users(id),
  deleted_by_name       VARCHAR(100),

  -- `실적_1월`~`실적_12월` 은 컬럼을 만들지 않는다 (정본 = 위 `월별실적` 배열).
  -- 입력/CSV 교환 포맷일 뿐이고 운영 값 0건. 잔재 키는 extra_fields 가 흡수한다.

  row_version           INTEGER NOT NULL DEFAULT 1,
  created_at            TIMESTAMP NOT NULL DEFAULT now(),
  updated_at            TIMESTAMP NOT NULL DEFAULT now(),
  extra_fields          JSONB NOT NULL DEFAULT '{}'      -- ★★★ catch-all (2.6.3 변경 1)
  -- 이관 제외: isEditing, _idChanged (UI 런타임 상태)
  -- linkedProjects 는 컬럼이 아니라 dt2_project_performance 로 흡수 (양방향 대조 후)
);

CREATE TABLE dt2_project_performance (          -- 과제↔성과 + 관계속성
  project_uuid      VARCHAR(64) REFERENCES dt2_projects(uuid)     ON DELETE CASCADE,
  performance_uuid  VARCHAR(64) REFERENCES dt2_performances(uuid) ON DELETE CASCADE,
  과제기여도        NUMERIC,
  실적수준          NUMERIC,
  PRIMARY KEY (project_uuid, performance_uuid)
);

CREATE TABLE dt2_project_editors (              -- 예비 (결정 2는 소유자+부서 규칙이라 당장은 미사용)
  project_uuid VARCHAR(64) REFERENCES dt2_projects(uuid) ON DELETE CASCADE,
  user_id      INTEGER REFERENCES users(id),
  granted_by   INTEGER REFERENCES users(id),
  granted_at   TIMESTAMP DEFAULT now(),
  PRIMARY KEY (project_uuid, user_id)
);

CREATE TABLE dt2_change_proposals (             -- ★ 결정 1: 핵심 필드 승인 대기열
  id            SERIAL PRIMARY KEY,
  project_uuid  VARCHAR(64) REFERENCES dt2_projects(uuid) ON DELETE CASCADE,
  patch          JSONB  NOT NULL,              -- {필드: 새값}
  before_values  JSONB  NOT NULL,              -- 제안 시점의 원래 값 (충돌 감지용)
  base_version  INTEGER NOT NULL,               -- 제안 시점 row_version
  reason        TEXT,                           -- AI가 제시한 근거
  proposed_by   INTEGER REFERENCES users(id),   -- 서비스 계정
  on_behalf_of  INTEGER REFERENCES users(id),   -- 시킨 사람
  status        VARCHAR(20) DEFAULT 'pending',  -- pending/approved/rejected/stale
  reviewed_by   INTEGER REFERENCES users(id),
  reviewed_at   TIMESTAMP,
  review_note   TEXT,
  created_at    TIMESTAMP DEFAULT now()
);
CREATE INDEX ON dt2_change_proposals (status, project_uuid);
```

### 2-2. 이관(복사) 스크립트 `scripts/dt2_import.py` (idempotent)

> 원본은 **읽기만** 한다. 이 스크립트는 `dashboard_data`에 절대 쓰지 않는다.

| 규칙 | 내용 |
|---|---|
| 키 | `uuid` 통일 (스캔: 328/432건 전부 고유) |
| **필드 커버리지** | **2-0 인벤토리의 55개 키를 전부 처리.** 표에 없는 키가 나오면 **즉시 중단하고 리포트** |
| 성과 참조 해석 | `성과항목UUID` → `성과항목ID` → `id` 순 |
| 성과 관계 속성 | 조인 테이블엔 `과제기여도`/`실적수준`만. 나머지(단위·대분류·목표수준·설명)는 성과 본체에서 조인 |
| 성과 복제본 불일치 | `성과목록` 원소의 복제 필드와 성과 본체가 다르면 **차이를 리포트**(자동 판단 금지) |
| 고아 참조 | 스킵 + 로그 (스캔: 2건) |
| 삭제분 104건 | `is_deleted` / `is_permanently_deleted`로 **함께 이관** (권한·이력 보존) |
| **선행과제** | `선행과제목록[].uuid` → `dt2_project_dependencies`. 존재하지 않는 uuid는 스킵+로그 |
| **첨부파일** | `project_attachments` → `dt2_project_attachments` (`project_id` → `project_uuid` FK 승격). **파일 실체는 이동하지 않는다** (`uploads/` 그대로) |
| **샘플 이슈** | `이슈목록`의 `_sample: true` 7개 과제분 — 방침 결정 후 적용(기본: 플래그 유지한 채 이관) |
| 소유자 | Phase 0-2 매핑표에서 주입 (knoxId → email 우선) |
| 재실행 | `INSERT … ON CONFLICT (uuid) DO UPDATE` |
| 제외 | 스냅샷 352건, activity_logs (V2는 새로 시작) |

### 2-3. 검증 스크립트 `scripts/dt2_verify.py` ★ API보다 먼저 만든다

```
V1 GET /data 응답  vs  V2 어댑터가 조립한 응답  →  정규화 후 deep diff
```

| 검사 항목 | 통과 기준 |
|---|---|
| 과제 수 | 활성 224 / 삭제 104 일치 |
| 성과 수 | 432 일치 |
| 과제-성과 참조 | 230건 중 유효 228건 일치 (고아 2건 제외) |
| **키 커버리지** | **V1의 55개 키가 전부 V2 어딘가에 존재.** 미매핑 키 **0** |
| 필드 단위 diff | 불일치 **0** |
| 첨부파일 | 건수 일치 + `project_uuid`가 실제 과제와 매칭 |
| 선행과제 | `선행과제목록` 참조 수 일치 |
| 이미지 | 참조 개수 일치 + 실제 파일 존재 확인 |
| 소유자 | `owner_user_id` NULL 0건 |

**이 스크립트가 통과하기 전에는 2-4로 가지 않는다.**

### 2-4. V2 읽기 API (어댑터)

- `GET /api/dt-v2/data` — 행들을 조립해 **V1과 동일한 형태**로 반환
  → 프론트 입장에선 응답 형태가 그대로라 바뀐 것을 모른다 (2.3 Q4 참조)
- 이 시점까지 **쓰기 API는 만들지 않는다.** 읽기 동일성만 증명한다.

### Phase 2 롤백

| 어디까지 갔든 | 되돌리는 방법 | 손실 |
|---|---|---|
| 2-1 이후 | `flask db downgrade` → `DROP TABLE dt2_*` | 없음 |
| 2-2 이후 | `TRUNCATE dt2_*` 후 재복사, 또는 전체 DROP | 없음 |
| 2-3 검증 실패 | 그냥 멈춘다 | 없음 |

V1은 한 번도 손대지 않았으므로 **어느 지점에서 중단해도 서비스 영향 0.**

---

## 7. Phase 3 — 과제별 쓰기 API + 권한 관문 (3~4일)

### 7-1. 권한 함수 (단일 지점)

```python
def can_edit_project(actor: User, project: Dt2Project) -> bool:
    if actor.role == 'admin':                    return True
    if project.owner_user_id == actor.id:        return True
    if actor.role == 'manager':
        # Phase 0-4에서 departments.division_id를 채웠기에 성립
        return actor_division_id(actor) == project.division_id
    return False
```

**모든 쓰기 경로(사람 UI·MCP·스크립트)가 이 함수를 반드시 경유한다.**

### 7-2. 필드 분류 — 결정 1의 구체화

| 등급 | 필드 | AI 동작 |
|---|---|---|
| **저위험** (즉시 반영) | 진행률, 월간진척현황, 액션아이템목록, 이슈목록, 과제상세설명, 상세정보_* | `PATCH` 즉시 커밋 |
| **핵심** (승인 필요) | 과제명, 시작/종료, **진행상태**, 사업부/division, 과제PL/owner, 과제구분, 과제영역, 성과연결·과제기여도, 삭제/복구 | `dt2_change_proposals`에 적재 |

> 사람이 UI에서 편집할 때는 등급 구분 없이 즉시 반영된다. **분류는 AI 쓰기에만 적용된다.**

### 7-3. 엔드포인트

```
GET    /api/dt-v2/projects?division=&status=&owner=&q=   요약 필드만, 권한 필터
GET    /api/dt-v2/projects/<uuid>                        1건 전체 (이미지는 참조)
PATCH  /api/dt-v2/projects/<uuid>                        본문: {patch, expected_version}
         → 200 성공 / 403 권한없음 / 409 버전충돌 / 202 제안접수(핵심 필드)
POST   /api/dt-v2/projects                               신규 생성
GET    /api/dt-v2/proposals?status=pending               승인 대기 목록
POST   /api/dt-v2/proposals/<id>/approve | /reject       승인/반려
```

### 7-4. 동시성 (앞선 논의 반영)

```python
with db.session.begin():
    p = Dt2Project.query.with_for_update().get(uuid)     # (a) 트랜잭션 락, 밀리초
    if p.row_version != expected_version:                # (b) 낙관적 락
        changed = diff_fields(p, expected_version)
        if not (changed & set(patch.keys())):
            pass          # 겹치지 않는 필드 → 자동 병합 허용
        else:
            abort(409)    # 같은 필드 → 거부
    apply(p, patch)
    p.row_version += 1
    log_change(p, before, after, actor)                  # 서버가 직접 diff 기록
```

- **비관적 편집 락(수정 중 점유)은 도입하지 않는다.** (근거: MCP 문서 3.5장)
- "편집 중" presence 표시는 Phase 4 이후 선택 과제.

### 7-5. 프론트 전환

- 전체 배열 업로드 → **변경된 과제만 `PATCH`**
- `metadata` 무조건 덮어쓰기 버그(`routes.py:1115`) 해소
- "로컬만 저장" 버튼은 **유지**한다 (낙관적 락이 오프라인과 궁합이 좋음)

---

## 7.5 컷오버까지 남은 일 — 전수 목록 (2026-07-29 재작성)

> 화면이 부르는 엔드포인트를 전부 훑어 다시 셌다. **`dashboard_data` 에 쓰는 경로가 7개**로,
> 처음 계획이 잡았던 것보다 넓다. 이 표가 "완전 전환" 의 실제 범위다.

### 7.5-1. 현재까지 완료된 것

| | 상태 |
|---|---|
| dt2 테이블 9개 · 이관 · 동일성 검증 | ✅ 운영 완료 (2026-07-29) |
| 성과·과제 진척 이력 테이블 | ✅ 운영 완료. seed 596 / 396 |
| V2 읽기 API + 응답 동일성 대조 | ✅ 운영 데이터로 전항 0 증명 |
| 읽기 토글 (`localStorage.dtV2Read`) | ✅ 운영 검증 완료. **기본값은 V1** |
| 권한 함수 · 과제 쓰기 API · AI 제안 큐 | ✅ 개발 완료(시험 93건). **미배포** |
| 쓰기 스위치 환경변수화 + v2_sync 상호배타 | ✅ 개발 완료 (2026-07-30). `dt3_test_writeswitch.py` 29항목. **미배포** |

### 7.5-2. 백엔드 쓰기 API — 남은 것

| V1 엔드포인트 | V2 대응 | 비고 |
|---|---|---|
| `POST /data/upload` | 프론트가 안 쓰게 됨 | 스냅샷 복원도 이 경로를 탄다 |
| `POST /data/upsert` | 동상 | |
| `POST /project/<uuid>/delete` | ❌ | 소프트 삭제 |
| `POST /project/<uuid>/restore` | ❌ | |
| `DELETE /project/<uuid>/permanent-delete` | ❌ | |
| `DELETE /performance/<id>/delete` | ❌ | |
| `POST /snapshots` | ❌ **설계 필요** | 아래 7.5-6 참조 |
| 과제 수정 / 생성 | ✅ `PATCH`/`POST /projects` | |
| **성과 생성·수정** | ❌ | |
| **과제–성과 연결 변경** | ❌ | |
| metadata 저장 | ❌ | |

**영향 없는 것** — `settings` · `kpi/*` · `attachments/*` · `report-images/*` · `activity-logs`.
별도 테이블이고 과제 uuid 로 붙어 있어 정본이 바뀌어도 그대로 동작한다.

### 7.5-3. V1→V2 즉시 동기화 ★ 읽기 전환의 전제

`/data/upload` · `/data/upsert` 가 커밋된 **직후** dt2 를 갱신한다.

> **왜 필수인가.** 화면은 페이지 진입 때마다 서버 데이터를 받아 로컬을 덮어쓴다
> (`DigitalTwinDashboardApp.jsx:197`). V2 가 배치로만 갱신되는 상태에서 읽기를 V2 로
> 넘기면, 사용자가 저장한 직후 새로고침할 때 **그 수정이 사라져 보인다.**
> "읽기만 먼저 전환하면 동결이 불필요하다" 는 초안 판단은 이 점을 놓친 것이었다.

**dt2 동기화가 실패해도 사용자의 저장은 성공시킨다.** V1 이 정본이므로 dt2 는 배치로 복구된다.
저장이 실패하는 쪽이 훨씬 나쁘다.

부수 효과가 크다.

| | 지금 | 동기화 후 |
|---|---|---|
| 읽기 전환 | 불가 (스테일) | 안전 |
| 이력 해상도 | 이관 주기 (주 1회) | **저장할 때마다** |
| 작업 스케줄러 | 필요 | 불필요 |
| 컷오버 위험 | V2 가 검증만 됨 | V2 가 맞다는 게 매일 증명됨 |

### 7.5-4. 프론트엔드 전환

| | 상태 |
|---|---|
| 읽기 토글 | ✅ 완료 |
| 읽기 기본값 V2 (`DEFAULT_V2_READ`) | 7.5-3 완료 후 |
| **저장을 과제별 `PATCH` 로** | ❌ **가장 큰 작업** |
| 409 충돌 처리 UI | ❌ |
| 전역 `version` → 행별 `row_version` | ❌ 충돌 감지 방식이 바뀐다 |
| "로컬만 저장" 유지 | 그대로 |

### 7.5-4.1 서버가 한글 키를 받는다 — 1단계-a (2026-07-30 완료, 미배포)

화면은 `진행률`·`과제명` 같은 한글 키로 데이터를 들고 있고, dt2 는 영문 컬럼(`progress`·`title`)이다.
저장을 V2 로 옮기려면 누군가 이름을 바꿔야 한다. **서버가 바꾸기로 했다.**

| | 화면이 번역 | **서버가 번역 (채택)** |
|---|---|---|
| 번역표 사본 | 서버 1개 + 화면이 받아 씀 | 서버 1개 |
| 화면 쪽 새 코드 | 번역 로직 필요 | 없음 |
| 새 실패 지점 | **표 받아오기** 실패하면 저장 불가 | 없음 |
| API 어휘 | 영어 하나 | **한글·영어 둘** ← 대가 |

**채택 이유는 검증 기반이다.** 백엔드는 헤드리스 시험이 400건 넘게 돌지만 프론트는 테스트 러너가
없다. 틀리면 데이터가 안 들어가는 종류의 로직은 검증이 쉬운 쪽에 둔다.

그리고 이 선택은 **되돌리기가 쉽다.** 서버는 영어 키를 계속 받으므로, 나중에 화면이 영어로 바꿔
보내기 시작하면 그게 곧 "화면이 번역" 안이다. 서버를 되돌릴 필요가 없다.

구현 — `field_maps.translate_keys()` 를 네 곳 앞단에 끼웠다
(`PATCH`·`POST /projects`, `PATCH`·`POST /performances`).

- 맵에 있는 키 → 컬럼명으로 변환
- 맵에 없는 키 → **그대로 둔다.** 영어 키가 여기 해당하고, 모르는 키는 뒤의 분류표가 400 으로 거부
- 서로 다른 키가 같은 칸을 가리키면(`과제명` + `title` 동시 전송) **400** — 조용한 덮어쓰기 방지
- 오류 메시지는 **보낸 이름**으로 되돌려 표시한다(한글로 보냈으면 한글로)

> ⚠️ 대가는 API 가 두 어휘를 받게 된 것이다. **AI·MCP 는 영어만 쓴다**(Phase 4).
> 컷오버 후 정리 항목: 한글 키 경로 폐지 검토.

검증: `python scripts\dt3_test_koreankeys.py` (28항목 — 컬럼 반영·영어 회귀·혼용·충돌 400·
모르는 키·불변 필드·생성·성과). 전체 회귀 **406 [OK] / 0 FAIL**.

### 7.5-4.2 첫 V2 분기 — `saveProjectEdit` (1단계-b 착수, 2026-07-30)

토글 두 개가 모두 켜졌을 때만 V2 로 간다. **기본값은 둘 다 꺼짐** — 사용자에게 변화 없음.

```
localStorage.dtV2Write === '1'   (화면)
DT2_WRITE_ENABLED=true           (서버 .env — 꺼져 있으면 V2 쓰기는 503)
```

**주소로도 켤 수 있다** — 개발자도구를 열지 않아도 된다. 링크로 공유할 수 있는 게 요점이다
("이 링크로 들어가서 화면이 예전과 같은지 봐주세요" 를 콘솔 설명 없이 부탁할 수 있다).

```
...?dtV2Read=1&dtV2Write=1     켜기 (한 번 열면 localStorage 에 남는다)
...?dtV2Write=0                끄기
```

반영 후 주소에서 그 파라미터만 지운다 — 다른 파라미터는 남기고, 뒤로가기에도 안 남는다.

**서버 쪽이 아직 안 켜져 있으면(503) V1 으로 물러선다.** 503 은 `before_request` 에서 막힌
것이라 **아무것도 쓰이지 않았다** — 그래서 다시 V1 으로 보내도 안전하다.
반대로 403·409·400 은 **물러서지 않고 그대로 실패시킨다.** 조용히 V1 으로 우회하면
권한 문제나 충돌을 못 보고 넘어간다. 덕분에 화면 토글만 먼저 켜도 저장이 깨지지 않는다.

화면은 "무엇이 바뀌었나" 를 `extractChanges(before, after)` 로 만든다. **필드 목록을 주지 않으면
전체 키를 비교한다**(`activityLogger.js:524`) — 그래서 "보낼 수 있는 필드 목록" 을 화면에 두지
않는다. 서버가 못 받는 키는 `ignore_unknown` 으로 걸러지고 `ignored` 로 되돌아온다.

> 처음에 "활동로그용 `extractChanges` 가 이미 diff 를 만든다" 고 봤는데 **틀렸다.**
> 호출부는 19개 필드 목록을 넘기고 있어서 `진행률` 이 빠진다. 목록 없이 부르는 게 맞다.

**연결이 바뀐 저장은 V2 로 보내지 않고 V1 로 물러선다.** `성과목록` 은 별도 API
(`PUT /projects/<uuid>/performances`) 소관인데 아직 안 붙였다. 그냥 보내면 연결 변경만 조용히
사라진다. 연결 API 를 붙이면 이 분기를 없앤다. `before` 를 못 받은 경우도 V1 로 물러선다.

과제 식별자는 `before?.uuid || project?.uuid || project?.id` 로 잡는다 —
`formData` 에는 uuid 가 없다(화이트리스트로 생성).

검증
- 서버 `dt3_test_koreankeys.py` 36항목 (한글 키 반영 · 영어 회귀 · 충돌 400 · `ignore_unknown`)
- 화면 어댑터 스모크 86항목 (토글 on/off · 바뀐 값만 전송 · 관계 키 제외 · 물러섬 · 503)
- 전체 회귀 **413 [OK] / 0 FAIL**, `npm run build` 통과

**개발서버 실측으로 end-to-end 확인 (2026-07-30).**

```
07-30 13:47:39  MX-33  v2  title  'test' → 'test11'    [ui]
07-30 13:47:33  MX-35  v2  title  'test' → 'test123'   [ui]
```

`dt2_project_changes` 에 사람 변경으로 기록되고 `row_version` 이 1→2 로 올랐다.
그동안 `dashboard_data.version` 은 660 에서 **그대로** — V2 쓰기가 V1 을 건드리지 않는다.
`v2_sync.sync_status()` 도 한 번도 안 돌았다(상호배타가 실제로 동작).

#### 실측에서 잡힌 것 — 관계 키가 매 저장마다 걸러지고 있었다

처음 켰을 때 **매번** `성과목록` 때문에 V1 으로 물러섰고, 그걸 고치자 이번엔
`선행과제목록` + 이미지 슬롯 5개가 `ignored` 로 나왔다. 둘 다 **가짜 변경**이었다.

| 원인 | 고친 방법 |
|---|---|
| 연결 원소에 성과 본체가 복제돼 있고 참조 키 이름이 4종 섞임 | 관계 속성(참조·기여도·실적)만 남겨 비교 — V2 연결 API 와 같은 기준 |
| 편집창이 없는 값에 기본값을 채움 (`선행과제목록: project.X \|\| []`) | `undefined`·`null`·`[]`·`''` 를 모두 "비어 있음" 으로 동일 취급 |

> 통째 `JSON.stringify` 비교는 **화면을 거친 데이터에는 못 쓴다.** 편집창이 모양을
> 정규화하고 기본값을 채우기 때문에, 아무것도 안 고쳐도 "바뀐 것" 이 된다.

관계 키가 **진짜** 바뀌면 여전히 V1 으로 물러선다(해당 API 가 아직 없으므로).
그리고 서버가 건너뛴 키는 `ignored` 로 돌아와 콘솔에 **경고**로 찍힌다 —
프론트의 관계 키 목록이 서버와 갈려도 조용히 틀어지지 않는 장치다.

남은 9개 함수는 같은 방식으로 붙인다. 여러 번 호출하는 함수(일괄추가·기여도)에서
**부분 실패 정책**을 먼저 정해야 한다.

### 7.5-4.3 `saveNewProject` — 생성이 드러낸 분류표 구멍 (2026-07-30)

수정은 **바뀐 필드만** 보내서 잘 안 드러났는데, 생성은 **전체를 보낸다.** 그래서 붙이자마자
분류표(`permissions.py`)에 없는 컬럼이 문제가 됐다 — 그 필드들은 `ignore_unknown` 아래에서
**통째로 빠진 채 과제가 만들어진다.**

맵 48개 컬럼 중 **17개가 미분류**였다. 채운 결과:

| 분류 | 채운 컬럼 | 근거 |
|---|---|---|
| **핵심** | `members_json` `owners_json` | ★ **편집 권한을 부여한다** (`can_edit_project` → `is_project_member`). 저위험일 수 없다 |
| 핵심 | `member_names` `depts_json` `dept_name` | 위의 표시용·레거시 짝이라 같이 둔다 |
| 핵심 | `process` | 사업부·과제구분과 같은 분류 축 |
| 핵심 | `manager_name` | `pl_name` 과 대칭 |
| 핵심 | `is_division_public` | 가시성을 바꾼다 |
| 핵심 | `canceled_at` | `status` 와 한 쌍 (화면이 취소 전환 시각을 만든다) |
| 저위험 | `author_name` `image_group1_category` `image_group2_category` | 표시용 이름표 |
| **불변** | `deleted_by_raw` `deleted_by_name` `permanently_deleted_*` | 누가 지웠는지는 **삭제 API 가 기록한다.** 본문으로 받으면 위조된다 |

성과 쪽 2개(`deleted_by_raw`·`deleted_by_name`)도 불변으로 채웠다.
이제 **맵의 모든 컬럼이 분류돼 있다** — `dt3_test_koreankeys.py` 가 매번 확인한다.

#### 생성이 uuid 를 받는다

`POST /projects` 는 화면이 만든 `uuid` 를 받아준다(생성 시에만, 중복이면 409).
화면은 로컬에서 uuid 를 먼저 만들고 **그 값으로 첨부파일 업로드와 성과 연결을 건다.**
서버가 다른 uuid 를 매기면 그 참조들이 전부 어긋난다. V1 도 화면이 만든 uuid 를 그대로
저장했으므로 계약을 유지하는 쪽이 맞다.

성과 연결·선행과제·이미지가 **딸린 채로** 생성되는 경우는 V1 로 보낸다 —
`POST /projects` 는 본체만 만들고 관계는 각자 API 소관이라, 그냥 보내면 그것들만 빠진다.

검증: `dt3_test_koreankeys.py` 53항목 · 어댑터 스모크 97항목 · 전체 회귀 **430 [OK] / 0 FAIL**.

### 7.5-4.4 사업부 재해석 · 삭제/복구 전환 (2026-07-30)

#### A. `division_id` 가 `사업부` 를 따라간다

과제의 사업부는 FK 가 아니라 **자유 텍스트**인데, 권한 판단(manager 분기)은 `division_id`
를 본다. 텍스트만 바꾸고 id 를 두면 **사업부를 옮긴 과제가 옛 사업부 기준으로 남는다** —
옛 manager 는 계속 고칠 수 있고 새 manager 는 못 고친다.

해석기(`P.resolve_division_id`)를 만들고 **`_apply` 한 곳**에 걸었다. 모든 쓰기 경로가
그 함수를 지나므로 화면 PATCH 도 AI 제안 승인도 같이 걸린다 — 호출부마다 넣으면
언젠가 하나를 빠뜨린다. 생성 경로도 따로 처리했다.

규칙은 이관 스크립트와 같다: **활성 사업부만**, 이름 정확 일치. 못 찾으면 `NULL` —
값을 지어내지 않는다(틀린 사업부에 붙는 것보다 '미상' 이 낫다).

#### B. 삭제 · 복구 4종

이미 1건 단위라 1:1 로 붙였다. 전역 version 추정을 **호출부에서 어댑터로 옮겼다** —
V2 경로에는 전역 version 이 없어서 거기서 올리면 나중에 V1 로 물러설 때 어긋난다.

> ⚠️ **영구삭제는 V2 에서 admin·dt_office 만 가능하다.** V1 은 로그인만 하면 남의 과제도
> 영구삭제할 수 있었다(그 경로에 권한 검사가 없었다). 되돌릴 수 없는 조작이라 Phase 3 가
> 의도적으로 좁힌 것이다. 일반 사용자가 403 을 받는 건 버그가 아니다.
> **컷오버 공지에 넣어야 할 동작 변화다.**

403 은 V1 으로 우회하지 않는다 — 우회하면 좁혀 놓은 권한이 무의미해진다. 503 만 물러선다.

#### `dt2_verify` · `dt2_compare_api` 는 컷오버 **전** 도구다

V2 쓰기를 켜면 V1 이 멈추고 dt2 만 앞서가므로 **불일치가 정상**이다. 실제로 V2 로 만든
과제 1건 때문에 `dt2_verify` 가 [FAIL] 을 냈다. 모르고 보면 이관이 깨진 줄 안다.
그래서 `dt2_verify.py` 가 `DT2_WRITE_ENABLED` 를 읽어 **원인을 먼저 알린다**(판정은 그대로).

검증: 어댑터 스모크 **114항목** · 백엔드 dt3 **427 [OK] / 0 FAIL** · 빌드 통과.

### 7.5-4.5 성과 연결 API — 저장 하나가 호출 둘이 되는 첫 사례 (2026-07-30)

`PUT /projects/<uuid>/performances` 를 붙였다. 편집창에서 성과 연결을 바꾼 저장이
더 이상 V1 으로 물러서지 않는다.

#### 순서: **연결 먼저, 필드 나중**

저장 한 번이 호출 두 번이 되면서 "절반만 저장된" 상태가 처음 가능해진다. 순서로 막는다.

| 순서 | 실패했을 때 남는 상태 |
|---|---|
| **연결 → 필드** (채택) | 연결이 실패하면 **아무것도 안 바뀐 채로** 끝난다 |
| 필드 → 연결 | "필드는 저장됐는데 연결은 실패한" 어중간한 상태가 남는다 |

연결이 실패하면 필드 PATCH 로 **넘어가지 않고** 에러를 낸다. V1 으로 우회하지도 않는다.

#### 생성은 순서를 뒤집을 수 없다

과제가 있어야 연결을 걸 수 있으므로 `POST` → `PUT` 이다. 그래서 여기서만은
"과제는 만들어졌는데 연결만 빠진" 상태가 가능하다. 조용히 넘어가지 않고
**무엇을 해야 하는지까지 담아** 올려보낸다:

```
과제는 만들어졌지만 성과 연결에 실패했습니다 (…). 편집창에서 성과를 다시 연결해 주세요.
```

#### 참조를 uuid 로 푼다

화면 원소의 참조 키가 여러 종류라(`성과항목UUID`/`성과UUID`/`uuid`/`성과항목ID`/`id`)
어떤 것은 uuid 가 아니라 코드('4')다. 서버는 **uuid 만** 받으므로 어댑터가
`performances` 목록으로 푼다. 못 찾으면 원래 값을 그대로 보낸다 — 여기서 지어내는 것보다
서버가 400 으로 알려주는 편이 낫다.

배열 **순서를 그대로 보낸다**(순서가 곧 표시 순서). 기여도가 비면 `null`.

검증: 어댑터 스모크 **124항목** · 백엔드 dt3 **427 [OK] / 0 FAIL** · 빌드 통과.

#### 여러 과제를 한 번에 — 부분 실패 정책 (2026-07-30 결정)

기여도 일괄 수정과 성과 화면의 연결 편집은 **여러 과제**를 바꾼다. 과제마다 `PUT` 을 보내야
하므로 "5개 중 3개만 성공" 이 가능하다. 순서로는 못 막는다(호출이 2번이 아니라 N번).

채택: **순차 실행 · 첫 실패에서 중단 · 되돌리지 않음 · 어디까지 됐는지 알림**

| | 이유 |
|---|---|
| 순차 | 동시에 보내면 같은 성과를 쓰는 과제끼리 서버 행 락에서 경합한다 |
| 첫 실패에서 중단 | 계속 밀어붙이면 "어디까지 됐는지" 가 더 흐려진다 |
| 되돌리지 않음 | 되돌리는 중에 또 실패하면 더 나쁜 상태가 된다 |
| 알림 | `3개 과제 중 1개까지 반영됐고 '과제2' 에서 멈췄습니다 (…). 나머지는 저장되지 않았습니다.` |

일부라도 반영된 뒤에는 **503 이어도 V1 으로 물러서지 않는다** — 절반은 V2, 절반은 V1 이 최악이다.

#### 활동 로그가 V2 경로에서 빠지고 있었다

V1 은 저장 페이로드에 활동 로그를 실어 보냈다. V2 에는 그 자리가 없어서, 그대로 뒀다면
**활동로그 화면에서 V2 저장분이 통째로 빠졌을 것**이다. (`dt2_project_changes` 는 필드 단위
이력이라 성격이 다르다 — 둘 다 필요하다)

그래서 V2 경로는 `POST /activity-logs` 로 따로 보낸다. **실패해도 저장을 실패시키지 않는다** —
데이터는 이미 저장됐고, 로그 한 줄 때문에 사용자에게 실패를 알리는 건 과하다.

검증: 어댑터 스모크 **135항목** · 빌드 통과.

### 7.5-4.6 성과 쓰기 · 그리고 새 검증 축 (2026-07-30)

성과 생성·수정을 `POST`/`PATCH /performances` 로 붙였다. 화면이 만든 uuid 를 받아준다
(과제의 성과목록이 이미 그 값을 참조하고 있다). 성과 수정 응답의 `affectedProjects` 를
로그에 함께 찍는다 — **성과 수정은 남의 과제 숫자까지 바꾼다.**

> ⚠️ **성과 ID(`code`) 변경은 V2 에서 불가능하다.** 식별자가 바뀌면 그 성과를 가리키는
> 과제들의 참조가 한꺼번에 깨진다. 지금은 그런 수정만 V1 으로 물러선다.
> **컷오버 후에는 아예 못 하게 된다 — 영구삭제 권한 축소와 함께 공지 대상.**

#### ★ 새 검증 축 — `dt3_check_frontend_shape.py`

2026-07-30 에 겪은 사고가 기존 검증 두 개를 **모두 빠져나갔다.**

V2 연결 API 가 참조 키를 `uuid` 로 썼다. 테이블도 맞고(`dt2_verify` 통과), 재조립도 됐다.
그런데 화면은 참조를 `성과항목UUID → id → 성과항목ID` 순으로만 읽는다. 그래서 **화면이
그 연결을 고아로 보고 지웠다.** 새로고침마다 하나씩 늘었다.

| 검증 | 보는 것 | 이 사고를 잡나 |
|---|---|---|
| `dt2_verify` | V1 JSON ↔ dt2 테이블 | ❌ 화면을 모른다 |
| `dt2_compare_api` | V1 응답 ↔ V2 응답 | ❌ 컷오버 후엔 V1 이 멈춰 쓸 수 없다 |
| **`dt3_check_frontend_shape`** | **V2 응답 ↔ 화면의 조회 규칙** | ✅ |

화면의 조회 규칙(`projectPerformanceLink.js`)을 서버에서 그대로 재현해 확인한다.
브라우저 없이 돌고, **저장 경로를 V2 로 옮길 때마다 돌리면 된다.**

이관이 일부러 보존한 V1 고아는 판정에서 뺀다(정보로만 표시) — 원본에 이미 깨져 있던 것이라
FAIL 로 잡으면 진짜 회귀가 잡음에 묻힌다.

#### 2026-07-30 회귀 확인 결과

10개 저장 경로 중 **8개가 V2**, 전부 개발서버에서 실측했다.

| 확인 | 결과 |
|---|---|
| 백엔드 시험 15종 + 화면모양 검사 | **435 [OK] / 0 FAIL** |
| 어댑터 스모크 | **147항목** |
| 화면 실측 | 과제 수정·생성·삭제·복구·영구삭제 · 성과 삭제·생성·수정 · 연결·기여도 |

실측으로 잡은 결함 4가지 — 전부 **코드 검토로는 안 보이던 것**들이다.

1. 관계 키 6개가 매 저장마다 걸러짐 (편집창의 타입 정규화·기본값 채우기)
2. 권한 분류표에 컬럼 17개 누락 → 생성 시 통째로 빠질 뻔 (`members_json` 은 권한 부여 필드였다)
3. 활동 로그가 V2 경로에서 유실
4. 연결 참조 키가 화면이 못 읽는 이름 (위)

### 7.5-4.7 일괄 추가 — 9/10 (2026-07-30)

일괄추가는 5가지 타입(과제·연결·액션아이템·팀멤버·성과)이 한 함수로 들어오고, 각 타입 함수가
**전체 배열**을 돌려준다. 그래서 타입으로 분기하지 않고 **변경 전 배열과 대조해 바뀐 것만
골라낸다** — 타입이 무엇이었든 diff 가 찾아낸다.

단건 흐름(`createProjectFlowV2`·`patchProjectEditV2`·`savePerformanceEditV2`)을 그대로
재사용하므로 순서 규칙(생성→연결, 연결→필드)이 자동으로 지켜진다.
**성과를 과제보다 먼저** 보낸다 — 새 과제가 새 성과를 참조할 수 있다.

**보내기 전에 전부 검사한다.** 절반 보내고 못 보내는 항목을 만나면 되돌릴 수 없으므로,
보낼 수 없는 게 하나라도 있으면 아무것도 보내지 않고 전체를 V1 으로 넘긴다.

확인된 사실 둘:

- `settings` 는 **V1 서버도 이 페이로드에서 읽지 않는다**(설정은 별도 API). 잔재였다.
- `metadata` 는 V2 가 **의도적으로 받지 않는다** — 개수는 파생이고 화면 취향은 사용자별이라,
  저장할 때마다 전역 metadata 를 덮던 버그를 그렇게 고쳤다(`routes_v2.py` metadata 절).
  그래서 V2 경로가 metadata 를 안 보내는 것이 맞다.

#### 실측 (개발서버)

성과 3건(204·205·206) · 과제 2건(ProjA·ProjB) 생성. 전부 dt2 에 반영되고 생성 이력도 남았다.
새 과제의 `사업부 'MX' → division_id=17` 해석도 일괄 경로에서 그대로 동작했다.

`dt3_check_frontend_shape.py` **첫 실전 사용 — 통과**(과제 226 / 성과 436, 참조 217건 전부 읽힘).

| 저장 경로 10곳 | 상태 |
|---|---|
| 과제 수정·생성(+연결) · 삭제/복구/영구삭제 · 성과삭제 | ✅ 실측 |
| 기여도 일괄 · 연결 편집 · 성과 생성/수정 · **일괄추가** | ✅ 실측 |
| 자동보정 · createdAt 백필 | ❌ V1 (저빈도) |
| 수동 서버 업로드 | ❌ V2 대응 없음 — 폐지 예정 |

### 7.5-4.8 반입 점검 — 폴더를 통째로 옮기기 때문에 필요하다 (2026-07-30)

배포는 **52 폴더를 통째로 압축해 운영으로 옮기고 → `backend` 에서 `flask db upgrade` →
기동** 이다. 업로드 파일만 별도 이관한다. 이 방식에는 구멍이 하나 있다.

> **`backend/.env` 가 같이 따라가면 안 된다.**
>
> 개발 `.env` 에는 `FLASK_ENV=development` 와, 시험하려고 켠 `DT2_WRITE_ENABLED=true` 가
> 들어 있다. 운영에 그대로 올라가면 —
>
> · **컷오버 전인데 V2 쓰기가 열리고 `v2_sync` 가 멈춘다.** V1 이 정본인 채로 dt2 만
>   앞서가 데이터가 갈라진다.
> · development 설정이라 **시험용 쓰기 우회 헤더(`X-DT2-Allow-Write`)까지 열린다.**
>
> 쓰기 스위치를 코드 상수에서 환경변수로 옮긴 것(7.5-4 서두)은 "개발에서 켠 게 운영에
> 따라가지 않게" 하려던 것이었다. **`.env` 가 함께 이동하면 그 전제가 깨진다.**

| 항목 | 반입 |
|---|---|
| `backend/.env` | ❌ **운영 것을 유지** (덮어쓰지 않는다) |
| `backend/uploads` (257MB) | ❌ 별도 이관 |
| `backend/scripts/out` | ❌ 개발 로그. 갈 필요 없다 |
| `frontend/node_modules` (229MB) | ❌ 운영은 빌드하지 않는다 |
| **`frontend/dist`** | ✅ **반드시** (운영 빌드 금지 — 부분 실패하면 에러 없이 화면만 깨진다) |

#### `scripts\dt3_preflight.py` — 기동 **직전**에 돌린다

```
폴더 풀기 → flask db upgrade → **python scripts\dt3_preflight.py** → 기동
```

읽기 전용이고 숫자·이름·판정만 출력한다(비밀값은 길이만, DSN 은 가려서).

- `.env` — `FLASK_ENV=production` · `DT2_WRITE_ENABLED` 기대값 · 비밀키가 예시값이 아닌지 ·
  DSN 이 개발 기본값/시험 DB 가 아닌지
- 빌드 자산 — `index.html` 이 가리키는 파일이 실제로 있는지
- 마이그레이션 — 코드 head 와 DB head 가 같은지
- dt2 테이블 10개가 다 있는지, 데이터 규모(개발 DB 에 붙었는지 감지)

컷오버 때는 `--cutover` 를 붙인다 — 그 시점에는 `DT2_WRITE_ENABLED` 가 켜져 있어야 정상이다.

> 만들면서 스크립트 자체에서 오진 둘을 잡았다(고치기 전에 실측으로 확인함).
> ① **병합 리비전은 `down_revision` 이 튜플**이라 문자열만 읽으면 head 가 3개로 잡힌다
>    (`17ca79b15ca6_merge_multiple_heads.py`). 마이그레이션은 멀쩡했고 정규식이 문제였다.
> ② dt2 테이블은 **10개**다. Phase 2 초기의 '9개' 는 그 뒤 `dt2_project_changes` ·
>    `dt2_change_proposals` 가 늘기 전 숫자다. 그래서 개수가 아니라 **이름으로** 확인한다.

### 7.5-4.9 마지막 두 경로 — 저장 전환 완료 (2026-07-30)

**createdAt 백필** — V2 로 전환(바뀐 과제만 N×PATCH). 이걸 V1 에 두면 컷오버 후
**조용한 거짓말**이 된다: 화면엔 성공이라 뜨는데 낡은 V1 에만 쓰이고 dt2 에는 아무것도
안 들어간다. 관리자는 채웠다고 믿는다.

**자동 보정** — V2 에서는 **서버로 보내지 않는다**(로컬 정리까지만). V2 는 연결을
테이블로 들고 있어 고아가 생길 수 없고, 화면이 고아로 보는 것은 이관이 **일부러 보존한
V1 원본 고아**뿐이다. 되돌려 쓰면 보존하기로 한 원본을 지우는 셈이다.

#### 저장 경로 10곳 최종

| 경로 | 컷오버 후 |
|---|---|
| 과제 수정·생성(+연결) · 삭제/복구/영구삭제 · 성과삭제 | V2 |
| 기여도 일괄 · 연결 편집 · 성과 생성/수정 · 일괄추가 · createdAt 백필 | V2 |
| 자동 보정 | 보내지 않음 (로컬만) |
| **수동 서버 업로드** | **V2 대응 없음 — 화면에서 감춘다** (스냅샷 메뉴와 함께) |

**V1 로 쓰는 경로가 남지 않는다.** 검증: 어댑터 스모크 **189항목**.

### 7.5-5. 컷오버 전 데이터 정리

- 소유자 없는 활성 과제 **1건**
- manager 없는 사업부 1개 → 과제 **9건**

둘 다 admin·dt_office 는 접근되므로 진행을 막지는 않는다.

### 7.5-6. 컷오버 후 '스냅샷' — **잠정 결정: 화면 기능 폐지** (2026-07-30)

지금은 `dashboard_data` JSON 을 통째로 복사한다. V2 가 정본이 되면 그 JSON 은 정본이 아니다.

| 방식 | 장점 | 단점 |
|---|---|---|
| dt2 를 조립해 지금 형식으로 저장 | 복원 로직 재사용 | 실데이터의 몇 배를 다시 쌓는다 |
| DB 덤프로 대체 | 가볍고 정확 | 화면에서 복원 못 함 |
| 변경 로그로 대체 | `dt2_project_changes` 가 이미 있음 | 임의 시점 복원 불가 |

#### 결정 근거 — 아무도 안 쓰고 있었다

개발 DB 실측:

| 타입 | 건수 | 마지막 |
|---|---|---|
| auto | 337건 (94%) | 2026-07-28 — Phase 1 에서 끈 그 시점 |
| upsert | 11건 | 2026-02-04 |
| **manual**(사람이 의도) | **6건** | **2025-11-24** |
| upload | 4건 | 2025-12-15 |

```
dashboard_snapshots  42 MB      ← 스냅샷
dt2_* 전체            6 MB      ← 실제 데이터
```

사람이 만든 스냅샷은 **8개월째 0건**인데 실데이터의 **7배**를 차지한다.

또 복원(`handleRestoreSnapshot`)은 **로컬에만 되돌린다.** 서버에 반영하려면 전체 덮어쓰기
(`/data/upload`)를 눌러야 하는데, 그건 V2 대응이 없는 폐지 예정 경로다.

#### 스냅샷이 막으려던 사고가 V2 에서 사라진다

| 사고 | V1 | V2 |
|---|---|---|
| `forceUpload` 가 남의 과제를 통째로 날림 | 실제로 있었다(고아 첨부 6건의 추정 원인) | 전체 덮어쓰기 경로 자체가 없다 |
| 낡은 배열이 최신 데이터를 덮음 | 매 저장마다 위험 | 과제별 PATCH + 필드 단위 충돌 감지 |
| 실수로 삭제 | 복구 어려움 | soft delete + 복구 API |
| 잘못된 값 저장 | 되돌릴 근거 없음 | `dt2_project_changes` 에 필드마다 before→after |

#### 대체 — 두 축으로 나눈다

| 목적 | 수단 | 상태 |
|---|---|---|
| **재해 복구**(DB 손상·디스크) | **DB 덤프** | ✅ Phase 0-0 백업 런북 |
| **되돌리기**(잘못 고침) | **변경 로그** `dt2_project_changes` | ✅ 이미 쌓이는 중 |

재해 복구는 원래 스냅샷의 영역이 아니다 — JSON 한 덩어리로는 dt2 9개 테이블·이력·연결을
되살릴 수 없다. 되돌리기는 변경 로그가 **더 정밀하다**: "그 시점 전체로" 대신
"이 과제의 이 필드를 이전 값으로" 가 되고, 남의 작업을 같이 되감지 않는다.

#### ⚠️ 확정 전 남은 확인 — 운영 수치

위는 **개발 DB** 수치다. 운영은 다를 수 있고, 이 문서는 전에 그걸로 한 번 틀렸다
(2-0 의 '보유 과제수' 가 개발 수치였던 건).

```
운영서버에서:  python scripts\dt3_snapshot_usage.py
```

읽기 전용이고 **숫자·타입·날짜만** 출력한다(내용도 사람 이름도 안 찍는다. 사람 수만 센다).
그대로 복사해 와도 반출 부담이 없다.

- `[OK]` (최근 사람이 만든 스냅샷 0건) → **폐지 확정**
- `[FAIL]` (쓰고 있음) → 누가 어떤 상황에 쓰는지 확인하고 대안을 정한다

#### 폐지할 때 할 일

1. 화면에서 스냅샷 메뉴 감추기 (컷오버 배포에 포함)
2. `dashboard_snapshots` 테이블은 **지우지 않는다** — V1 폐기까지 과거 기록으로 보존
3. 되돌리기 화면이 필요하면 나중에 `dt2_project_changes` 기반으로 (컷오버 필수 아님)

### 7.5-7. 진행 순서

```
1. ✅ V1→V2 즉시 동기화      (2026-07-29 완료)
2. 성과 · 연결 쓰기 API
3. 삭제 / 복구 API
4. 프론트 저장 전환          ← 가장 큼
5. 스냅샷 방식 결정 · 구현
6. 데이터 정리 + 컷오버 런북
7. 컷오버 — 읽기·쓰기를 **한 번에**
```

### 7.5-8. 읽기만 먼저 전환하지 않기로 한 이유 (2026-07-29 결정)

초안은 "읽기를 먼저 옮겨 동결 창을 쪼갠다" 였다. **채택하지 않는다.**

- 읽기 전환의 목적인 "V2 읽기가 맞는가" 는 이미 운영 데이터로 증명됐다
  (`dt2_compare_api.py` 전항 0, 화면·CSV 직접 대조 일치).
  기본값을 바꿔 추가로 얻는 건 "다른 사용자 화면에서도 괜찮은가" 정도다.
- 대가로 **쓰기 V1 / 읽기 V2 하이브리드** 상태가 생긴다. 동기화 지연 창,
  조용한 동기화 실패, 진실이 둘인 상태, 재배포 없이는 못 되돌리는 기본값.
- 이를 막을 방어 코드(반영 버전 가드·동기화 모니터링)는 **컷오버하면 버려진다.**
- 무엇보다 **컷오버 위험의 대부분은 쓰기 쪽**이고, 읽기를 먼저 옮겨도 그건 안 줄어든다.

대신 7.5-3 동기화가 같은 검증을 **위험 없이** 해준다.
사용자는 계속 V1 만 보는데, 저장할 때마다 dt2 가 실제 변경을 흡수한다.
주기적으로 `dt2_compare_api.py` 를 돌리면 "V2 가 여전히 V1 과 같다" 가 실데이터로
매일 확인된다. 읽기를 옮기는 것보다 안전하면서 검증 강도는 더 높다.

> 읽기 토글(`localStorage.dtV2Read`)은 **검증 도구로 남긴다.**
> 기본값 전환은 컷오버(7번)에 흡수한다.

### Phase 3 컷오버 런북

| 순서 | 작업 | 되돌리기 |
|---|---|---|
| 1 | 전체 덤프 + 코드 zip 스냅샷 | — |
| 2 | 최종 V1→V2 복사 + `dt2_verify.py` · `dt2_compare_api.py` 통과 확인 | 불일치 시 중단 |
| 3 | **공지 후 V1 읽기 전용 동결** (짧게, 수십 분 목표) | 동결 해제 |
| 4 | 마지막 증분 복사 + 재검증 | 3으로 |
| 5 | **쓰기 스위치 전환** — 운영 `.env` 에 `DT2_WRITE_ENABLED=true` 추가 후 백엔드 재기동 | 값 제거 후 재기동 |
| 6 | 프론트 V2 전환 배포 | 이전 zip 재배포 |
| 7 | 관찰 1~2주 (V1은 읽기 전용 보존) | V1 정본 복귀 |
| 8 | 안정 확인 후 V1 폐기 | — |

#### 5번 상세 — 쓰기 스위치 (2026-07-30 변경)

**코드를 고쳐서 켜지 않는다.** 예전에는 `routes_v2.V2_WRITE_ENABLED` 상수였는데,
그러면 개발서버에서 켠 파일이 그대로 반입돼 **운영에서도 켜진다.** 그래서 환경변수로 옮겼다
(`config.DT2_WRITE_ENABLED`).

`v2_sync` 중단은 **따로 하지 않는다.** 같은 값을 보고 스스로 멈춘다 —
"둘을 한 쌍으로 같이 뒤집는다"는 수동 절차를 없앴다. 둘이 동시에 켜지면
V2 에 쓴 값이 V1 기준 동기화에 덮여 사라지는데(2026-07-29 실측), 이제 구조적으로 불가능하다.

기동 로그에서 상태를 눈으로 확인한다:

```
[DT-V2] 쓰기 차단 (컷오버 전) — 정본=dashboard_data, 저장 시 dt2_* 동기화
[DT-V2] 쓰기 활성 (컷오버 완료 상태) — 정본=dt2_*, V1→V2 동기화 중단됨
```

> ⚠️ 운영 기동 로그에 **`[DT-V2] 시험 우회 헤더 허용됨`** 줄이 보이면 안 된다.
> 보인다면 `FLASK_ENV` 가 `production` 이 아니라는 뜻이다.

시험 우회 헤더(`X-DT2-Allow-Write: test`)는 개발·시험 설정에서만 통한다.
운영에서는 **환경변수로도 열 수 없다**(`ProductionConfig` 하드코딩). 열려 있으면
유효 토큰만 있으면 누구나 dt2 에 직접 쓸 수 있고, 그 값은 다음 저장에 덮여 사라진다.

검증: `python scripts\dt3_test_writeswitch.py` (29항목 — 기본 차단·값 파싱·운영 우회 차단·
인증 우선순위·읽기 비차단·스위치 동작·상호배타). 데이터를 건드리지 않으므로 언제든 돌려도 된다.

> **동결 창을 짧게 유지하는 것이 divergence를 막는 유일한 방어책이다.** 이관은 미리 끝내두고, 동결 중엔 증분만 돌린다.
>
> 7.5-3 동기화가 들어가면 동결 창이 더 짧아진다. V2 가 이미 실시간으로 맞춰져 있어
> 4번에서 확인할 것이 줄기 때문이다.

---

## 8. Phase 4 — MCP 서버 (2~3일)

### 8-1. 원칙

> **MCP는 Phase 3 REST API를 호출만 하는 얇은 래퍼다. DB에 직접 접근하지 않는다.**
> 권한·락·감사는 전부 API 안에 있고, 사람 경로와 AI 경로가 **같은 관문**을 통과한다.
> MCP에 권한 로직을 따로 넣으면 두 벌이 되어 반드시 어긋난다.

### 8-2. 도구 표면

| 도구 | 동작 |
|---|---|
| `list_projects(division?, status?, owner?, q?)` | 요약만. 권한 필터 적용 |
| `get_project(uuid)` | 1건. 이미지는 참조 URL |
| `update_project(uuid, patch, expected_version)` | 저위험 → 즉시 / 핵심 → 제안 접수(202) |
| `add_action_item(uuid, item)` | 저위험, 즉시 |
| `update_monthly_progress(uuid, month, text)` | 저위험, 즉시 |
| `propose_change(uuid, patch, reason)` | 핵심 필드 명시적 제안 |
| `list_proposals(status?)` | 제안 현황 조회 (승인은 사람이 UI에서) |

### 8-3. AI 신원

- **서비스 계정 + `on_behalf_of` 기록** — 통제와 감사를 둘 다 확보
- 서비스 계정의 권한은 일반 사용자와 동일하게 `can_edit_project()`를 통과해야 한다 (특권 없음)

### 8-4. 안전 순서

> **Phase 3의 `row_version` 락과 감사 로그가 완료되기 전에는 MCP 쓰기를 열지 않는다.**
> 읽기 도구(`list_projects`, `get_project`)는 먼저 열어도 무방하다.

---

## 9. 전체 일정

> ★ **운영 실측 반영으로 재조정됨 (2026-07-28).** 변경점:
> - **Phase 1을 최우선으로 올림** — 이미지가 payload의 94.4%, 스냅샷 25.2 GB가 계속 증가 중
> - **Phase 0-3(`updatedAt` 백필) 삭제** — 운영 결측 0건, 불필요
> - **Phase 0-2(소유자 확정) 대폭 축소** — 자동 매핑 99.7%, 현업 확인 1건 → 임계경로 아님
> - 결과적으로 **전체 일정 단축**, 다만 이미지 이관량 증가(3건→23건, 33.9 MB)로 Phase 1-2는 공수 증가

| Phase | 내용 | 기간 | 병렬 | 선행 |
|---|---|---|---|---|
| 0-0 | **백업 확보 + staging 복원** ⚠️ 25.2 GB 스냅샷 포함 → 디스크 여유 확인 필수 | 0.5~1일 | | — |
| **1 (긴급)** | **자동 스냅샷 중단 + 누적분 정리 (25.2 GB 회수)** | **0.5일** | | 0-0 |
| **1 (최우선)** | **이미지 파일 분리 (33.9 MB → 참조)** | **1~1.5일** | | 0-0 |
| 1 | gzip + 저장후 재다운로드 제거 | 0.5일 | | |
| 0 | 기반 데이터 정리 (소유자 1건 확인 · 조직 백필) | **0.5일** (현업 대기 사실상 없음) | ✅ Phase 1과 | 0-0 |
| 2 | V2 정규화 (스키마·이관·검증·읽기 API) | 4~6일 | | 0 |
| 3 | 쓰기 API + 권한 + 컷오버 | 3~4일 | | 2 |
| 4 | MCP 서버 | 2~3일 | | 3 |
| | 소계 | 약 3주 | | |
| +α | **운영서버 대응 견고화** (dry-run·사전조건검사·롤백·로그·런북) | **+3~5일** | Phase별 분산 | 2.6 |
| | **합계** | **약 3.5~4주** (현업 확인 대기 별도) | | |

> 운영서버에서 AI를 쓸 수 없으므로 **스크립트가 사람 대신 판단**해야 한다. 그 비용이 +3~5일이다.
> 시행착오가 불가능한 환경에서는 이 투자가 가장 싸다.

---

## 10. 검증 게이트 요약 — 통과 못 하면 다음 단계 금지

> 모든 게이트는 **운영서버에서 스크립트가 `[OK]`/`[FAIL]`로 판정**한다. 사람이 해석하지 않는다. (2.6.4)

| Phase | 게이트 | 판정 위치 |
|---|---|---|
| 0-0 | `dxdigitaltwin_stg` 복원 성공 + 과제 조회됨 → **백업이 실제로 살아남을 증명** | 운영 staging |
| 0-1 | 스캔 완료, **2-0 인벤토리에 없는 키 목록 확보** (있어도 진행 — `extra_fields`가 받음) | 운영 |
| 0-2 | 활성 과제 **전부** 유효한 `owner_user_id` 보유 (admin 임시 포함) | 운영 |
| 0-3 | `updatedAt` NULL 0건, **과제 수 불변** | 운영 (staging 선행) |
| 0-4 | `departments.division_id` NULL 0건 | 운영 (staging 선행) |
| 1-2 | 이미지 전건 정상 표시, `projects` payload 목표치 이하 | 운영 |
| 2-2 | 이관 완료, **미매핑 키 0** (전부 컬럼 또는 `extra_fields`로 귀속) | 운영 staging → 운영 |
| **2-3** | **왕복 검증: 원본 JSON == 재조립 JSON, 불일치 0** ★ 최중요 | 운영 staging → 운영 |
| 3 | 권한 매트릭스 전항 통과, 동시 쓰기 테스트에서 lost update 0 | 개발(합성) + 운영 staging |
| 4 | MCP 도구가 권한 없는 과제에 403 반환 | 개발 + 운영 |

---

## 11. 리스크 등록부

| 리스크 | 확률 | 영향 | 대응 |
|---|---|---|---|
| 소유자 확정 지연으로 전체 지연 | 높음 | 중 | 미확정은 admin 임시 소유 → Phase 2~3 병렬 진행 |
| 이관 중 데이터 불일치 | 중 | 높음 | V1이 정본이므로 V2 비우고 재복사. `dt2_verify.py` 게이트 |
| `updatedAt` 백필이 잘못된 시각을 넣음 | 중 | 중 | **과거로만** 넣는다. test DB 선행 실행 |
| 컷오버 후 문제 발견 | 중 | 높음 | V1 읽기 전용 1~2주 보존 → 정본 복귀 가능 |
| AI가 사람 작업을 덮어씀 | 낮음 | 높음 | Phase 3 완료 전 MCP 쓰기 금지. 핵심 필드는 승인 경유 |
| 권한 로직이 REST/MCP 두 벌로 갈라짐 | 중 | 높음 | MCP는 REST 호출만. DB 직접 접근 금지를 코드 리뷰 기준으로 |
| 스냅샷 정리 중 필요한 백업 삭제 | 낮음 | 중 | `manual`/`upload` 전량 보존, 삭제 전 덤프 |
| 운영 중 배포로 인한 중단 | 중 | 중 | Phase 1·2는 무중단(Additive). 동결은 Phase 3 컷오버 때만 |
| **운영서버에서 스크립트가 예상 밖 동작** | 중 | 높음 | **운영 덤프 복제본으로 개발서버에서 N회 리허설.** dry-run 필수, 사전조건 자동 거부 (2.6.4) |
| **운영서버에서 사람이 즉석 판단해야 하는 상황 발생** | 중 | 높음 | 런북에 "[OK] 아니면 중단"만 남긴다. 판단 지점 자체를 없앤다 (2.6.7) |
| **오프라인이라 필요한 패키지를 못 깔음** | 중 | 중 | 신규 패키지를 `Flask-Compress` 1개로 제한. 또는 프록시 gzip으로 0개 (2.6.5) |
| **운영 데이터 반출 불가 → 개발서버에서 실물 리허설 불가** | **확정** | 높음 | ① `extra_fields` catch-all ② **운영서버 내부 staging DB**에서 리허설 ③ 왕복 검증, 숫자만 반출 (2.6.3) |
| **개발 데이터로 세운 수치가 운영과 다름** | **확정** | 중 | Phase 0-1 운영 스캔으로 전면 교체. 구조적 결론은 유효하므로 **설계는 안 바뀜** |
| **운영에만 있는 미지의 키 → 이관 시 유실** | 중 | 높음 | **`extra_fields`가 무조건 받아낸다.** 인벤토리가 틀려도 손실 없음 |
| 운영 이미지 보유 과제가 예상보다 많음 | 중 | 중 | Phase 0-1에서 먼저 측정 → Phase 1-2 우선순위 상향 |
| `linkedProjects` 역방향 참조 누락으로 연결 유실 | 중 | 높음 | 이관 시 양방향 대조 + 차집합 리포트 (2-0 판단사항 라) |
| `reportConfirmations` 미이관으로 보고확인 상태 소실 | 중 | 중 | `dt2_projects.report_confirmation`으로 흡수 (2-0 판단사항 바) |

---

## 12. 지금 바로 할 수 있는 것

| # | 작업 | 위치 | 비고 |
|---|---|---|---|
| 1 | **`scripts/dt_scan.py` 작성** (읽기 전용, 값 미출력) | 개발서버 | AI로 작성. 반입 대상 |
| 2 | **운영서버에서 스캔 실행** → `dt_scan_YYYYMMDD.md` | 운영서버 | **본 문서 수치를 운영 기준으로 교체** |
| 3 | `python export_database.py` (백업) | 운영서버 | 8개월 만. 서버 내부 보관 |
| 4 | 덤프를 `dxdigitaltwin_stg`로 복원 | 운영서버 | 복원 가능함을 증명 + 리허설 환경 확보 |
| 5 | 스캔 결과로 **매핑표 CSV 생성** → 현업 회람 | 운영→현업 | **대기시간 최장. 가장 먼저 띄운다** |
| 6 | 합성 데이터셋 생성 + Phase 1 착수 | 개발서버 | 5를 기다리는 동안 병렬 |

> **1→2가 최우선이다.** 본 문서의 수치는 개발 데이터라, 운영 스캔 전에는 Phase 2 규모를 확정할 수 없다.
> 반대로 **설계와 방향은 스캔 결과와 무관하게 확정**이므로, Phase 1과 스크립트 작성은 지금 시작해도 된다.
