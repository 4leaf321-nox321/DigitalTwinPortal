# ⚠️ mcp 2.0 에서 `mcp.server.fastmcp` 가 사라졌다. 그냥 `pip install mcp` 하면
#    2.x 가 잡혀 import 부터 깨진다. 참고 구현(ReportArchive)이 1.27 로 돌고 있고
#    그 패턴을 그대로 쓰므로 1.x 로 고정한다. 2.x 이관은 별도 작업.
mcp>=1.27.0,<2.0.0
httpx>=0.27.0
