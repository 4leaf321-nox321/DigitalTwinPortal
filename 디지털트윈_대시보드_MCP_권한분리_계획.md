# 디지털 트윈 대시보드 — AI(MCP) 쓰기를 위한 과제별 권한 분리 계획

> 작성일: 2026-07-28 · 목표: **AI가 MCP로 과제를 수정하되, 과제 단위로 수정 권한을 통제할 수 있는 구조**
> 선행 문서: [저장성능 개선계획](./디지털트윈_대시보드_저장성능_개선계획.md) · [정규화 검토](./디지털트윈_대시보드_정규화_검토.md)

---

## 0. 우선순위 재조정

앞선 성능 문서에서 정규화(V2)를 **후순위**로 권했으나, **MCP 쓰기 요구사항이 들어오면 그 판단은 뒤집힌다.**

- 성능은 정규화 없이도 고칠 수 있다 (payload를 줄이면 됨).
- **과제별 쓰기 권한은 정규화 없이 고칠 수 없다.** 권한을 걸 대상(행)과 주체(소유자)가 DB에 존재하지 않기 때문이다.
- 다행히 **성능 개선 항목의 상당수가 MCP 준비 작업과 같은 길 위에 있다.** 헛수고가 아니라 앞당기는 것.

---

## 1. 지금 구조가 과제별 권한을 막는 5가지

### 차단 1. 권한을 걸 "대상"이 없다

모든 쓰기가 `/data/upsert` 하나로 들어오고, 그 호출은 `dashboard_data` **한 행 전체**를 다시 쓴다.

```
현재 권한의 해상도:  "이 API를 호출할 수 있다"  =  "328개 과제 전부를 고칠 수 있다"
                     "호출할 수 없다"           =  "아무것도 못 고친다"
```

과제 단위로 나눌 지점 자체가 없다. AI에게 "MX-29만 수정해"라고 시켜도, **API 레벨에서는 전체 쓰기 권한을 준 것**이다.

### 차단 2. 과제에 식별 가능한 "주체"가 없다 ⚠️ 가장 과소평가된 문제

| 필드 | 저장 형태 | users 계정과 매칭 |
|---|---|---|
| `과제PL` | 자유 텍스트 | **활성 224건 중 4건 (1.8%)** |
| `작성자` | 자유 텍스트 | 21건 (9%) |
| `담당부서목록` | 문자열 배열 | FK 없음 |

실제 값 분포: `박용진`(71), `PL7`/`PL8`/`PL9`…(각 20), `test`, `TTTT`, `SDFSDF`, 빈 값(26).

**"누가 이 과제를 고칠 수 있는가"를 계산할 근거가 DB에 없다.** 테이블을 나눠도 이게 없으면 권한 규칙을 쓸 수 없다.
→ 정규화보다 **더 앞선** 전제조건.

### 차단 3. 동시 쓰기에 잃어버린 갱신(lost update)이 있다

`routes.py:846`은 `DashboardData.query.first()`로 **락 없이** 읽고 → 파이썬에서 병합 → 통째로 UPDATE 한다.

```
사용자 A 저장 요청 ─ 읽기(v571) ─ 병합 ─────────── 커밋(v572)
AI    B 저장 요청 ───── 읽기(v571) ─ 병합 ── 커밋(v572)   ← A의 변경을 통째로 되돌림
```

사람끼리는 드물지만 **AI가 자동으로 쓰기 시작하면 상시 발생**한다.

게다가 병합의 최신성 판정이 `updatedAt` 문자열 비교(`routes.py:900`)인데:

| `updatedAt` 상태 | 과제 수 |
|---|---|
| ISO+Z 정상 | 128 |
| **NULL** | **199** |
| Z 없음 | 1 |

**60%가 NULL** → `'' >= ''`가 참 → **무조건 덮어쓴다.** 지금도 이미 안전장치가 사실상 죽어 있다.

### 차단 4. AI에게 줄 수 있는 데이터 단위가 "전부"뿐

`GET /data`는 1.21 MB 통짜다. MCP 도구가 과제 1건을 읽으려 해도 전부를 받아야 한다.

- **컨텍스트 낭비**: 1.21 MB 중 55%가 base64 이미지. 과제 1건 읽자고 컨텍스트를 태운다.
- **정보 범위 문제**: 권한 없는 과제까지 전부 노출된다. 읽기 권한도 과제별로 못 나눈다.

### 차단 5. 서버가 감사 로그를 만들지 못한다

`dashboard_activity_logs`는 **프론트가 보낸 요약 텍스트를 그대로 저장**한다(`routes.py:1151-1164`).
서버는 실제로 무엇이 바뀌었는지 모른다. AI 쓰기는 "누가·무엇을·언제" 추적이 필수인데, 현재는 **호출자가 스스로 신고한 내용**을 믿는 구조다.

---

## 2. 목표 구조

### 2.1 스키마 (기존 V2 계획 + 권한 컬럼)

```sql
projects
  uuid            PK                      -- 스캔 확인: 328건 전부 고유
  id              UNIQUE                  -- "VD-1"
  과제명, 사업부, 프로세스, 과제영역, 과제구분, 시작, 종료, 진행상태, ...
  액션아이템목록/과제참여인력목록/담당부서목록/월간진척현황  JSON  -- 1차엔 행 내 JSON 유지

  owner_user_id   FK → users.id           -- ★ 신설: 권한의 근거
  division_id     FK → divisions.id       -- ★ 부서/사업부 단위 권한용
  row_version     INT   DEFAULT 1         -- ★ 과제별 낙관적 락 (전역 version 대체)
  updated_at, updated_by FK → users.id    -- ★ 서버가 직접 기록
  is_deleted, deleted_at, deleted_by

project_editors                            -- ★ 명시적 편집자 (권한 모델에 따라 선택)
  project_uuid FK, user_id FK, can_edit, granted_by, granted_at
  PRIMARY KEY (project_uuid, user_id)

performances / project_performance / project_attachments
  -- 정규화 검토 문서 6장과 동일
```

### 2.2 권한 판정 지점 하나로 집중

```python
def can_edit_project(actor, project) -> bool:
    if actor.role == 'admin':                       return True
    if project.owner_user_id == actor.id:           return True
    if actor.id in project.editor_ids:              return True
    if actor.role == 'manager' and actor.division == project.division:  return True
    return False
```

**이 함수 한 줄이 지금은 쓸 수가 없다.** `project.owner_user_id`가 존재하지 않기 때문이다.
Phase 0~2가 끝나면 이 함수가 성립하고, 그 다음부터는 사람이든 AI든 같은 관문을 통과한다.

### 2.3 MCP 도구 표면

```
list_projects(filter)                             → 요약 필드만, can_view 필터 적용
get_project(uuid)                                 → 1건 (이미지는 참조 URL만)
update_project(uuid, patch, expected_version)     → 403(권한) / 409(충돌) / 200
add_action_item(project_uuid, item)
update_monthly_progress(project_uuid, month, text)
link_performance(project_uuid, performance_uuid, 과제기여도)
```

- **MCP 서버는 얇은 래퍼여야 한다.** 권한·락·감사는 전부 REST API 안에 있고, MCP는 그걸 호출만 한다.
  → 사람이 쓰는 경로와 AI가 쓰는 경로가 **같은 관문**을 통과 (이게 핵심).
- MCP에 권한 로직을 따로 넣으면 두 벌이 되어 반드시 어긋난다.

---

## 3. 단계별 계획

### Phase 0 — 데이터 정합성 복구 (1~2일) ★ 이게 없으면 뒤가 전부 뜬다

| # | 작업 | 비고 |
|---|---|---|
| 0-1 | `updatedAt` NULL 199건 백필 | 현재 병합 안전장치가 죽어 있음. 지금도 위험 |
| 0-2 | **과제 소유자 확정** | `과제PL` 자동매칭 4건뿐 → **사람 판단 필요**. 매핑표 작성 or 일괄 지정 |
| 0-3 | `과제PL`/`작성자` → user_id 매핑표 산출 | 미매칭분 목록 뽑아 확인 요청 |

> 0-2가 이 프로젝트 전체의 진짜 병목이다. 기술이 아니라 **"누가 어느 과제의 주인인가"를 정하는 일**이다.
> 224건을 사람이 확인해야 하므로 **지금부터 병렬로 시작**하는 게 좋다.

### Phase 1 — 성능 개선 중 MCP에도 필요한 것 (0.5~1일)

[저장성능 개선계획](./디지털트윈_대시보드_저장성능_개선계획.md)의 1단계를 그대로 수행하되, **이미지 분리(H)를 앞당긴다.**

| 항목 | MCP 관점의 이유 |
|---|---|
| **이미지 압축 → 파일 분리** | AI가 과제를 읽을 때 base64가 섞이면 안 됨 (차단 4) |
| 자동 스냅샷 제거/정리 | AI가 자주 쓰면 스냅샷이 폭증 |
| gzip + `ensure_ascii=False` | 도구 응답 크기 |
| 저장 후 전체 재다운로드 제거 | 부분 쓰기로 가는 사전작업 |

### Phase 2 — 과제별 행으로 정규화 (3~5일)

[정규화 검토 문서 12장](./디지털트윈_대시보드_정규화_검토.md)의 **"V2 모듈 복제 병행"** 안을 그대로 쓰되, 스키마에 **`owner_user_id` / `row_version` / `updated_by`를 추가**한다.

- 프론트 계약은 어댑터로 유지(배열↔행) → 프론트 무손상
- 검증 게이트: `V1 GET /data` vs `V2 GET /data` 자동 비교로 동일성 증명 후 진행
- 삭제분 104건은 `is_deleted` 컬럼으로 함께 이관 (권한·감사 이력 보존)

### Phase 3 — 과제별 쓰기 API + 권한 관문 (2~3일)

| # | 작업 |
|---|---|
| 3-1 | `PATCH /projects/<uuid>` — `SELECT ... FOR UPDATE` + `row_version` 체크 → 409 |
| 3-2 | `can_edit_project()` 중앙화, 모든 쓰기 경로가 반드시 경유 |
| 3-3 | 서버가 before/after diff를 **직접 계산**해 감사 로그 기록 (차단 5 해소) |
| 3-4 | 프론트를 부분 저장으로 전환 → **전체 배열 업로드 폐기** |
| 3-5 | `GET /projects?...` 요약 조회 + 권한 필터 |

이 단계가 끝나면 **MCP 없이도 이미 목표 구조**다. 사람 UI도 안전해진다.

### Phase 4 — MCP 서버 (2~3일)

- Phase 3 API를 감싼 얇은 계층. 도구 6~8개.
- 인증 주체 결정 필요(→ 4장 결정사항 2).
- 각 도구 호출 = 1 REST 호출 = 1 권한 체크 = 1 감사 로그.

### 순서에 대한 핵심 논리

> **MCP를 먼저 만들고 권한을 나중에 붙일 수는 없다.** 붙일 자리가 없기 때문이다.
> 반대로 Phase 2~3만 끝나면 **MCP는 며칠짜리 얇은 래퍼**가 된다.
> Phase 0(소유자 확정)은 코드와 무관하므로 **오늘부터 병렬 진행 가능**.

---

## 3.5 동시성 제어 — "수정 중 잠금"이 정말 필요한가

### 락은 세 종류이고, 섞으면 안 된다

| 종류 | 지속 시간 | 목적 | 판정 |
|---|---|---|---|
| **(a) 트랜잭션 락** `SELECT … FOR UPDATE` | 밀리초 | 쓰기 순간의 원자성. lost update 차단 | **필수** |
| **(b) 낙관적 락** `row_version` 비교 | 편집 시작~저장 | "내가 읽은 뒤 남이 바꿨나" 감지 | **필수** |
| **(c) 비관적 편집 락** "수정 중 잠금" | 분 단위 | 남이 아예 못 들어오게 | **비권장** |

흔히 "수정하는 동안 잠금"이라고 하면 (c)를 떠올리지만, 실제로 필요한 건 **(a) + (b)**다.

### 행 분리가 (a)를 가능하게 한다 — 여기가 진짜 이득

지금은 **락을 걸 행이 없다.** 싱글톤 한 행에 `FOR UPDATE`를 걸면 = **전체 직렬화** =
누군가 저장하는 동안 전원 대기. 그래서 아무도 락을 안 걸고, 그래서 lost update가 난다(차단 3).

```
현재:   dashboard_data 1행 락 = 전 사용자 대기       → 그래서 락을 안 씀 → 덮어쓰기 발생
정규화: projects[MX-29] 행만 락 = MX-2는 영향 없음   → 락을 쓸 수 있게 됨
```

**"과제를 행으로 나눠야 잠금이 된다"는 직관은 정확하다.** 다만 필요한 잠금은 (c)가 아니라 (a)다.

### (c) 비관적 편집 락을 권하지 않는 이유

**이유 1 — 지금 앱은 로컬 우선이라 "편집 시작"을 서버가 모른다.**

과제 편집 모달에 저장 버튼이 **두 개**다 (`DigitalTwinDashboardApp.jsx:3130-3131`):

```
onSubmit         = handleUpdateProject          → IndexedDB에만 저장 (서버 안 감)
onSubmitAndUpload= handleUpdateProjectAndUpload → 로컬 + 서버
```

오프라인에서 편집하고 나중에 올리는 흐름이 살아 있다. **서버가 편집 개시를 관측할 수 없으므로 락을 잡을 수단이 없다.**
(c)를 도입하려면 오프라인 우선 모델을 포기해야 한다 — 그건 별개의 큰 결정이다.

**이유 2 — 락 주변 코드가 락 자체보다 크다.**

- 모달 열고 자리 비움 → 락 방치 → **TTL + 하트비트** 필요
- 브라우저 강제 종료 → stale lock → **관리자 강제 해제 UI** 필요
- 획득 실패 시 UX → 대기? 알림? 강제 진입? → **정책과 화면이 추가로 필요**

**이유 3 — AI가 들어오면 (c)는 오히려 위험하다.**

```
AI 사이클:  get_project → LLM 추론(수 초~수십 초) → update_project
```

이 사이에 락을 쥐고 있다가 AI가 중간에 죽으면(컨텍스트 초과·타임아웃·도구 오류)
**락이 남고 사람이 그 과제를 못 고친다.**

반면 **AI는 재시도가 싸다.** 409를 받으면 다시 읽고 다시 쓰면 그만이다.
사람은 재입력이 비싸서 낙관적 락이 불편할 수 있지만, AI에게는 낙관적 락이 정답이다.

### 락은 필드 단위 충돌을 해결하지 못한다

과제 행 전체를 잠그면, **사람이 진행률을 고치는 동안 AI가 액션아이템을 못 넣는다.**
실제로는 충돌이 아닌데 막히는 것이다.

```
update_project(uuid, patch={진행률: 80}, expected_version=12)
  → 서버가 v12→v13 사이 변경분을 확인
  → 겹치지 않는 필드면 자동 병합, 같은 필드면 409
```

이 구분은 **"어떤 필드가 바뀌었는지"를 서버가 알아야** 가능하다. 지금은 과제를 통째로 받으므로 불가능하다.
→ 이것도 행 분리 + patch API가 푸는 문제.

### 권고

| 계층 | 도입 여부 | 위치 |
|---|---|---|
| 트랜잭션 락 `SELECT … FOR UPDATE` | **필수** | Phase 3 |
| 과제별 `row_version` 낙관적 락 | **필수** | Phase 2 스키마 |
| 필드 단위 patch + 겹침 없을 때 자동 병합 | **권장** | Phase 3 |
| **"편집 중" presence 표시** | **권장 — 락 대신** | Phase 3 이후 |
| 비관적 편집 락 (c) | **비권장** | — |
| AI 전용 짧은 advisory lock (TTL ≤ 30초) | 조건부 | AI를 동시 다중 실행할 때만 |

> **presence는 락이 아니라 알림이다.** "박용진님이 지금 이 과제를 보고 있습니다"를 30초 폴링으로 띄우기만 해도
> 충돌의 대부분이 **발생 전에** 사라진다. WebSocket도, TTL도, 강제 해제 UI도 필요 없다.
> 락의 효과 80%를 비용 10%로 얻는 방법이다.

### 부수 결정 — "로컬만 저장" 버튼을 어떻게 할 것인가

과제별 `row_version`이 생기면 오프라인 우선은 **유지 가능하다**.
로컬에서 편집한 뒤 나중에 올릴 때 서버가 버전을 비교해 충돌을 감지하면 되기 때문이다(= 낙관적 락이 오프라인과 궁합이 좋은 이유).
반대로 비관적 락을 도입하면 이 버튼은 **없애야 한다.** → (c)를 피할 또 하나의 이유.

---

## 4. 지금 결정해야 할 것

### 결정 1. 권한 모델

| 안 | 규칙 | 장단 |
|---|---|---|
| A. 소유자 단독 | 과제PL만 편집 + admin | 단순·명확. PL 부재/교체 시 막힘 |
| B. 부서 단위 | 같은 사업부/부서면 편집 | 현업 흐름에 가까움. 해상도 낮음 |
| C. 명시적 편집자 목록 | `project_editors`로 지정 | 가장 정밀. 관리 비용 발생 |
| D. A + B 혼합 | 소유자 + 같은 부서 manager | **권장**. 현행 role 체계와 접합 좋음 |

### 결정 2. AI의 신원

| 안 | 방식 | 장단 |
|---|---|---|
| a. 사용자 위임 | 사람의 토큰으로 AI가 대행 | 권한 재설계 불필요. "누가 시켰나"가 명확 |
| b. 서비스 계정 | AI 전용 계정 + 별도 권한 | 통제 쉬움. 권한 부여 관리 필요 |
| c. 혼합 | 서비스 계정이되 `on_behalf_of` 기록 | **권장**. 감사와 통제 둘 다 |

### 결정 3. AI 쓰기를 즉시 반영할 것인가, 승인받을 것인가 ★ 설계가 크게 갈린다

| 안 | 구조 | 영향 |
|---|---|---|
| 즉시 반영 | AI가 `update_project`를 직접 커밋 | 단순. 권한 설계가 유일한 방어선 → **Phase 0~3 완주 필수** |
| 제안(draft) 후 승인 | `project_change_proposals` 테이블에 쌓고 사람이 승인 | 테이블·UI 1벌 추가. 대신 **권한 리스크가 크게 줄어** 초기 도입이 안전 |
| 혼합 | 저위험 필드(진척률·액션아이템)는 즉시, 핵심 필드(예산·일정·성과연결)는 승인 | 실무적 |

### 결정 4. 정규화 실행 방식

- **V2 모듈 복제 병행**(기존 채택안): 운영 무손상·롤백 쉬움. 전환기간 두 벌 유지
- **기존 모듈 직접 개조**: 두 벌 관리 없음. 롤백 어려움
- → MCP까지 감안하면 **V2 복제 병행 유지 권장** (AI 쓰기를 V2에서만 먼저 열어 실험 가능)

---

## 5. 예상 일정

| Phase | 내용 | 기간 | 병렬 가능 |
|---|---|---|---|
| 0 | 데이터 정합성 + **소유자 확정** | 1~2일 + 현업 확인 | ✅ 지금 시작 |
| 1 | 성능 개선 (이미지 분리 포함) | 1~2일 | ✅ Phase 0과 병렬 |
| 2 | 과제별 행 정규화 (V2) | 3~5일 | Phase 0 필요 |
| 3 | 과제별 쓰기 API + 권한 관문 | 2~3일 | Phase 2 필요 |
| 4 | MCP 서버 | 2~3일 | Phase 3 필요 |
| | **합계** | **약 2~3주** | |

---

## 6. 리스크

| 리스크 | 대응 |
|---|---|
| **소유자 확정이 안 끝나 전체가 지연** | Phase 0을 최우선·병렬로. 미확정 과제는 admin 소유로 두고 순차 이관 |
| AI 쓰기가 사람 작업을 덮어씀 | Phase 3의 `row_version` 락 완료 **전에는 MCP 쓰기를 열지 않는다** |
| 권한 로직이 REST와 MCP 두 벌로 갈라짐 | MCP는 반드시 REST 호출만. 직접 DB 접근 금지 |
| 전환기 V1/V2 divergence | 기존 문서 방침대로 전환기간 최소화 |
| `updatedAt` NULL로 인한 조용한 덮어쓰기 | **Phase 0-1은 지금 당장** (MCP와 무관하게 현존 위험) |
