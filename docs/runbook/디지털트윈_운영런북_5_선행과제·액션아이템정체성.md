# 디지털 트윈 대시보드 — 선행 과제 + 액션아이템 정체성 반입 런북

작성 2026-08-08 · 대상: 운영 서버(오프라인) 작업자

---

## 이 문서를 쓰는 법

- **위에서부터 순서대로** 한다. 건너뛰지 않는다.
- 회색 상자의 명령을 **그대로 복사해** 실행한다.
- 각 단계 끝의 **확인**이 `[OK]` 면 다음으로, `[FAIL]` 이면 **멈추고** 그 단계의
  「막혔을 때」를 본다.
- **운영서버에는 AI 도 인터넷도 없다.** 판단이 필요한 지점은 전부 미리 적어 뒀다.
  여기 없는 상황을 만나면 **진행하지 말고** 개발 담당자에게 연락한다.
- 되돌리는 방법은 맨 아래 **R장**에 있다. 시작하기 전에 R장을 **먼저 한 번 읽는다.**

**소요 시간** 대략 20분 (백업 시간 제외).

> **런북 4(DX KPI)보다도 가볍다.**
> **DB 스키마를 바꾸지 않는다 — `flask db upgrade` 를 하지 않는다.**
> `.env` 도 고치지 않는다. 새 표도 새 칸도 없다.
> 유일하게 데이터를 건드리는 것은 6장(액션아이템에 `uuid` **한 칸 더하기**)이고,
> 그것도 기존 값은 하나도 안 바꾼다.

---

## 무엇을 하는 작업인가

두 가지다. 서로 독립이지만 같이 반입한다.

### ① 선행 과제 연결 **되살리기**

과제끼리 "이 과제는 저 과제 다음이다" 를 잇는 기능이다.
표(`dt2_project_dependencies`)와 편집 화면은 **원래부터 있었는데**,
V2 컷오버(2026-07-31) 때 **쓰기 경로만 없어서 화면을 숨겨 뒀다.**
이번에 쓰기 API 를 만들고 화면을 되돌린다.

| 어디 | 무엇 |
|---|---|
| 과제 편집창 ▸ **기타** 탭 맨 위 | **이전 과제 연결** — 이 과제의 앞선 과제를 고른다 |

### ② 액션아이템에 **바뀌지 않는 식별자**(`uuid`) 부여

액션아이템의 `id` 는 정체성이 아니라 **순번**이다 — 화면이 저장할 때마다
위치 순서로 다시 매긴다. 그래서 **3개 중 첫 번째를 지우면 활동 로그에
엉뚱한 항목이 삭제됐다고 기록된다.** 마지막 것을 지울 때만 우연히 맞았다.

`uuid` 를 **더해서** 그걸 고친다. **`id` 는 그대로 둔다.**

**화면에 보이는 변화는 ①뿐이다.** ②는 눈에 안 보인다.
**기존 데이터는 바뀌지 않는다.** 기존 활동 로그도 그대로 둔다.

---

## 0. 시작 전 — 이미 끝나 있어야 하는 것

| | 상태 |
|---|---|
| V2 컷오버 | ✅ 2026-07-31 완료 (`dt2_*` 가 정본) |
| `dt2_project_dependencies` 표 | ✅ V2 이관 때 이미 만들어졌다. **이번에 만들지 않는다** |
| `.env` | ✅ **고칠 것이 하나도 없다** |
| DB 마이그레이션 | ✅ **없다.** `flask db upgrade` 를 하지 않는다 |
| 개발 DB 시험 | ✅ 선행과제 57건 · 액션아이템 uuid 28건 · 회귀 4종 통과 |

> **사전 공지는 필요 없다.** 기존 기능이 바뀌지 않고, 새 화면은 비어 있는 채로 시작한다.

---

## 1. 개발 PC — 반입본 만들기

### 1-1. 프론트엔드 빌드

```
cd frontend
npm run build
```

**확인** — 마지막에 `✓ built in ...` 이 나오고 `dist/` 가 새로 만들어진다.

> ⚠️ **운영에서는 build 를 하지 않는다.** 부분 실패하면 `index.html` 이 없는 CSS 를
> 가리키고 SPA 폴백이 200 으로 돌려줘서 **화면 스타일이 조용히 사라진다.**

### 1-2. 폴더 압축

프로젝트 폴더를 통째로 압축한다(기존 반입 방식과 같다).

> ⚠️ **`.env` 도 같이 들어간다.** 압축 전에 개발 PC 의 `backend/.env` 가 운영 값과
> 다른 부분(`FLASK_ENV`·DB 접속)을 **운영 기준으로 맞춰 둔다.**
> 이번 작업에서 `.env` 에 **새로 추가하거나 바꿀 값은 없다.**

---

## 2. 운영 — 백업 (되돌릴 수 있는 유일한 수단)

```
pg_dump -U postgres -d dxdigitaltwin -F c -f dt_dep_before_20260808.dump
```

**확인** — 파일이 만들어지고 크기가 0 이 아니다.

```
dir dt_dep_before_20260808.dump
```

**막혔을 때** — 백업이 안 되면 **여기서 멈춘다.** 백업 없이 진행하지 않는다.

> 6장이 `action_items_json` 을 **실제로 고쳐 쓴다**(칸 하나 더하기). 되돌리기는
> R-3 한 줄이면 되지만, 그래도 백업 없이 운영 DB 를 건드리지 않는다는 원칙이다.

---

## 3. 운영 — 서비스 내리기

```
net stop <서비스이름>
```

또는 실행 중인 콘솔에서 `Ctrl+C`.

**확인** — 브라우저에서 대시보드가 열리지 않는다.

> **왜 지금 내리나** — 6장의 백필이 도는 동안 사용자가 액션아이템을 저장하면,
> 옛 코드가 `uuid` 없는 배열로 덮어써서 그 과제만 백필이 헛돈다.
> 큰 사고는 아니지만(그 과제는 다음 저장 때 uuid 를 받는다) 굳이 겪을 이유가 없다.

---

## 4. 운영 — 반입

### 4-1. 기존 폴더 이름 바꾸기 (되돌리기용)

`<운영폴더>` 는 지금 서비스가 돌고 있는 폴더 이름이다. 기존 반입 때와 같다.

```
ren <운영폴더> <운영폴더>_before_20260808
```

**확인** — 이름이 바뀐 폴더가 보인다. **지우지 않는다.** R장에서 쓴다.

### 4-2. 압축 풀기

새 압축본을 원래 폴더 이름으로 푼다.

**확인** — 아래 두 파일이 있어야 한다.

```
dir backend\scripts\dt5_verify.py
dir backend\scripts\dt3_backfill_action_uuids.py
```

둘 중 하나라도 없으면 **압축본이 잘못됐다.** 개발 담당자에게 연락한다.

### 4-3. `.env` 확인

```
type backend\.env | findstr /C:"FLASK_ENV" /C:"DT2_WRITE_ENABLED"
```

**확인** — 아래 두 줄이 나와야 한다.

```
FLASK_ENV=production
DT2_WRITE_ENABLED=true
```

`DT2_WRITE_ENABLED` 가 `true` 가 아니면 **선행 과제 저장이 503 으로 실패한다.**
컷오버 때 넣은 값이므로 없으면 압축본에 개발 `.env` 가 들어온 것이다 — 그 줄을 고친다.

> **`flask db upgrade` 는 하지 않는다.** 이번 작업에는 마이그레이션이 없다.
> 실수로 실행해도 "이미 최신" 이라고 나올 뿐이지만, 하지 않는 것이 맞다.

---

## 5. ★ 운영 — 반입 전 점검 (이 작업의 판단 지점)

```
cd backend
python scripts\dt5_verify.py
```

**아무것도 고치지 않는 읽기 전용 스크립트다.** 몇 초면 끝난다.

**확인** — 마지막 줄이 이래야 한다.

```
결과: [OK] N/N — 다음 단계로 넘어가도 됩니다.
```

### 5-1. ★★ 「2. 선행 과제 연결」 블록을 **눈으로** 본다

개발 DB 에는 선행 과제가 2건뿐이라, 아래 둘은 **운영에서만 나올 수 있다.**
`[OK]` 라면 그냥 넘어가면 된다. 나왔을 때만 읽는다.

| 나온 줄 | 뜻 | 어떻게 |
|---|---|---|
| `★ 순환(A→B→A)이 없다` **[FAIL]** | 서로를 선행으로 가리키는 과제가 있다 | **반입은 계속한다.** 9장에서 그 담당자에게 알린다 |
| `★ 끊긴 연결 N건` (정보) | 대상 과제가 이미 지워졌다 | **반입은 계속한다.** 화면이 흐리게 표시하고, 사용자가 지우면 된다 |
| `자기 자신을 가리키는 연결` **[FAIL]** | 데이터가 손상돼 있다 | **멈추고** 개발 담당자에게 연락 |

> **순환이 있으면 무엇이 일어나나**
> 그 과제들은 **선행 연결을 고쳐서 저장할 때** *"선행 과제가 서로를 가리키게 됩니다"*
> 400 을 본다. 다른 저장(과제명·진행률 등)은 아무 영향이 없다.
> 지금도 화면에는 그대로 보이므로 **데이터를 잃는 것이 아니다.**
> 담당자가 한쪽 연결을 지우면 풀린다. 화면에 나온 과제 코드를 종이에 적어 둔다.

### 5-2. 「3. 액션아이템 정체성」의 숫자를 적어 둔다

```
[정보] 액션아이템 NNN건 · uuid 있음 0건 · 없음 NNN건
[정보] id 가 없는 항목 N건
```

지금은 **uuid 있음 0건**이 정상이다. 6장에서 채운다. 8장에서 이 숫자와 맞춰본다.

---

## 6. ★ 운영 — 액션아이템 uuid 채우기

> ### 이 장을 건너뛰면 안 되는 이유
>
> "어차피 저장할 때 서버가 붙여 주지 않나" 싶을 수 있다. **절반만 맞다.**
> 서버는 그 저장에 **액션아이템 목록이 실려 있을 때만** 붙인다. 개발 DB 실측:
>
> | 저장 종류 | uuid 가 붙나 |
> |---|---|
> | 과제명·진행률만 바꾼 저장 | **안 붙는다** |
> | 액션아이템을 바꾼 저장 | 붙는다 |
>
> 그래서 건너뛰면 이렇게 된다.
>
> · **액션아이템을 한 번도 안 건드리는 과제는 영영 uuid 가 없다.** 대부분이 그렇다.
> · **삭제 로그 버그가 「그 과제의 첫 편집 한 번」은 그대로 남는다.**
>   화면은 *새로 만드는* 항목에만 uuid 를 붙인다 — 이미 있던 항목에 소급해 붙이지
>   않는다. 그래서 첫 편집에서는 여전히 순번으로 비교하고, 첫 항목을 지우면
>   **엉뚱한 항목이 삭제됐다고 기록된다.** 두 번째 편집부터 정상이 된다.
> · 사람이 과제를 열어 저장할 때마다 **"액션아이템목록 변경" 이력이 한 줄씩** 남는다.
>   여기서 한 번에 채우면 그 이력이 하나도 안 남는다.

### 6-1. 먼저 미리보기 (아무것도 안 쓴다)

```
python scripts\dt3_backfill_action_uuids.py
```

**확인** — 마지막에 이렇게 나온다.

```
과제 NNN개 · 액션아이템 NNN개
  이미 uuid 있음   0개
  새로 부여        NNN개  (과제 NN개)
[미리보기] 아무것도 쓰지 않았습니다. 반영하려면 --commit 을 붙이세요.
```

**`새로 부여` 숫자가 5-2 에 적은 「없음」 숫자와 같아야 한다.** 다르면 **멈추고** 연락한다.

### 6-2. 반영

```
python scripts\dt3_backfill_action_uuids.py --commit
```

**확인** — 마지막 줄이 이래야 한다.

```
[OK] 반영했습니다. 다시 실행하면 "새로 부여 0개" 가 나와야 합니다.
```

### 6-3. 두 번 돌려서 확인 (멱등성)

```
python scripts\dt3_backfill_action_uuids.py
```

**확인** — `새로 부여 0개` 가 나온다. 안 나오면 **멈추고** 연락한다.

> **이 스크립트가 하지 않는 것** — `row_version` 을 올리지 않고, 변경 이력도 남기지
> 않는다. 사용자가 바꾼 것이 아니라 기술적인 식별자 부여이기 때문이다.
> `id` 도 그대로 둔다. 기존 값은 하나도 안 바꾼다.

---

## 7. 운영 — 반입 후 점검 + 서비스 올리기

```
python scripts\dt5_verify.py
```

**확인** — 「3. 액션아이템 정체성」이 이렇게 바뀐다.

```
[정보] 액션아이템 NNN건 · uuid 있음 NNN건 · 없음 0건
[OK]   ★ uuid 가 겹치지 않는다
[OK]   ★ 전부 uuid 를 가지고 있다 (백필 완료 상태)
```

`uuid 가 겹치지 않는다` 가 **[FAIL]** 이면 **멈추고** R장으로 간다.

```
net start <서비스이름>
```

**확인** — 브라우저에서 대시보드가 열린다.

---

## 8. 운영 — 화면 확인 (눈으로 본다)

여섯 가지를 순서대로 본다. **하나라도 다르면 R장으로 간다.**

### 8-1. 기존 화면이 그대로다

- 간트(`과제 진행 현황`)가 뜬다
- `전체 요약` 의 과제 수가 평소와 같다
- 아무 과제나 열어 **액션아이템 목록이 그대로**인지 본다 (개수·제목·완료 표시)

> 여기까지 정상이면 **6장이 안전했다는 뜻이다.** 아래는 새 기능이다.

### 8-2. 선행 과제 — 화면이 있다

아무 과제를 열고 **`기타`** 탭을 누른다.

- 맨 위에 **이전 과제 연결** 이 보인다
- 탭 이름 옆 숫자에 선행 과제 개수가 더해져 있다

### 8-3. 선행 과제 — 고르고 저장된다

- `이전 과제 추가` → 연도/검색으로 과제를 찾아 체크 → `추가`
- **저장** → 창을 닫았다 다시 연다 → **연결이 남아 있다**
- **F5 로 새로고침한 뒤에도 남아 있다** ← 이것이 핵심이다

> 저장 시 브라우저 콘솔(F12)에 `[DT] V2 선행 과제 연결 저장 — N건` 이 찍힌다.

### 8-4. 선행 과제 — 후보에 지운 과제가 안 나온다

- `이전 과제 추가` 목록에 **휴지통에 넣은 과제가 없다**

### 8-5. 선행 과제 — 순환이 막힌다

- A 과제에 B 를 선행으로 걸고 저장
- B 과제를 열어 A 를 선행으로 걸고 저장 → *"선행 과제가 서로를 가리키게 됩니다(순환)"*
  **정상이다.**

### 8-6. 변경 이력

- 선행 과제를 바꾼 과제의 편집창 ▸ **변경 이력** 탭
- **선행 과제 연결** 이라는 이름으로 한 줄 남아 있다 (영문 컬럼명이 아니다)

---

## 9. 마무리

- 백업 파일(`dt_dep_before_20260808.dump`)을 보관한다
- 이전 폴더(`<운영폴더>_before_20260808`)를 **1주일간 지우지 않는다**
- **5-1 에서 순환이 나왔다면** 그 과제 담당자에게 개별로 알린다

> [개별 안내] `<과제코드>` 과제와 `<과제코드>` 과제가 서로를 이전 과제로
> 가리키고 있습니다. 편집창 ▸ 기타 탭에서 **한쪽 연결을 지워** 주세요.
> 지우기 전에는 그 과제의 이전 과제 연결을 저장할 수 없습니다.

- 아래를 사용자에게 알린다

> **[안내] 디지털 트윈 대시보드 — 이전 과제 연결 기능**
>
> 과제끼리 "이 과제는 어느 과제를 이어받았는가" 를 연결할 수 있게 됐습니다.
>
> · **과제 편집창 ▸ 기타** 탭 맨 위의 `이전 과제 추가` 에서 고르시면 됩니다.
> · 여러 개 걸 수 있고, 연도·과제명·사업부·PL 로 검색됩니다.
> · 서로를 가리키게(A→B, B→A) 는 걸 수 없습니다.
>
> 처음에는 비어 있습니다. 담당하신 과제부터 채워 주시면 됩니다.

---

## R. 되돌리기 (롤백)

### R-0. 먼저 읽을 것

이번 작업은 **표도 칸도 더하지 않는다.** 되돌릴 마이그레이션이 없다.

되돌릴 것이 있다면 딱 둘이다.

| | 무엇 | 되돌리는 법 |
|---|---|---|
| 폴더 | 코드·화면 | R-1 (이름 되돌리기) |
| DB | 액션아이템의 `uuid` 칸 | R-3 (한 줄) — **대개 되돌릴 필요가 없다** |

### R-1. 서비스를 켜기 전 (8장 이전)

**폴더만 되돌리면 된다.** DB 는 그대로 둬도 문제없다 —
액션아이템의 `uuid` 는 **옛 코드가 읽지도 쓰지도 않는다.** 모르는 키는 그냥 실려 다닌다.

```
ren <운영폴더> <운영폴더>_failed
ren <운영폴더>_before_20260808 <운영폴더>
net start <서비스이름>
```

**확인** — 기존 화면이 정상으로 뜬다. `기타` 탭에 `이전 과제 연결` 이 없다(원래 상태다).

### R-2. 서비스를 켠 뒤 (사용자가 저장했을 수 있을 때)

1. 서비스를 내린다
2. R-1 대로 폴더를 되돌린다
3. **DB 는 그대로 둔다.**
   - 저장된 선행 과제 연결은 옛 코드가 **읽기만** 하므로 화면에 영향이 없고,
     나중에 다시 반입하면 **그대로 살아난다**
   - 액션아이템의 `uuid` 도 옛 코드가 무시한다

**이 경우 DB 를 되돌리지 않는 것이 낫다.** 되돌리면 그 사이 저장된 연결이 사라진다.

### R-3. 액션아이템 `uuid` 까지 지워야 할 때

거의 쓸 일이 없다. 7장에서 `uuid 가 겹친다` 가 나왔을 때만 쓴다.

```
psql -U postgres -d dxdigitaltwin -c "UPDATE dt2_projects SET action_items_json = (SELECT jsonb_agg(item - 'uuid') FROM jsonb_array_elements(action_items_json) AS item) WHERE jsonb_typeof(action_items_json) = 'array' AND jsonb_array_length(action_items_json) > 0;"
```

**확인**

```
python scripts\dt5_verify.py
```

「3. 액션아이템 정체성」이 `uuid 있음 0건` 으로 돌아온다.

> ⚠️ 이 한 줄은 **`uuid` 키만** 지운다. 제목·완료여부·세부항목은 건드리지 않는다.
> 그래도 실행 전에 2장의 백업이 있는지 확인한다.

### R-4. 선행 과제 연결을 지워야 할 때

사용자가 잘못 건 연결이 많아 통째로 되돌리고 싶을 때만.
**연결만** 지운다 — 과제는 안 건드린다.

```
psql -U postgres -d dxdigitaltwin -c "DELETE FROM dt2_project_dependencies;"
```

> ⚠️ **반입 전부터 있던 연결까지 같이 지워진다.** 5장에서 `선행 과제 연결 N건` 이
> 0 이 아니었다면 이 명령을 쓰지 말고 개발 담당자에게 연락한다.

### R-5. 최후 — 백업에서 복원

위 어느 것으로도 안 되면 2장의 백업으로 되돌린다.

```
pg_restore -U postgres -d dxdigitaltwin -c dt_dep_before_20260808.dump
```

**이건 반입 이후의 모든 변경을 잃는다.** 다른 방법을 먼저 시도한 뒤에 쓴다.

---

## 부록 — 자주 틀리는 것

| 증상 | 원인 | 어떻게 |
|---|---|---|
| `기타` 탭에 `이전 과제 연결` 이 없다 | `dist` 가 옛 것이다 | 1-1 부터 다시 |
| 저장은 되는데 새로고침하면 사라진다 | 옛 `dist` 라 V1 로 저장됐다 | 1-1 부터 다시 |
| 저장 시 *"V2 쓰기가 켜져 있어야"* | `.env` 의 `DT2_WRITE_ENABLED` | 4-3 |
| *"선행 과제가 서로를 가리키게 됩니다"* | 순환이다 | **정상이다.** 한쪽을 지운다 |
| *"이미 없어진 과제를 가리키는 연결이 N건"* | 대상 과제가 지워졌다 | **정상이다.** 흐린 줄을 지우고 저장 |
| *"볼 수 없는 과제는 선행으로 지정할 수 없습니다"* | 사업부 내 공개 과제다 | **정상이다.** 볼 수 있는 과제만 걸 수 있다 |
| 후보 목록에 지운 과제가 나온다 | `dist` 가 옛 것이다 | 1-1 부터 다시 |
| 백필이 `새로 부여 0개` 로 나온다 | 이미 돌렸다 | **정상이다.** 6-3 과 같은 상태다 |
| 6장을 건너뛰어도 되나 | — | **안 된다.** 6장 머리말 참조 |
| AI 에게 시켰더니 403 | AI 는 선행 과제를 못 바꾼다 | **정상이다.** 사람이 편집창에서 건다 |
