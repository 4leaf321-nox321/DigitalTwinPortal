# 운영서버 런북 1-1 — 자동 스냅샷 중단 및 누적분 정리

> 대상: **운영서버에서 직접 실행** (AI 없음 · 인터넷 없음)
> 근거: [디지털트윈_대시보드_실행계획.md](./디지털트윈_대시보드_실행계획.md) Phase 1-1
> 선행: **[런북 0-0 백업](./디지털트윈_운영런북_0-0_백업.md) 이 완료되어 있어야 한다** (특히 단계 2 스냅샷 백업)
> 소요: **약 30분 ~ 1시간**
> 서비스 영향: **배포 시 재시작 수 초** + **VACUUM 중 스냅샷 화면만 잠시 대기.** 일상 저장에는 영향 없음

---

## 무엇을 왜 하는가

**문제** — 저장할 때마다 전체 데이터(운영 약 37 MB)의 복사본이 스냅샷으로 한 건씩 쌓이고 있었다.

| 운영 실측 (2026-07-28) | 값 |
|---|---|
| 스냅샷 건수 | **4,896건** |
| 논리 크기 | **28.8 GB** |
| 디스크 실사용 | **25.2 GB** — DB 최대 용량 소비원 |
| 증가 속도 | **저장 1회당 약 37 MB** (지금도 계속) |

**조치 두 가지**

1. **코드**: 업서트(일상 저장) 시 자동 스냅샷 생성을 **중단** → 더 이상 쌓이지 않는다
2. **데이터**: 이미 쌓인 것을 보존 정책에 맞게 **정리** → 디스크 회수

**보존 규칙 — 아래 중 하나라도 해당하면 남긴다**

```
1. 타입이 manual / upload      사람이 만든 스냅샷 + 전체 덮어쓰기 직전 백업
2. 최근 7일 이내 생성           타입 무관
3. 자동 생성분 중 최신 30건
```

> **수동 스냅샷 기능은 그대로 동작한다.** 전체 덮어쓰기(`/data/upload`) 직전 자동 백업도 유지된다.
> 없어지는 것은 "일상 저장마다 뜨던 백업"뿐이다.

---

## 이 런북을 쓰는 법

- 명령은 **그대로 복사해서 붙여넣는다.**
- 각 단계의 **"이 결과가 나와야 정상"** 과 다르면 **거기서 멈춘다.**
- **판단하지 않는다.** 예상과 다르면 화면을 캡처하고 담당자에게 전달한다.

---

# 준비

## P-1. 변수 설정 — **한 번만 수정**

아래에서 **경로와 비밀번호만** 실제 값으로 바꾼 뒤 실행한다.

```powershell
$PROJ   = "C:\경로를_여기에\52_KnowledgeGraphProject"
$BACKUP = "D:\dt_backup"
$PGBIN  = "C:\Program Files\PostgreSQL\17\bin"
$DB     = "dxdigitaltwin"
$env:PGPASSWORD = "여기에_비밀번호"
```

## P-2. 백업이 있는지 확인 ★ 없으면 진행 금지

```powershell
Get-ChildItem $BACKUP -Filter "snapshots_*.dump" | Sort-Object LastWriteTime -Descending |
  Select-Object -First 3 Name, @{n='GB';e={[math]::Round($_.Length/1GB,2)}}, LastWriteTime
```

**기대 출력** — 최근 스냅샷 백업 파일이 보여야 한다.

```
Name                          GB    LastWriteTime
----                          --    -------------
snapshots_20260728_1830.dump  2.10  2026-07-28 오후 7:05:12
```

**실패 시** — 파일이 없으면 **[런북 0-0](./디지털트윈_운영런북_0-0_백업.md) 단계 2를 먼저 수행**한다.

가장 최근 파일 경로를 변수에 담는다.

```powershell
$SNAPBK = (Get-ChildItem $BACKUP -Filter "snapshots_*.dump" | Sort-Object LastWriteTime -Descending | Select-Object -First 1).FullName
Write-Host "사용할 백업: $SNAPBK"
```

---

# 단계 0. 현재 상태 기록

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT snapshot_type, count(*), pg_size_pretty(sum(length(snapshot_data::text))::bigint) AS 논리 FROM dashboard_snapshots GROUP BY 1 ORDER BY 2 DESC;"
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT pg_size_pretty(pg_total_relation_size('dashboard_snapshots')) AS 디스크, pg_size_pretty(pg_database_size(current_database())) AS db전체;"
```

**기대 출력** (숫자는 환경마다 다름 — **화면 숫자를 적어둔다**)

```
 snapshot_type | count |  논리
---------------+-------+---------
 auto          |  48XX | 28 GB
 upsert        |    NN | ...
 manual        |    NN | ...
 upload        |    NN | ...

  디스크  | db전체
----------+--------
  25 GB   | 26 GB
```

---

# 단계 1. 코드 배포 (자동 스냅샷 중단) ★ 정리보다 먼저

> **순서가 중요하다.** 코드를 먼저 배포해야 정리하는 동안 새로 쌓이지 않는다.

## 1-1. 백엔드 파일 교체

담당자가 전달한 `routes.py` 를 아래 위치에 덮어쓴다.

```
<프로젝트>\backend\app\modules\digital_twin_dashboard\routes.py
```

교체 전 기존 파일을 보관한다.

```powershell
Copy-Item "$PROJ\backend\app\modules\digital_twin_dashboard\routes.py" "$BACKUP\routes.py.before_1-1" -Force
Write-Host "기존 파일 보관 완료"
```

## 1-2. 변경이 반영됐는지 확인

```powershell
Select-String -Path "$PROJ\backend\app\modules\digital_twin_dashboard\routes.py" -Pattern "Phase 1-1", "_prune_auto_snapshots" | Select-Object LineNumber, Line
```

**기대 출력** — 최소 3줄 이상 나와야 한다 (업서트 중단 주석 + 정리 함수 정의 + 호출).

```
LineNumber Line
---------- ----
       672 # 스냅샷 보존 정책 (Phase 1-1)
       681 def _prune_auto_snapshots(keep_count=...
       ...
      1140 # [Phase 1-1] 업서트 시 자동 스냅샷 생성 중단
```

**실패 시** — 파일이 제대로 교체되지 않았다. 담당자에게 알린다.

## 1-3. 서비스 재시작

운영에서 백엔드를 기동하는 방식대로 재시작한다 (서비스/작업스케줄러/배치 파일).

재시작 후 정상 응답을 확인한다.

```powershell
Invoke-WebRequest -Uri "http://localhost:5174/api/digital-twin-dashboard/health" -UseBasicParsing | Select-Object StatusCode
```

**기대 출력**

```
StatusCode
----------
       200
```

**실패 시** — 백엔드가 안 떴다. 콘솔 로그를 확인하고, 해결이 안 되면 `$BACKUP\routes.py.before_1-1` 을 되돌린 뒤 재시작한다.

## 1-4. 자동 스냅샷이 실제로 멈췄는지 확인 ★

> ⚠️ **"스냅샷 0" 만 보면 안 된다.** 저장 자체가 실패해도 0 이 나온다.
> **저장이 실제로 일어났다는 증거(버전 증가)와 함께** 확인한다.

**① 저장 전 — 현재 버전 기록**

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT version AS 저장전_버전 FROM dashboard_data;"
```

**② 대시보드에서 과제 하나를 열어 저장한다**

**③ 저장 후 — 확인**

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT (SELECT version FROM dashboard_data) AS 저장후_버전, (SELECT count(*) FROM dashboard_snapshots WHERE created_at > (now() AT TIME ZONE 'UTC') - interval '10 minutes') AS 최근10분_스냅샷;"
```

**정상 — 둘 다 만족해야 한다**

| 값 | 정상 |
|---|---|
| `저장후_버전` | ①보다 **증가** |
| `최근10분_스냅샷` | **0** |

- 버전 증가 + 스냅샷 0 → **[OK]** 단계 2로 진행
- 버전 증가 + 스냅샷 1 이상 → 코드 미반영. **단계 1-2로 돌아간다**
- **버전 그대로** → 저장이 안 된 것이다. 스냅샷 0 은 의미 없다. ②부터 다시 한다

> `created_at` 은 **UTC** 로 저장되므로 `now()`(KST) 와 그냥 비교하면 9시간이 어긋나 **항상 0** 이 나온다.
> `now() AT TIME ZONE 'UTC'` 가 그것을 맞춘 것이다. (2026-07-28 검증에서 발견)

---

# 단계 2. 정리 대상 확인 (아무것도 지우지 않음)

```powershell
cd "$PROJ\backend"
.\venv\Scripts\activate
python scripts\dt_snapshot_cleanup.py
```

**기대 출력**

```
 모드 : 확인만 (dry-run)
 정책 : 자동 생성분 최신 30건 보존 / 최근 7일 보존 / manual·upload 전량 보존

── 정리 전 현황 ──
  타입                건수         논리 크기
  auto            48XX        28.0 GB
  ...
  디스크 실사용: 25.2 GB

── 정리 계획 ──
  **삭제 대상**              : 4,8XX건 / 논리 28.0 GB
  정리 후 예상 건수           : 40건 내외

 확인만 했습니다. 아무것도 삭제하지 않았습니다.
```

**확인할 것**
- `정리 후 예상 건수` 가 **0이 아니어야 한다** (0이면 전부 지우는 것이므로 중단)
- `manual` / `upload` 건수는 **정리 후에도 그대로** 남는다

**실패 시** — 오류가 나면 중단한다. 아직 아무것도 바뀌지 않았다.

---

# 단계 3. 실제 삭제

```powershell
python scripts\dt_snapshot_cleanup.py --commit --backup-file "$SNAPBK"
```

**기대 출력**

```
[OK]   백업 파일 확인: snapshots_20260728_1830.dump / 2.1 GB / ...

── 삭제 실행 (4,8XX건, 200건씩) ──
  200 / 4,8XX 건 삭제
  400 / 4,8XX 건 삭제
  ...
[OK]   커밋 완료 — 4,8XX건 삭제

── 결과 ──
  건수      : 4,896 → 40  (-4,856)
  논리 크기 : 28.8 GB → 200 MB
  디스크    : 25.2 GB → 25.2 GB

[주의] 디스크 사용량이 아직 줄지 않았습니다. **정상입니다.**
```

> **디스크가 안 줄어드는 것이 정상이다.** PostgreSQL 은 DELETE 만으로 파일을 반환하지 않는다.
> 실제 반환은 단계 4에서 한다.

**실패 시** — 스크립트가 **자동으로 롤백**한다. `[FAIL]` 이 뜨면 데이터는 변경되지 않은 상태다.
`scripts\out\dt_snapshot_cleanup_*.log` 를 담당자에게 전달한다.

---

# 단계 4. 디스크 실제 반환 (VACUUM FULL)

> ⚠️ **이 단계는 `dashboard_snapshots` 테이블을 잠근다.**
> 그동안 **스냅샷 목록 화면만** 잠시 멈춘다. 과제 저장·조회 등 일상 작업에는 영향이 없다
> (단계 1에서 자동 스냅샷 생성을 이미 끊었기 때문).
> 사용자가 적은 시간대를 권장하지만, 필수는 아니다.

```powershell
python scripts\dt_snapshot_cleanup.py --vacuum-full --backup-file "$SNAPBK"
```

**기대 출력**

```
[VACUUM FULL] 시작 — 이 동안 dashboard_snapshots 접근이 대기합니다.
[OK]   VACUUM FULL 완료 (NNN초)
  VACUUM 후 디스크: 200 MB 내외

 결과: [OK] 완료
```

**소요** — 데이터가 크므로 **수 분** 걸릴 수 있다. 화면이 멈춘 것처럼 보여도 **기다린다.**

**실패 시** — 디스크 여유 부족이 가장 흔한 원인이다. 남은 데이터 크기만큼 여유가 필요하다.

---

# 단계 5. 최종 확인

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT snapshot_type, count(*) FROM dashboard_snapshots GROUP BY 1 ORDER BY 2 DESC;"
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT pg_size_pretty(pg_total_relation_size('dashboard_snapshots')) AS 스냅샷디스크, pg_size_pretty(pg_database_size(current_database())) AS db전체;"
```

**기대 출력** — 단계 0과 비교한다.

| 항목 | 단계 0 | 지금 | 판정 |
|---|---|---|---|
| `auto` 건수 | 4,8XX | **30 내외** | 줄어야 정상 |
| `manual` 건수 | NN | **단계 0과 동일** | 줄면 이상 → 담당자 연락 |
| `upload` 건수 | NN | **단계 0과 동일** | 줄면 이상 → 담당자 연락 |
| 스냅샷 디스크 | 25 GB | **수백 MB** | 크게 줄어야 정상 |

## 5-1. 화면 동작 확인

1. 대시보드에서 **스냅샷 목록**을 연다 → 최근 것들이 정상적으로 보인다
2. **수동 스냅샷을 하나 만들어 본다** → 정상 생성된다
3. 과제 하나를 **저장해 본다** → 정상 저장되고, **스냅샷은 늘지 않는다**

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT count(*) AS 최근10분_생성 FROM dashboard_snapshots WHERE created_at > (now() AT TIME ZONE 'UTC') - interval '10 minutes';"
```

**기대 출력** — 수동 스냅샷을 만들었다면 **1**, 안 만들었다면 **0**.
과제 저장만으로 늘어나면 안 된다.

> `created_at` 은 UTC 로 저장되므로 `now()`(KST) 와 직접 비교하면 항상 0 이 나온다. 위 쿼리는 그것을 맞춘 것이다.

---

# 완료 체크리스트

```
□ P-2   스냅샷 백업 파일 확인 (런북 0-0 단계 2 완료)
□ 0     정리 전 건수·용량 기록
□ 1-1   routes.py 교체, 기존 파일 보관
□ 1-2   Phase 1-1 변경 반영 확인
□ 1-3   재시작 후 health 200
□ 1-4   과제 저장해도 스냅샷 안 늘어남 ★
□ 2     dry-run 으로 정리 대상 확인 (정리 후 건수 > 0)
□ 3     --commit 실행, [OK] 커밋 완료
□ 4     --vacuum-full 실행, 디스크 감소 확인
□ 5     manual/upload 건수 유지, 디스크 크게 감소
□ 5-1   스냅샷 목록 조회 / 수동 스냅샷 생성 / 과제 저장 정상
```

**11개 항목이 모두 체크되면 Phase 1-1 완료.**

---

# 되돌리기

## 코드만 되돌리기 (자동 스냅샷을 다시 켜고 싶을 때)

```powershell
Copy-Item "$BACKUP\routes.py.before_1-1" "$PROJ\backend\app\modules\digital_twin_dashboard\routes.py" -Force
```
이후 서비스 재시작.

## 삭제한 스냅샷 되돌리기

단계 3 이후에는 **삭제된 스냅샷을 되살리려면 백업에서 복원**해야 한다.
운영에 바로 되돌리는 것은 위험하므로, **먼저 staging 에 복원해 필요한 스냅샷만 확인**한다.

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d postgres -c "DROP DATABASE IF EXISTS dxdigitaltwin_stg;"
& "$PGBIN\psql.exe" -U postgres -h localhost -d postgres -c "CREATE DATABASE dxdigitaltwin_stg;"
& "$PGBIN\pg_restore.exe" -h localhost -U postgres -d dxdigitaltwin_stg --no-owner --no-privileges "$SNAPBK"
```

그 뒤 담당자와 함께 필요한 스냅샷만 골라 옮긴다. **혼자 판단해서 운영에 직접 복원하지 않는다.**

---

# 문제가 생겼을 때

1. **더 이상 아무 명령도 실행하지 않는다**
2. `scripts\out\` 폴더의 최신 `dt_snapshot_cleanup_*.log` 를 확보한다
3. 화면 전체를 캡처한다
4. 담당자에게 전달한다

> 단계 3의 삭제는 **단일 트랜잭션**이다. 중간에 실패하면 자동으로 전부 되돌려진다.
> "일부만 지워진" 어중간한 상태는 생기지 않는다.

---

# 자주 나오는 상황

| 상황 | 답 |
|---|---|
| 단계 3에서 디스크가 안 줄었다 | **정상.** PostgreSQL 은 DELETE 로 파일을 반환하지 않는다. 단계 4에서 줄어든다 |
| VACUUM FULL 이 몇 분째 멈춰 있다 | **정상.** 데이터가 크다. 종료 메시지가 나올 때까지 기다린다 |
| VACUUM 중에 사용자가 저장해도 되나 | **된다.** 잠기는 것은 스냅샷 테이블뿐이다 |
| 백업 파일이 없다고 거부당한다 | 의도된 안전장치다. 런북 0-0 단계 2를 먼저 수행한다 |
| `upsert` 타입이 통째로 사라졌다 | 자동 생성분이라 정책상 정리 대상이다. `manual`/`upload` 가 남았으면 정상 |
| 정리 후에도 스냅샷이 계속 늘어난다 | 코드가 반영되지 않았다. 단계 1-2·1-4를 다시 확인한다 |
| 보존 개수를 바꾸고 싶다 | `--keep 50 --keep-days 14` 처럼 지정. 기본은 30건/7일 |

---

# 이 런북의 검증 이력

개발 환경(PostgreSQL 17.5)에서 **운영 데이터 복제본을 만들어 전 과정을 실제 실행함** (2026-07-28).

| 항목 | 결과 |
|---|---|
| dry-run | 358건 중 삭제 대상 318건 정확히 산출, 데이터 무변경 |
| 안전장치 A | `--commit` 인데 `--backup-file` 없음 → **거부** (exit 1) |
| 안전장치 B | 존재하지 않는 백업 파일 → **거부** |
| 안전장치 C | 1 MB 미만 가짜 백업 파일 → **거부** |
| 실제 삭제 | 358 → **40건** (auto 337→30, manual 6, upload 4 **전량 보존**) |
| 논리 크기 | 159.8 MB → **33.5 MB** |
| 디스크 (DELETE 직후) | 41.2 MB → **41.2 MB** (안 줄어듦 — 예상대로) |
| 디스크 (VACUUM FULL 후) | → **10.4 MB** |
| 원본 DB | **358건 그대로 — 복제본에서만 작업** |

> 운영은 데이터가 훨씬 커서 **소요 시간만 다르고 절차·판정 기준은 동일**하다.
