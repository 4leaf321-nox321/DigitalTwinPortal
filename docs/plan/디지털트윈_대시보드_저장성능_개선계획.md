# 디지털 트윈 대시보드 — 저장/불러오기 성능 개선 계획

> 작성일: 2026-07-28 · 대상: `backend/app/modules/digital_twin_dashboard/`, `frontend/src/modules/digital-twin-dashboard/`
> 계측 대상 DB: `postgresql://postgres@localhost:5432/dxdigitaltwin` (v571, 최종 수정 2026-07-28 05:17)
> 관련 문서: [디지털트윈_대시보드_정규화_검토.md](./디지털트윈_대시보드_정규화_검토.md) (2026-07-04, 구조 정규화 검토)

---

## 1. 한눈에 보는 결론

- **범인 1순위는 정규화 부재가 아니라 "본문에 박힌 base64 이미지"다.** 과제 보고서 이미지가
  `data:image/jpeg;base64,...` 문자열로 과제 JSON 안에 저장되고 있고, **단 3개 과제가 전체 저장 payload의 55%**를 차지한다.
- 저장 1회 = 전 과제(328) + 전 성과(432)를 **1.21 MB 통째로 업로드** → 서버가 **같은 크기를 자동 스냅샷으로 한 벌 더 INSERT** →
  저장 직후 프론트가 **1.21 MB를 다시 전부 다운로드**. **편집 1건당 왕복 ~2.5 MB.**
- **DB는 병목이 아니다.** 싱글톤 UPDATE + 스냅샷 INSERT 실측 **17 ms**. 병목은 **payload 크기(네트워크·직렬화)와 쓰기 증폭**이다.
- 이미지 기능이 확산되면 **선형으로 악화**된다. 과제 30건만 이미지를 채워도 회당 10 MB를 넘긴다. 지금이 손볼 시점.
- **가장 싼 해법부터 하면 정규화(V2) 없이도 대부분 해결된다.** 1단계(반나절~1일)만으로 편집 1건당 전송량이 **~2.5 MB → ~250 KB**,
  3단계까지 가면 **~10 KB** 수준.

---

## 2. 현황 측정치 (2026-07-28)

### 2.1 저장 데이터

| 항목 | 값 |
|---|---|
| `dashboard_data` 행 수 / version | 1행 (싱글톤) / **v571** |
| 과제 | **328개** (활성 224 / 삭제 104) |
| 성과 | **432개** |
| `projects` JSON 텍스트 | **909,793 B (888 kB)** |
| `performances` JSON 텍스트 | **332,426 B (325 kB)** |
| **저장/조회 1회당 JSON 크기** | **≈ 1.21 MB** (압축 없음) |

### 2.2 payload가 어디에 쓰이고 있나 — 과제 JSON 키별 용량 Top

| 키 | 전체 합계 | 최대 1건 |
|---|---|---|
| **`이미지_좌측`** | **134 kB** | 134 kB |
| **`이미지_상세내용그림`** | **134 kB** | 89 kB |
| **`이미지_개요그림`** | **133 kB** | 45 kB |
| **`이미지_우측`** | **89 kB** | 89 kB |
| `성과목록` | 42 kB | 1.5 kB |
| `액션아이템목록` | 23 kB | 5 kB |
| `과제구분` | 22 kB | 78 B |
| (이하 전부 10 kB 미만) | | |

- **이미지 4개 키 합계 ≈ 490 kB = `projects` 888 kB의 55%.**
- 과제 1건 평균 **2,772 B**, 최대 **378,740 B** → **136배 편차**.

| 과제 | 크기 | 비고 |
|---|---|---|
| MX-29 디지털 트윈 과제 관리 플랫폼 개발 | **370 kB** | 이미지 4그룹 |
| MX-2 전각도 낙하 AI 기반 Simulation | 97 kB | 이미지 2그룹 |
| MX-32 test | 49 kB | 이미지 1그룹 |
| 나머지 325개 | 평균 ~1.4 kB | |

### 2.3 그 외 낭비 요소

| 항목 | 값 | 의미 |
|---|---|---|
| 삭제된 과제 104건 | **136 kB** | `GET /data`는 `_deleted` 과제를 걸러내지 않음 → 매번 전송 |
| `dashboard_snapshots` | **358행 / JSON 텍스트 합계 160 MB / 디스크 41 MB** | **DB 최대 용량 소비원.** auto 337건 |
| 저장 텍스트 인코딩 | 한글이 `\uXXXX`로 escape 되어 저장 | 한글 1자당 **3B → 6B** (2배) |
| HTTP 응답 압축 | **없음** (Flask-Compress 미설치, Waitress 기본 무압축) | 1.21 MB가 그대로 전송 |
| `project_attachments` (디스크 첨부파일) | **1건 / 7.5 kB** | ✅ 이쪽은 문제 없음 |

> **"첨부파일 때문인 것 같다"는 감은 맞았지만 대상이 다르다.** 진짜 첨부파일(`project_attachments`)은 디스크에 저장되고
> 메타데이터만 DB에 있어 무해하다. 문제는 **과제 상세정보 모달의 "보고서 이미지"**로, 이건 첨부파일 API를 안 타고
> 과제 JSON 본문에 base64로 인라인된다.

### 2.4 실측 — DB는 병목이 아님

```
BEGIN; UPDATE dashboard_data SET projects=projects, performances=performances;
       INSERT INTO dashboard_snapshots (... 전체 JSON ...); ROLLBACK;
→ Time: 17.179 ms
```

`dashboard_data`의 TOAST 테이블은 448 kB, autovacuum 110회 정상 수행 중, dead tuple 0.
**즉 "DB가 느려서"가 아니라 "매번 1.21 MB를 만들고, 옮기고, 두 번 쓰기" 때문이다.**

---

## 3. 저장 1회에 실제로 일어나는 일

과제 하나의 진행률만 바꿔도 아래가 전부 발생한다.
(`DigitalTwinDashboardApp.jsx:810-826` → `routes.py:818-1229`)

```
[1] POST /data/upsert          업로드 1.21 MB   ← 328개 전 과제 + 432개 전 성과
[2] 서버: JSON 파싱                             (1.21 MB 역직렬화)
[3] 서버: 기존 싱글톤 로드                       (1.21 MB detoast + 파싱)
[4] 서버: uuid 병합 (380줄 로직)
[5] 서버: 자동 스냅샷 INSERT   쓰기 1.21 MB   ← routes.py:1087  매 저장마다 전체 복사본
[6] 서버: 싱글톤 UPDATE        쓰기 1.21 MB
[7] 서버: 활동로그 INSERT + COMMIT
[8] GET /data/download         다운로드 1.21 MB ← executeServerDownload({silent:true})
[9] 서버: 다운로드 활동로그 INSERT + COMMIT      ← 조회인데 쓰기가 발생
[10] 프론트: setProjects → 전체 리렌더 + IndexedDB 전체 재기록
```

- **네트워크 왕복 ≈ 2.5 MB** (압축 없음)
- **서버 디스크 쓰기 ≈ 2.4 MB**
- 사내망이면 체감이 덜하지만, VPN·원격·모바일에서는 이게 그대로 대기시간이 된다.

### 향후 전망 (조치 안 할 경우)

보고서 이미지는 **슬롯 5종 × 각 3장 = 과제당 최대 15장**이고 **리사이즈·압축·용량 제한이 전혀 없다**
(`DetailInfoModal.jsx:663-670`, `reader.readAsDataURL(file)` 원본 그대로).

| 이미지 입력된 과제 수 | 저장 1회 payload | 편집 1건당 왕복 |
|---|---|---|
| 3건 (현재) | 1.2 MB | 2.5 MB |
| 30건 | ~11 MB | ~22 MB |
| 100건 | ~36 MB | ~72 MB |

스마트폰 사진 1장(3 MB)을 올리면 base64로 **4 MB**가 과제 JSON에 박히고, 그게 매 저장마다 왕복하고 매 스냅샷에 복사된다.

---

## 4. 개선안 — 효과/노력 순

### 0단계. 진단 확정 (30분, 코드 변경 없음)

`routes.py:827` 부터 이미 **타이밍 계측 코드가 들어가 있다.** 운영서버 콘솔에서 아래 로그를 확인하면
"네트워크 vs 서버 처리" 중 어디가 아픈지 즉시 확정된다.

```
[Upsert timing] N_proj=328 N_perf=432 payload=1270000B | parse=..ms load=..ms merge=..ms
                snapshot_write=..ms singleton_write=..ms commit_rest=..ms total=..ms
```

- `total`이 작은데 사용자 체감이 느리다 → **네트워크 전송량 문제** → A·C·D·F가 답
- `snapshot_write`가 크다 → **B**가 답
- 운영 DB 현황은 [부록 A](#부록-a-운영-db-현황-확인-쿼리) 쿼리로 동일하게 스캔할 것 (본 문서 수치는 로컬 DB 기준)

---

### 1단계. 즉효 처방 (반나절~1일, 구조 변경 없음) ⭐ 먼저 하기

#### A. 보고서 이미지 리사이즈·압축 — **payload 55% → 5% 이하**

`DetailInfoModal.jsx`의 `triggerImageUpload`에서 `readAsDataURL` 원본 저장 대신 canvas로 축소 후 JPEG 인코딩.

```
현재:  reader.readAsDataURL(file)              // 원본 그대로 → 3 MB 사진 = 4 MB base64
개선:  canvas 리사이즈(최대 1600px) + toDataURL('image/jpeg', 0.75)
       → 보고서 표시 용도로 충분한 화질에서 통상 10~20배 감소 (134 kB → 8~15 kB)
```

- 업로드 시점 **원본 용량 상한**(예: 10 MB 초과 거부)도 함께 추가.
- 기존 3건은 [3단계 H]의 이관 스크립트에서 같이 처리하거나, 해당 과제를 한 번 열어 재저장.
- **노력: 낮음 / 효과: 최상 / 위험: 낮음(신규 업로드에만 적용)**

#### B. 자동 스냅샷 정책 변경 + 기존 정리 — **저장당 쓰기 절반, 41 MB 즉시 회수**

- `routes.py:1087`의 upsert 자동 스냅샷을 **제거하거나 조건부로** 변경.
  - 권장: **덮어쓰기(`/data/upload`)에만 자동 스냅샷 유지**, 일상 저장(`/data/upsert`)에서는 제거.
  - 보수적 대안: "직전 스냅샷이 N분 이내면 생략" 또는 "하루 첫 저장만".
- **보존 개수 제한**(예: auto 최근 30개)을 두고, 358개 중 초과분 일괄 삭제 → **디스크 41 MB, 논리 160 MB 회수**.
- **노력: 낮음 / 효과: 큼 / 위험: 낮음** (스냅샷은 백업 용도이고 별도 수동 스냅샷/DB 덤프가 존재)

#### C. 응답 gzip 압축 + 한글 escape 해제 — **전송량 1/8~1/10**

```python
# app/__init__.py
from flask_compress import Compress
Compress(app)              # 또는 리버스 프록시(nginx/IIS)에서 gzip on
app.json.ensure_ascii = False   # 한글 \uXXXX(6B) → UTF-8(3B)
```

- 한글 JSON은 gzip이 매우 잘 듣는다. **단, base64 이미지는 이미 압축된 JPEG라 gzip이 안 먹는다 → A를 먼저 해야 효과가 난다.**
- `ensure_ascii=False`는 응답뿐 아니라 DB 저장 텍스트에도 적용하려면 SQLAlchemy `json_serializer` 지정이 필요(2단계에서).
- **노력: 매우 낮음(설정 수준) / 효과: 큼 / 위험: 낮음**

#### D. 저장 직후 전체 재다운로드 제거 — **왕복 1.21 MB 절감**

`DigitalTwinDashboardApp.jsx`의 5곳(`:629, :826, :1271, :1391, :1551`)에서 저장 성공 직후
`await executeServerDownload({ silent: true, preserveMetadata: true })`를 호출한다.

- upsert 응답이 이미 `version`과 통계를 돌려주므로, **버전만 갱신하면 되고 전체 재다운로드는 불필요**하다.
- 부수 효과: `/data/download`가 매번 활동로그를 INSERT하는 것(조회인데 쓰기)도 함께 사라짐.
- 다중 사용자 환경에서 "남의 변경 반영"이 목적이었다면 → 주기적 폴링이나 명시적 "새로고침" 버튼으로 분리.
- **노력: 낮음 / 효과: 큼 / 위험: 중간(동기화 UX 확인 필요)**

---

### 2단계. 부분 저장으로 전환 (2~3일)

#### E. `metadata` 덮어쓰기 버그 수정 — **F의 선행 조건 (지금도 살아있는 버그)**

`routes.py:1115`가 `server_data.data_metadata = metadata`를 **무조건** 실행한다.
그런데 `DigitalTwinDashboardApp.jsx:1542`, `:1637`은 이미 `metadata` 없이 부분 upsert를 호출한다
→ 이 경로로 저장할 때마다 **서버 `data_metadata`가 `{}`로 초기화된다.**

```python
# 수정: 요청에 metadata가 있을 때만 반영
if 'metadata' in req_data:
    server_data.data_metadata = metadata
```

`projects`/`performances`도 동일하게 "키가 없으면 건드리지 않음"으로 정리.

#### F. 변경된 과제만 전송 — **업로드 1.21 MB → ~3 KB**

**백엔드는 이미 준비되어 있다.** `/data/upsert`는 uuid 기준 병합이라 **일부만 보내도 나머지는 그대로 보존**된다.
실제로 일부 경로(`:1542`, `:1637`)는 이미 변경분만 보내고 있다.

- `handleUpdateProjectAndUpload`(`:810`) 등 전체 배열을 보내는 경로를 **변경된 과제만** 보내도록 통일.
- 성과도 동일 (`performances: globalPerformances` → 변경분만).
- 삭제는 이미 별도 엔드포인트가 있으므로 영향 없음.
- **노력: 중간 / 효과: 최상 / 위험: 중간** — E 선행 필수, 그리고 "보내지 않은 항목은 서버가 유지한다"는 계약을 테스트로 고정할 것.

#### G. `GET /data`에서 삭제 과제 제외 — **payload 15% 감소**

`routes.py:654`는 `_permanentlyDeleted`만 거르고 `_deleted`(휴지통) 104건은 그대로 내려보낸다.

- 기본 조회에서 제외하고, 휴지통 화면에서만 `?includeDeleted=true`로 별도 조회.
- **노력: 낮음 / 효과: 중간 / 위험: 낮음**(휴지통·복구 UI 확인 필요)

---

### 3단계. 이미지를 구조적으로 분리 (2~3일) — 근본 해결

#### H. 보고서 이미지 → 파일 저장 + JSON엔 참조만

A(압축)는 완화책이고, 이게 근본이다. **이미 검증된 `ProjectAttachment` 패턴을 그대로 재사용**하면 된다.

```
현재:  과제JSON.이미지_좌측 = [{ dataUrl: "data:image/jpeg;base64,...(134kB)", caption, fileName }]
개선:  과제JSON.이미지_좌측 = [{ imageId: 123, caption, fileName }]          ← ~80 B
       신규 테이블/재사용: project_report_images (또는 ProjectAttachment에 category='report')
       파일 실체: backend/uploads/digital-twin-dashboard/  (기존과 동일)
       조회: GET /report-images/<id>  (보고서 화면에서 lazy load)
```

- 효과: 저장 payload에서 **이미지가 완전히 사라진다.** 이미지 100장을 넣어도 저장 속도는 그대로.
- 이관: 현재 3개 과제 7개 키 → base64 디코드 후 파일로 저장하고 JSON을 참조로 치환하는 **일회성 스크립트**(재실행 안전하게).
- 보고서 화면에서는 `<img src>`가 서버 URL을 가리키므로 브라우저 캐시도 탄다.
- **노력: 중간 / 효과: 최상(항구적) / 위험: 중간**(이관 스크립트 + 롤백용 백업 필요)

---

### 4단계. 중장기 (선택)

#### I. `json` → `jsonb` + 저장 시 `ensure_ascii=False`
- 저장 텍스트 ~40% 감소, 조회 시 텍스트 재파싱 제거.
- 주의: `jsonb`는 키 순서를 보존하지 않고 중복 키를 제거한다. 프론트가 키 순서에 의존하지 않는지 확인 필요.

#### J. 과제별 행 정규화 (기존 V2 계획)
- [정규화 검토 문서](./디지털트윈_대시보드_정규화_검토.md) 12장의 "V2 모듈 복제 병행" 안.
- **판단: 지금 통증의 원인이 아니므로 후순위.** 1~3단계를 하면 체감 속도 문제는 사라진다.
  정규화의 진짜 이득은 무결성(FK)·코드 단순화이고, 그건 급하지 않다.
- 단, 1~3단계는 전부 **정규화와 충돌하지 않는다**(오히려 F/H는 정규화의 사전작업 성격).

---

## 5. 권장 실행 순서 및 예상 효과

| 순서 | 작업 | 노력 | 편집 1건당 왕복 |
|---|---|---|---|
| — | **현재** | | **~2.5 MB** |
| 0 | 운영서버 `[Upsert timing]` 로그 + DB 스캔으로 진단 확정 | 30분 | — |
| 1 | **A** 이미지 압축 + **B** 스냅샷 정리 + **C** gzip + **D** 재다운로드 제거 | 0.5~1일 | **~250 KB** |
| 2 | **E** metadata 버그 수정 → **F** 부분 저장 + **G** 삭제분 제외 | 2~3일 | **~10 KB** |
| 3 | **H** 이미지 파일 분리 + 기존 3건 이관 | 2~3일 | **~10 KB (영구 고정)** |
| 4 | I / J (선택, 별건) | — | — |

> 1단계만으로도 체감은 크게 달라진다. **A와 B는 오늘 바로 해도 되는 수준**이고 위험도 낮다.

---

## 6. 리스크 & 안전장치

| 항목 | 리스크 | 안전장치 |
|---|---|---|
| B 스냅샷 삭제 | 백업 소실 | 삭제 전 DB 덤프(`export_database.py` 관례 존재), 수동/upload 타입은 보존, auto만 정리 |
| D 재다운로드 제거 | 다른 사용자 변경이 늦게 보임 | 명시적 "서버 데이터 불러오기" 버튼 유지 + 주기 폴링 검토 |
| F 부분 저장 | 안 보낸 항목이 지워질 위험 | **E 선행 필수.** "일부만 보내고 나머지 보존"을 검증하는 테스트 먼저 |
| H 이미지 이관 | 이미지 유실 | 이관 스크립트는 재실행 안전(idempotent), 원본 JSON은 이관 후에도 일정 기간 보존 |
| I jsonb 전환 | 키 순서/중복 키 변화 | `dxdigitaltwin_test`에서 리허설 후 적용 |

---

## 7. 지금 결정해야 할 것

1. **1단계를 바로 진행할지** (A·B·C·D 묶음, 0.5~1일)
2. **자동 스냅샷 정책**: upsert에서 완전 제거 / 하루 1회 / N분 쿨다운 — 어느 쪽?
3. **기존 스냅샷 358개 정리 범위**: 최근 30개만 보존? 수동·upload 타입은 전량 보존?
4. **이미지 처리**: 3단계(파일 분리)까지 갈지, 1단계 A(압축)에서 멈출지
   - 보고서 이미지를 앞으로 널리 쓸 계획이면 → **H까지 가야 한다**
   - 소수 과제에만 쓸 거면 → A만으로도 당분간 충분
5. **정규화(V2)**: 이번 성능 개선과 분리해서 별도 판단 (본 문서 권고: 후순위)

---

## 부록 A. 운영 DB 현황 확인 쿼리

```sql
-- 저장 payload 크기
SELECT version,
       pg_size_pretty(octet_length(projects::text)::bigint)     AS proj,
       pg_size_pretty(octet_length(performances::text)::bigint) AS perf,
       json_array_length(projects) AS n_proj, json_array_length(performances) AS n_perf
FROM dashboard_data;

-- 과제 JSON 키별 용량 Top 15 (어느 필드가 무거운가)
WITH e AS (SELECT value AS p FROM dashboard_data, json_array_elements(projects) value),
     kv AS (SELECT key, value FROM e, json_each(e.p))
SELECT key, pg_size_pretty(sum(octet_length(value::text))::bigint) AS total,
            pg_size_pretty(max(octet_length(value::text))::bigint) AS max_one
FROM kv GROUP BY key ORDER BY sum(octet_length(value::text)) DESC LIMIT 15;

-- base64 이미지가 박힌 과제
SELECT p->>'id' AS id, pg_size_pretty(octet_length(p::text)::bigint) AS sz
FROM (SELECT value AS p FROM dashboard_data, json_array_elements(projects) value) e
WHERE p::text LIKE '%data:image%' ORDER BY octet_length(p::text) DESC;

-- 스냅샷 누적량
SELECT snapshot_type, count(*),
       pg_size_pretty(sum(length(snapshot_data::text))::bigint) AS json_text
FROM dashboard_snapshots GROUP BY 1 ORDER BY 2 DESC;

-- 테이블 실디스크 사용량
SELECT C.relname, pg_size_pretty(pg_total_relation_size(C.oid)) AS total
FROM pg_class C JOIN pg_namespace N ON N.oid = C.relnamespace
WHERE N.nspname='public' AND C.relkind='r'
ORDER BY pg_total_relation_size(C.oid) DESC LIMIT 10;
```

## 부록 B. 근거 파일 경로

| 내용 | 위치 |
|---|---|
| 이미지 base64 생성 (압축 없음) | `frontend/.../ProjectModal/components/DetailInfoModal.jsx:652-673` |
| 이미지 필드 정의 | `frontend/.../ProjectModal/EditProjectModal.jsx:256-262, 621-627` |
| 보고서 화면 렌더 | `frontend/.../Dashboard/ProjectReportView.jsx:2000, 2189, 2351` |
| 전체 배열 저장 호출 | `frontend/.../DigitalTwinDashboardApp.jsx:810-826` |
| 저장 후 전체 재다운로드 | `frontend/.../DigitalTwinDashboardApp.jsx:629, 826, 1271, 1391, 1551` |
| 부분 저장 선례 (이미 동작) | `frontend/.../DigitalTwinDashboardApp.jsx:1542, 1637` |
| upsert 병합 + 자동 스냅샷 | `backend/.../digital_twin_dashboard/routes.py:818-1229` (스냅샷 `:1087`, metadata 덮어쓰기 `:1115`) |
| 타이밍 계측 (이미 삽입됨) | `backend/.../routes.py:827, 1185-1197` |
| `GET /data` 필터 | `backend/.../routes.py:633-669` |
| `GET /data/download` (조회인데 로그 INSERT) | `backend/.../routes.py:1232-1291` |
| 컬럼 타입 `postgresql.JSON` (jsonb 아님) | `backend/.../models.py:8, 234-236` |
| 진짜 첨부파일 (디스크 저장, 무해) | `backend/.../models.py:301-342` |
