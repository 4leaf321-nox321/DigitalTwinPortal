# 디지털 트윈 관계도 — AI 에이전트 계획

작성 2026-08-09 · 대상 `digital_twin_dashboard` 관계도 탭 + 로컬 LLM
선행 문서 `디지털트윈_지식그래프_계획.md` (§6-C 로 만든 관계도가 이 기능의 바탕)

---

## 0. 한 줄 요약

관계도가 **관계를 보는 화면**이라면, 이 기능은 **그 관계를 읽어 "무엇이 막혔고 누가
무엇을 해야 하는지"를 찾아 주는 것**이다. 성격은 기술 분석(Root Cause)이 아니라
**PMO·경영관리**다 — 데이터가 과제·성과·KPI·사람·조직·액션아이템이기 때문이다.

---

## 1. 설계 원칙 — 이 셋이 이 문서의 전부다

### ① LLM 은 **계산하지 않는다**

```
파이썬(결정적)   그래프 탐색 · 집계 · 순위 · 달성률   →  숫자와 경로
LLM             그 결과를 문장으로 엮기              →  서술만
```

경영 도구에서 **틀린 숫자는 답이 없는 것보다 나쁘다.** "관련 과제 7개" 가 실제로
6개면 그 화면 전체를 못 믿게 된다. 그리고 한 번 못 믿게 된 화면은 안 쓴다.

**이미 서버에 있는 계산을 그대로 쓴다.** 새로 만들지 않는다 — 만들면 화면과 AI 가
다른 숫자를 말하는 날이 온다(규칙 복제 금지, 이 저장소가 여러 번 겪은 사고다).

| 쓸 것 | 어디 |
|---|---|
| 기여도 합 이상 | `routes_v2.contribution_report` |
| KPI 달성률 | `dx_kpi_management.achievement` |
| 진행률 파생 | `routes_v2.derive_progress` |
| 액션아이템 완료 파생 | `routes_v2.normalize_action_items` |
| 그래프 탐색 | `graph_view.build_graph` |
| 가시성 | `permissions.can_view_project` |

### ② 모든 답에 **근거 경로**를 담는다

응답에 `path: ["kpi:12", "project:…", "perf:…"]` 를 실어 보내고, 관계도가 그
경로만 남기고 나머지를 흐리게 한다. **말이 아니라 그림으로 근거를 댄다.**

별도 「Reasoning Graph」 화면은 만들지 않는다 — 그림을 두 개 보게 하면 둘을
대조하는 일이 사용자 몫이 된다. `GraphCanvas` 의 `focusRef`/`neighborRefs`
하이라이트를 그대로 확장한다.

### ③ **답보다 먼저 신뢰도를 말한다**

이 데이터에는 빈칸이 있다. 빈칸을 모르고 낸 결론은 자신 있게 틀린다.
모든 답변 아래에 **그 답이 무엇을 기준으로 했는지** 한 줄을 붙인다.

> ⓘ 이 분석은 **knoxId 연결률 42%** 기준입니다. 계정이 연결되지 않은 참여인력은
> 그래프에 없어 부하 계산에서 빠집니다.

---

## 2. 데이터 실측 — 무엇이 되고 무엇이 안 되는가

개발 DB, 활성 과제 100건 기준 (2026-08-09).

| 재료 | 수치 | 판정 |
|---|---|---|
| KPI 연결 | 310건 · 100/100 과제 | ✅ |
| 기여등급 | primary 91 · support 102 · indirect 75 · **미지정 42** | ⚠️ 14% 빈칸 |
| 성과 연결 | 112건 · 기여도(%) 100% 입력 | ✅ |
| 성과 실적 | 목표 112/112 · **실적 96/112** | ✅ "성과가 났나" 판단 가능 |
| 액션아이템 | 303건(미완료 181) · **목표일 100% 입력** | ✅ 지연 계산 가능 |
| 사람 knoxId | 참여 300/300 · PL 100/100 | ⚠️ **시드라서 100%. 운영은 다르다** |

### ⚠️ 개발 DB 는 시드다 — 운영과 다르다

위 숫자는 씨앗 데이터라 거의 완벽하다. **운영은 그렇지 않다.**
`owner-links 감사` 기능을 만든 이유 자체가 "운영에 이름만 있고 knoxId 가 빈 과제가
많아서" 였다. 그래서 **0단계(데이터 공백 리포트)를 운영에서 먼저 돌려 보는 것**이
이 계획의 실질적인 첫 걸음이다.

### ⚠️ 「숨은 연결」은 지금 방식이면 **잡음**이다

`과제 A → KPI X ← 과제 B` 는 이 기능에서 가장 매력적인 아이디어지만, 실측:

- 과제당 평균 KPI **3.1개**, KPI 는 16개뿐 → KPI 하나에 과제 **약 19개**
- **97/100 과제가 KPI 를 2개 이상** 갖는다

즉 **아무 두 과제나 KPI 를 공유한다.** "협업 가능성이 높습니다" 가 거의 모든 쌍에
대해 참이 되어 정보가 0 이다. 사람도 같다(8명이 100과제).

→ **희소성 가중이 필요하다.** 흔한 KPI 로 이어진 것은 약하게, **드문 KPI**(과제
2~3개만 미는 것)로 이어진 것은 강하게 센다(TF-IDF 와 같은 발상). 여기에
**"직접 연결은 없는데 간접으로만 강한"** 조건을 걸어야 비로소 발견이 된다.

### ⚠️ 담당자 부하는 **연결률을 먼저 말하지 않으면 거짓말**이 된다

그래프의 사람 노드는 **knoxId 가 있는 사람만**이다(이름만으로 노드를 만들면
동명이인이 한 사람이 되므로 일부러 그렇게 했다). 운영 연결률이 40% 면
**부하의 60% 가 안 보이는 채로** "특정인에게 몰려 있다" 고 단정하게 된다.
사람에 대한 판단이라 틀리면 값이 비싸다.

---

## 3. 화면 설계 — 새 화면을 만들지 않는다

관계도 탭의 **우측 패널을 260px → 380px 로 넓히고 내용을 바꾼다.** 모달은 쓰지
않는다 — 분석의 근거가 그래프 위 강조이므로 **그래프가 가려지면 기능이 죽는다.**

```
┌─ 관계도 ──────────────────────────────────────────────────┐
│ [연도][사업부][관계][색기준]          [AI 분석 ▾] [다시 읽기] │
├────────────────────────────────────┬──────────────────────┤
│                                    │ ◀ 분석 결과          │
│         (그래프 캔버스)             │ 가상 검증률           │
│                                    │ 달성률 62% · 위험     │
│   ● 경로에 든 것만 진하게           │ ──────────────────   │
│   ○ 나머지는 흐리게                 │ ① 기여 과제    12 ▸  │
│                                    │ ② 성과 확인     7 ▸  │
│                                    │ ③ 성과 없음     5 ⚠▸ │
│                                    │ ④ 미완료 액션   9 ▸  │
│                                    │ ⑤ 병목 담당자   2 ⚠▸ │
│                                    │ ──────────────────   │
│                                    │ [LLM 서술 3~4줄]      │
│                                    │ 우선 대응 과제 3      │
│                                    │ ⓘ knoxId 연결률 …    │
└────────────────────────────────────┴──────────────────────┘
```

- **①~⑤ 는 버튼이다.** 누르면 그래프가 그 집합만 남기고 흐려진다.
  이것이 「Agent Reasoning Graph」다 — 두 번째 그림을 그리지 않는다.
- **진입점 둘**
  - 노드 **우클릭 → 「AI 분석」** (이미 만들어 둔 컨텍스트 메뉴에 한 줄 추가)
  - 상단 **「AI 분석 ▾」** — 검증된 질문 목록
- **맨 아래 신뢰도 한 줄** (§1-③)

### 자유 질의는 나중에

로컬 LLM 에 **의도 파싱까지** 맡기면 답이 흔들린다.
`버튼 → 결정적 계산 → LLM 서술` 순서를 먼저 굳히고, 자유 질의는 그 위에 얹는다
(질문 → 정해진 분석 중 하나 고르기). AI 폼 도우미에서 이미 검증된 순서다.

---

## 4. 단계

| 단계 | 무엇 | LLM | 새 테이블 |
|---|---|---|---|
| **0** | **데이터 공백 리포트** | 없음 | 없음 |
| **1** | **KPI 한 장 브리핑** (메인) | 서술만 | 없음 |
| **2** | 근거 경로 하이라이트 | 없음 | 없음 |
| **3** | 위험 KPI Top-N + 원인 | 서술만 | 없음 |
| **4** | 숨은 연결 (희소성 가중) | 서술만 | 없음 |
| **5** | 유사·중복 과제 | 판정 | 임베딩 저장 |
| **6** | 자유 질의 (0~5 를 툴로) | 의도 파싱 | 없음 |

### 0. 데이터 공백 리포트 — LLM 없이

에이전트의 **체온계**다. 이것 없이 1단계를 만들면 답변에 신뢰도를 못 붙인다.

- 성과 없는 과제 / KPI 미연결 과제
- 기여도 합이 100 이 아닌 성과 (`contribution_report` 그대로)
- **목표일이 지난 미완료 액션** (목표일 100% 입력이라 계산된다)
- PL·참여인력 **knoxId 미연결률**
- 기여등급 미지정 건수

계산만으로 되고 **즉시 유용하다.** 운영에서 이걸 먼저 돌려 보면 1단계가 얼마나
믿을 만할지 미리 안다.

### 1. KPI 한 장 브리핑 ← **메인 기능**

KPI 하나를 고르면 한 번에:

```
KPI  →  기여 과제 N  →  성과 확인 N / 성과 없음 N
                     →  미완료 액션 N (그중 지연 N)
                     →  병목 담당자 N
                     →  우선 대응 과제 N  →  추천 액션
```

- 숫자·순위·경로는 **전부 파이썬**
- LLM 은 그 결과를 받아 **3~4문장**으로 엮는다
- **추천 액션은 후보만** 낸다 — `AiActionItemsModal` 패턴(사람이 골라서 저장)을
  재사용한다. AI 쓰기 금지 원칙과 맞는다

### 2. 근거 경로 하이라이트

응답의 `path` 를 `GraphCanvas` 하이라이트에 연결한다. 1단계와 같이 나가야
"근거를 보여준다" 가 성립한다.

### 3. 위험 KPI Top-N

**추정이 아니라 사실이다.** `dx_kpi_management` 에 목표·실적·달성률 로직이 이미
있다. 제안 중 근거가 가장 단단한 축이므로 먼저 한다.

### 4. 숨은 연결 (희소성 가중)

§2 의 경고를 반영한 뒤에만 만든다. 가중 없이 내보내면 "모든 과제가 협업 후보"
라는 결론이 나오고, 그 순간 기능 전체가 장난처럼 보인다.

### 5. 유사·중복 과제

**임베딩이 필요하다.** 100개면 4,950쌍이라 LLM 쌍 비교는 불가능하다.
텍스트를 벡터로 한 번 바꿔 두면(호출 100번) 비교는 순수 계산이 된다.

열린 질문 — **로컬 LLM 이 `/v1/embeddings` 를 여는가.** 지금 `ai/llm.py` 는
`/chat/completions` 만 부르고 임베딩 설정이 없다. **운영 담당자 확인 필요.**
- 열리면 → 벡터를 JSONB 로 저장(과제 수백~수천이면 pgvector 불필요),
  `row_version` 을 같이 적어 낡은 것만 다시 만든다
- 안 열리면 → KPI·성과 공유로 후보를 10쌍 정도로 좁힌 뒤 **그 10쌍만** LLM 판정

### 6. 자유 질의

0~5 를 툴로 노출하고 LLM 이 고르게 한다. 순서상 마지막이다.

---

## 5. 만들지 않는 것

- **기술 Root Cause 분석** — 이 데이터에는 불량·시험·부품이 없다
- **AI 가 값을 고치는 기능** — 이 에이전트는 **읽기 전용**이다.
  추천은 후보까지고 저장은 사람이 한다
- **별도 Reasoning Graph 화면** — 관계도 위 강조로 대신한다
- **벡터 DB 도입** — 지금 규모에서 과잉

---

## 6. 검증 방법

**개발 PC 는 실제 GLM(10.198.143.137:10000)에 못 붙는다.** 스텁으로 만든다.

그런데 §1-① 구조(파이썬 계산 → LLM 서술) 덕에 **스텁으로 로직 전체를 검증할 수
있다.** 숫자와 경로는 LLM 과 무관하게 나오므로, 시험은 이렇게 나뉜다.

| 대상 | 어떻게 |
|---|---|
| 탐색·집계·순위·경로 | `dt3_test_*` 로 **결정적 검증** (LLM 없이) |
| 신뢰도 표기 | 데이터를 일부러 비워 놓고 문구가 바뀌는지 |
| 가시성 | 비관리자로 불러 **볼 수 있는 것만** 나오는지 (관계도 시험과 같은 방식) |
| LLM 서술 | 스텁이 마커(`### dt-graph-agent:`)로 답하게 하고, 형식만 확인 |

**서술이 비어도 숫자는 나와야 한다.** LLM 이 죽어도 브리핑이 반쯤은 쓸모 있어야
한다 — 그게 "LLM 은 계산하지 않는다" 의 실질적 이득이다.

---

## 6-2. 분석 목록 — 무엇을 물을 수 있게 할 것인가

처음 셋(데이터 공백·위험 지표·숨은 연결)을 만들고 나서, **아직 안 쓴 재료**를 훑어
시나리오를 넓혔다. 실측(개발 DB, 활성 과제 100건):

```
진척 이력        776행 (2행 이상인 과제 100개)   ← 시간 축. 아무도 안 쓰던 데이터
성과 이력      1,676행
미완료 액션 목표월  2026-12 → 91건 · 2026-09 → 52건 · 나머지 38건
이슈             56건 중 미해결 41건 (73%)
중점과제 26 · PoC 18
그림 있는 과제      1 / 100
변경 이력      2,772행 (손댄 과제 398)
```

**미완료 액션 181건 중 91건이 12월 목표**다. "계획을 뒤로 미뤘다" 가 데이터에 그대로
찍혀 있다 — 만들기 전에 이미 신호가 잡힌 셈이다.

| # | 분석 | 재료 | 왜 |
|---|---|---|---|
| a | **데이터 공백** ✅ | 연결·기여도·목표일·knoxId | 다른 분석의 신뢰도를 정한다 |
| b | **KPI 브리핑** ✅ | KPI↔과제↔성과↔액션↔사람 | 메인 |
| c | **위험 지표** ✅ | 달성률 | 추정이 아니라 사실 |
| d | **숨은 연결** ✅ | 희소성 가중 | 그래프라서 되는 것 |
| e | **멈춘 과제** ✅ | `dt2_project_history` | **유일하게 시간 축을 연다** |
| f | **일정 쏠림** ✅ | 액션 목표일 분포 | 12월 몰림이 실측으로 잡힌다 |
| g | **이슈 적체** ✅ | `issues_json` | 미해결 73% 인데 어디에도 안 보인다 |
| h | **중점과제의 말과 실제** ✅ | `is_key` + 연결 | 선언과 실제의 불일치 |
| i | **보고 준비도** ✅ | 상세정보·그림·실적 | 「결과 보고서」 전 체크리스트 |
| j | 방치 탐지 | `dt2_project_changes.updated_at` | 나중 — e·f 와 겹치는 면이 있다 |
| k | 사업부 비교 | 커버리지·밀도 | 나중 — **사업부를 줄 세우는 화면**이라 의도를 먼저 정해야 한다 |
| l | 계보·PoC 전환 | 선행과제 | **데이터가 아직 없다**(운영 실측 0건). 사람들이 채운 뒤에 |

### e(멈춘 과제)가 가장 값진 이유

지금 화면들은 전부 **스냅샷**이다. 「종합 대시보드」도 「진행률 현황」도 지금 값을
보여줄 뿐, "이 과제가 3주째 그대로다" 는 어디에도 없다. `dt2_project_history` 는
2026-07-29 부터 쌓이고 있었는데 **읽는 코드가 하나도 없었다.**

궤적으로 볼 수 있는 것:
- 상태는 `정상진행` 인데 **진행률이 그대로**인 과제 (말과 실제의 불일치)
- 진행률이 **거꾸로** 간 과제 — 나쁜 게 아니라 **계획이 커진 것**이다
  (액션아이템을 늘리면 파생 진행률이 내려간다). 그렇게 읽어야 한다
- 액션 완료 **속도**로 본 남은 기간 소화 가능성

### 안 만드는 것 (지금은)

- **k(사업부 비교)** — 계산은 쉽지만 사업부를 줄 세우는 화면이다. 무엇을 위해
  비교하는지 먼저 합의하지 않으면 숫자가 정치적으로 쓰인다.
- **l(계보)** — 선행과제 데이터가 쌓이기를 기다린다.

---

## 7. 진행 상황 (2026-08-09 · 개발서버)

**0·1·2·3·4 완료.** 6(자유 질의)만 남겼다 — 임베딩과 무관하지만 의도 파싱은 위험이
다른 종류라, 0~4 가 굳은 뒤 얹는 것이 맞다. 5(유사·중복)는 임베딩 확인 대기.

| 만든 것 | 어디 |
|---|---|
| 분석 (LLM 없음) | `ai/graph_agent.py` — `data_gaps` · `kpi_briefing` · `risky_kpis` · `hidden_links` · `coverage` |
| 서술 (LLM 여기만) | `ai/graph_narrate.py` |
| API | `GET /graph/agent/{gaps,kpi/<id>,risky,hidden}` · `POST /graph/agent/narrate` |
| 화면 | `components/GraphView/AgentPanel.jsx` (우측 260→380px) |
| 스텁 | `llm_stub.py` 에 `### dt-graph-agent: narrate` |
| 시험 | `scripts/dt3_test_graph_agent.py` (41건) |

### 만들면서 데이터를 보고 고친 것 둘

**병목은 절대 건수가 아니라 상대값이다.** 처음에 "미완료 3건 이상" 으로 재니 관련자
8명이 **전원** 병목으로 잡혔다. 다들 비슷하게 많으면 그건 병목이 아니라 바쁜 팀이다.
**중앙값의 1.8배**를 넘어야 병목으로 본다(`BOTTLENECK_RATIO`). 고친 뒤 0명이 됐고,
그게 맞는 답이다.

**숨은 연결의 희소성 가중이 실제로 값을 했다.** 그냥 세면 4,950쌍이 거의 다 이어지는데,
과제 8개 넘게 걸린 흔한 고리 19개를 빼고 `1/(고리에 걸린 과제 수)` 로 가중하니
**74쌍**으로 줄었다.

### 파일을 가른 것이 규칙을 지킨다

`graph_agent.py` 에는 **`llm` 이라는 글자가 없다.** 그래서 그 파일의 모든 숫자가
결정적임이 코드만 보고 보장된다. 시험도 그것을 못 박는다 — `LLM_BASE_URL` 을 비우고
분석을 부르면 **숫자가 똑같이 나오고**, 서술만 `null` 로 온다.

---

## 8. 첫 걸음 제안

**0단계(데이터 공백 리포트)를 먼저 만들고 운영에서 한 번 돌려 본다.**
하루면 되고, LLM 이 필요 없으며, 그 결과가 1단계의 설계를 바꾼다 —
knoxId 연결률이 낮으면 「병목 담당자」를 1단계에서 빼고 시작해야 한다.
