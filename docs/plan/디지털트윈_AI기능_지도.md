# 디지털 트윈 — AI 기능 지도

작성 2026-08-01 · 갱신 2026-08-08 (⑦ 폼 도우미 추가) ·
**AI 관련 작업을 시작하기 전에 이 문서를 먼저 본다.**

이 프로젝트에는 이름에 `ai` 가 붙은 것이 **여덟 갈래**고, 서로 다른 물건인데 이름이 비슷하다.
특히 `ai_tools.py` 와 `ai/agent_tools.py` 는 **같은 폴더에 나란히** 있다.
새 AI 기능을 붙이기 전에 **어느 갈래인지 먼저 정하고**, 다 만들면 이 표를 갱신한다.

> ⚠️ 이 표에 없는 AI 모듈을 새로 만들면 `dt3_test_agent.py` 가 **실패한다.**
> 검사를 고치지 말고 **이 표에 한 줄을 추가**하라 — 지도가 낡는 것을 막으려는 장치다.

---

## 한눈에

```
[브라우저]                                    [백엔드]
                                              
문서자동작성 ──/llm──> localhost:8080          ← ① 브라우저가 LLM 을 직접 부른다
 (데이터 매핑)                                    도구 못 씀. 살아 있음

AiChatSidebar ─X                              ← ② 내렸다 (2026-08-01). 코드는 남아 있음
 (일반 대화)                                     백엔드 /api/ai/* 는 그대로

AiAgentPanel ──/api/dt-v2/ai/agent──> ai/agent.py ──LLM_BASE_URL──> GLM
 (관리자 전용)                              │        ← ③ 백엔드가 부른다. 도구 씀
                                            └──REST──> /api/dt-v2/* (권한·202·409)

Claude Code ──PAT──> mcp_server(3003) ──REST──> /api/dt-v2/*
                                                 ← ④ 외부 AI. ③과 같은 도구·같은 규칙

AiFillPanel        ──/api/dt-v2/ai/form/*──> ai/form_assist.py ──> GLM
AiActionItemsModal   (편집 권한 있는 사람)      ← ⑦ 값을 **제안만** 한다. 도구 없음·쓰기 없음
AiPeopleModal                                     저장은 사람이 평소의 저장 버튼으로
AiKpiSuggestModal
 (편집창 안)
```

---

## 여덟 갈래

| # | 이름 | 무엇 | 상태 |
|---|---|---|---|
| ① | `/llm` (Vite 프록시 → `localhost:8080`) | **브라우저가 직접** 부르는 LLM. 도구 못 씀 | 살아 있음 — `AutoDocumentApp.jsx:1005` 데이터 매핑 |
| ② | `app/modules/ai_context/` → `/api/ai/*` | 옛 채팅(`AiChatSidebar`)의 페이지 맥락·분석 | **화면에서 내렸다**(2026-08-01). API 는 살아 있음 |
| ③ | `…/digital_twin_dashboard/ai_tools.py` | **필드 안내**(`describe_fields`). 어휘·위험도·선택지 | 살아 있음 — ③④⑦이 **같이** 쓴다 |
| ④ | `…/digital_twin_dashboard/ai/` (`agent`·`agent_tools`·`llm`) | **에이전트**(어댑터·도구·루프). Phase 5 | 살아 있음 — **읽기는 전원**, 쓰기는 관리자 |
| ⑤ | `mcp_server/` (포트 3003) | **외부 AI**(Claude Code 등)용 MCP 서버 | 살아 있음 — 별도 프로세스·venv |
| ⑥ | `mcp_server/skill/digitaltwin/SKILL.md` | AI 가 지킬 **절차 규칙**. ④⑤가 같이 쓴다 | 살아 있음 |
| ⑦ | `…/ai/form_assist.py` → `/api/dt-v2/ai/form/*` · `AiFillPanel` · `AiActionItemsModal` · `AiPeopleModal` · `AiKpiSuggestModal` | **폼 도우미** — 편집창의 칸·액션아이템·참여인력·DX KPI 후보. **쓰기 코드가 없다** | 2026-08-08 추가 — 편집 권한이 있는 사람 |
| ⑧ | `…/detail_rules.py` | 상세 과제 정보 7섹션의 **한계**(39자·항목 수). 저장 경로(`routes_v2`)와 ⑦이 **같이 본다** | 2026-08-08 분리 |

---

## 헷갈리기 쉬운 짝 — 여기서 사고가 난다

### 🚨 `ai_tools.py` vs `ai/agent_tools.py` (같은 폴더!)

| | `ai_tools.py` | `ai/agent_tools.py` |
|---|---|---|
| 무엇 | **필드** 안내 — 무엇을 고칠 수 있나 | **도구** 정의 — LLM 이 부를 함수 |
| 누가 읽나 | MCP `describe_fields` · 에이전트 | LLM (function-calling 스키마) |
| 고칠 일 | 필드가 늘거나 위험도가 바뀔 때 | 도구를 늘리거나 인자를 바꿀 때 |

**"tools" 라는 말이 서로 다른 것을 가리킨다.** 필드를 손볼 일인데 `agent_tools.py` 를 열었다면
잘못 온 것이다.

### `AiChatSidebar` vs `AiAgentPanel`

| | `components/AiChatSidebar.jsx` | `…/digital-twin-dashboard/components/AiAgentPanel.jsx` |
|---|---|---|
| 누가 부르나 | **브라우저**가 `/llm` 직접 | **백엔드**가 LLM 을 부른다 |
| 도구 | 못 씀 | 20개 씀(권한 검사와 함께) |
| 지금 | **화면에 없다**(진입점 주석 처리) | 살아 있음 · **관리자 전용** |

### 🚨 `ai/agent.py` vs `ai/form_assist.py` (같은 폴더!)

**여기가 가장 헷갈리는 짝이다.** 둘 다 백엔드가 LLM 을 부르는데, **고치는 주체가 다르다.**

| | `ai/agent.py` (④) | `ai/form_assist.py` (⑦) |
|---|---|---|
| 누가 고치나 | **LLM 이** 도구로 고친다 | **사람이** 편집창에서 저장한다 |
| 도구 | 20개 (REST 로 자기 API 호출) | **없다** |
| 확인 절차 | 핵심 필드는 **202 → confirm_change** | **편집 화면 자체가 확인 화면**이다 |
| 프롬프트 | `SKILL.md` 를 그대로 싣는다 | 짧은 자체 규칙 (도구가 없어 그 절차가 안 맞는다) |
| 관문 | 읽기는 **전원**, 쓰기는 **관리자** | **그 과제를 고칠 수 있는 사람**(`can_edit_project`) |
| 쓰기 | 있다 (`patch_project` 등) | **한 줄도 없다** — 값을 돌려줄 뿐 |
| 화면 | `AiAgentPanel` (대시보드 우하단) | `AiFillPanel`·`AiActionItemsModal` (편집창 안) |

**관문의 기준이 다른 이유** — ④는 LLM 이 스스로 쓰기 때문에 **쓰기만** 관찰이 끝날 때까지
좁혀 둔다. ⑦은 어차피 그 사람이 손으로 채울 칸을 대신 채우는 것이라 편집 권한 밖으로
새는 것이 없다.

**④의 읽기는 2026-08-08 에 전원 개방했다.** 조회는 그 걱정이 없어서다. 이때
**읽기 전용을 서버가 강제한다** — 비관리자가 `readonly: false` 를 보내도 라우트가
되돌리고 `readonlyForced` 로 알려준다. 화면의 스위치는 안내일 뿐이고, 잠긴 채로
**보여준다**(숨기면 "그런 기능이 있는 줄도 모르는" 상태가 된다).

⚠️ **⑦에는 확인 대기(202)가 없다.** 없어도 되는 것이지 빠뜨린 것이 아니다 —
값이 폼에 들어갈 뿐이고 사람이 보고 저장을 누른다. 그래서 화면이 **적용 전에
before → after 를 반드시 보여준다.** 자동 적용을 넣으면 그 관문이 사라진다.

**⑦이 채운 칸은 변경 이력에 `source='ai_fill'` 로 남는다**(2026-08-08). 저장을 누른 것은
사람이라 나머지 칸은 `ui` 다 — **한 저장 안에서 칸마다 섞인다.** 그래서 저장 단위가 아니라
**칸 단위로** 기록한다(`_apply(ai_fields=…)`). `ai`(에이전트가 스스로 쓴 것)와도 갈라 둔다:
저쪽은 202 확인을 거쳤고 이쪽은 사람이 화면에서 보고 저장했다.

### 🚨 참여인력만 규칙이 하나 다르다 (`AiPeopleModal`)

다른 둘은 AI 가 만든 **값**을 사람이 확인하면 되지만, 여기서 AI 는 **값을 만들지 않는다** —
이름만 찾고 **계정은 서버가 붙이고 누구인지는 사람이 고른다.**

| 단계 | 누가 |
|---|---|
| 원문에서 이름·근거 뽑기 | 모델 (**knoxId 는 절대 만들지 않는다**) |
| 이름 → 계정 후보 | 서버 (`_people_candidates`, 이름이 정확히 같은 활성 계정) |
| 누구인지 확정 | **사람** (후보가 하나여도 눌러야 한다) |

이렇게 나눈 이유는 하나다 — 참여인력에 들어간 사람은 **그 과제를 고칠 수 있게 되고**
(`is_project_member`), 원문에는 동명이인을 가릴 정보가 없다.

### 🚨 DX KPI 추천은 **막아 둔 자리를 조건부로 연 것**이다 (`AiKpiSuggestModal`)

`replace_project_kpi_links` 는 AI 쓰기를 **403 으로 막았었다** — "추측으로 채우면 매트릭스의
빈칸(=계획의 구멍)이 가짜로 메워진다".

> ✅ **2026-08-12 그 403 이 202 로 바뀌었다.** 금지를 푼 것이 아니라 **조건을 붙여 연 것**이다
> — MCP·에이전트가 `set_project_kpi_links` 로 **제안**할 수 있고, 항상 확인 대기로 가며
> `reason`(근거)이 없으면 제안조차 만들어지지 않는다(400). 승인 preview 에 지표 이름·
> 대상 사업부·기여 등급을 다 싣는다. 과제-성과 연결이 지나간 길과 같다.
> 회귀는 `dt3_test_kpi_ai_propose.py`.
>
> **아래 폼 도우미 절은 그대로 유효하다** — 그쪽은 여전히 연결을 만들지 않고 후보만 낸다.
> 둘은 다른 물건이다: 폼 도우미는 *화면에서 사람이 고르게*, `set_project_kpi_links` 는
> *대화에서 제안하고 사람이 승인하게*.

폼 도우미가 예외인 근거는 셋이고, **하나라도 무너지면 막아 둔 이유가 되살아난다:**

1. AI 는 연결을 만들지 않는다 — **후보와 근거**만 낸다
2. 화면이 **자동으로 체크하지 않는다** (기본 선택 없음. 사람이 하나씩 고른다)
3. 저장은 평소의 KPI 저장 경로 (사람 권한·낙관적 락)

**대상 사업부·기여 등급 규칙은 서버가 강제한다.** `routes_v2._resolve_link_target` 이 정본이다
— 자기 사업부만 / 기능조직은 골라야 함 / 사업부 전용 지표. 화면(`KpiLinkSection.toggleKpi`)은
그 규칙을 **미리 막아 주는 UX** 이고, 서버가 최종 판정을 한다.

> 🐞 2026-08-12 조사에서 *"규칙이 화면에만 있다"* 고 잘못 적혀 있던 것을 바로잡았다.
> 서버가 원래부터 검증하고 있었고(`list_project_kpi_links` 주석의 "서버도 그 규칙을
> 강제한다 — 아래 PUT 참조"), 그래서 AI 제안을 여는 데 규칙 이관이 필요 없었다.
> AI 가 보낸 조합도 **제안을 만들기 전에** 같은 함수로 검증된다.

### `/api/ai/*` vs `/api/dt-v2/ai/agent` vs `/api/dt-v2/ai/form/*`

앞은 **옛 채팅**의 보조 API(`ai_context`), 가운데는 **에이전트**, 뒤는 **폼 도우미**다.
경로가 비슷하지만 서로 관계없다. 폼 도우미 둘은 아무것도 쓰지 않아
`_READ_ONLY_ENDPOINTS` 에 들어 있다 — **그 목록에 넣은 경로에 DB 쓰기를 넣으면
컷오버 쓰기 차단이 뚫린다.**

### `LLM_BASE_URL` vs `/llm` 프록시

| | `LLM_BASE_URL` (`backend/.env`) | `/llm` (`vite.config.js`) |
|---|---|---|
| 누가 | 백엔드(에이전트) | 브라우저(문서자동작성) |
| 지금 값 | 개발 스텁 `localhost:9001/v1` | `localhost:8080` |
| 없으면 | 에이전트만 503 | 문서자동작성의 데이터 매핑이 깨짐 |

⚠️ **둘은 다른 서버를 가리킬 수 있다.** 하나를 바꿨다고 다른 쪽이 따라가지 않는다.

---

## 새 AI 기능을 붙일 때 — 어디에 두나

**AI 가 스스로 고치나, 사람이 보고 저장하나?**

- **AI 가 고친다** → ④ 에이전트에 **도구를 추가**한다. `ai/agent_tools.py` 의 `_CATALOG` 에 한 줄.
  ⚠️ **판단(권한·검증)을 여기 두지 말 것.** REST 로 넘기고 판단은 `routes_v2.py` 에 둔다.
  ⚠️ MCP(`mcp_server/server.py`)에도 **같은 이름**으로 추가한다 —
  `dt3_test_agent.py` 가 두 목록이 같은지 강제한다.

- **사람이 보고 저장한다(화면의 칸을 채운다)** → ⑦ `ai/form_assist.py` 에 붙인다.
  · 채울 칸은 `_FILL_SPECS` 에 한 줄, **안 채울 이유는 `_EXCLUDED` 에 한 줄.**
    (다음 사람이 "왜 이건 없지" 를 물을 때 답이 코드에 있어야 한다)
  · **선택지·한계를 손으로 적지 말 것** — 선택지는 `describe_fields()`, 상세정보의
    39자·항목 수는 `detail_rules`(⑧)에서 읽어 온다. 복제하면 **화면은 넣었다는데
    저장이 400** 이 되고, 사용자는 원인을 알 방법이 없다.
  · 화면은 **적용 전에 before → after 를 보여준다.** 이게 이 갈래의 유일한 관문이다.
  · 채운 칸을 화면이 `__dtAiFilled` 로 실어 보내면 이력에 `ai_fill` 로 남는다.
    **사람·권한이 걸린 값을 새로 열 때는 이걸 먼저 붙일 것.**
  · `dt3_test_form_assist.py` 에 못 박는다. 특히 **파생·금지 필드가 새어 들어가지
    않는지** — 새면 사용자는 저장 버튼을 눌러서야(400) 안다.

- **사람을 지정하는 값이다** → ⑦에 붙이되 **값이 아니라 후보를 낸다**(`AiPeopleModal` 방식).
  모델에게 이름만 뽑게 하고, 계정 조회는 서버가, 확정은 사람이 한다.
  ⚠️ 여기 들어간 사람에게 **편집 권한이 생기는지** 부터 확인할 것.

- **그냥 대화·요약·생성(데이터를 안 고친다)** → ① 처럼 화면에서 `/llm` 을 부르면 된다.
  단 **데이터를 고치는 것이면 안 된다** — 권한 검사가 브라우저에 없다.

**규칙·절차를 알려줘야 하나?** → ⑥ `SKILL.md` 에 쓴다. **프롬프트에 직접 쓰지 말 것** —
에이전트가 그 파일을 그대로 싣고 있어서, 프롬프트에 따로 쓰면 규칙이 두 곳이 된다.

**필드·위험도를 바꿨나?** → ③ 은 `field_maps.py`·`permissions.py` 에서 **읽어서** 만든다.
정본만 고치면 안내는 따라온다. `ai_tools.py` 에 목록을 복제하지 말 것.

---

## 지금 상태에서 알아둘 것

- **일반 사용자의 AI 창구는 둘이다** — ⑦(편집창 도우미)과 ④의 **읽기**(우하단 패널).
  ②는 내렸다. ④의 쓰기만 아직 관리자 몫이고, 그건 관찰이 끝나면 넓힐 수 있다.
- **④는 요청 하나가 스레드 두 개**를 쓴다(바깥 요청 + 자기 REST 호출).
  운영에서 `WAITRESS_THREADS` 를 넉넉히 줄 것.
  **⑦은 하나만 쓴다** — REST 를 되부르지 않기 때문이다(도구가 없다).
- **개발서버에는 LLM 이 없다.** `scripts/llm_stub.py` 를 띄우면 ④⑦ 모두 진짜 코드로 돈다.
  ⑦은 스텁이 **시스템 프롬프트의 표식**(`### dt-form-assist: …`)을 보고 그럴듯한 JSON 을
  돌려준다. 표식을 바꾸면 스텁도 같이 바꿀 것 — `dt3_test_form_assist.py` 가 강제한다.
- **운영에서 확인한 것은 ④의 "읽기" 까지다.** ⑦은 실제 GLM 과 아직 안 붙여봤다.

---

## 관련 문서

- `mcp_server/README.md` — ④⑤⑥ 상세 (설치·실행·운영 반출)
- `mcp_server/skill/digitaltwin/SKILL.md` — AI 가 지킬 절차 (⑥)
- `backend/DT2_CUTOVER_TODO.md` §4-9 — 반출 대기 목록
- 회귀 시험 — `backend/scripts/dt3_test_agent.py`(④) · `dt3_test_form_assist.py`(⑦)
