import React from 'react';
import { BrowserRouter as Router, Routes, Route, Navigate } from 'react-router-dom';
import Cards from './components/Cards/Cards';
import KnowledgeGraphApp from './components/KnowledgeGraphApp/KnowledgeGraphApp';
import './App.css';

function App() {
  console.log('🚀 메인 App 컴포넌트가 렌더링되었습니다!');
  console.log('🌐 현재 URL:', window.location.href);
  console.log('📍 현재 pathname:', window.location.pathname);
  
  return (
    <Router>
      <div className="App">
        <Routes>
          <Route path="/" element={
            <>
              {console.log('🏠 메인 홈 페이지 라우트 매칭!')}
              <Cards />
            </>
          } />
          
          <Route path="/knowledge-graph" element={
            <>
              {console.log('🔗 지식그래프 페이지 라우트 매칭!')}
              <KnowledgeGraphApp />
            </>
          } />
          
          <Route path="*" element={<Navigate to="/" replace />} />
        </Routes>
      </div>
    </Router>
  );
}

export default App;
