# 디지털 트윈 대시보드 — 저장구조 정규화 검토

> 작성일: 2026-07-03 (개정: 2026-07-04) · 대상 모듈: `backend/app/modules/digital_twin_dashboard/`
> 목적: 현재 "싱글톤 JSON 한 행" 저장 구조의 문제와, "과제별 행"으로 정규화하는 개선안을 정리해
> 나중에 진행 여부/범위를 판단하기 위한 결정 지원 문서.
>
> **최종 채택 방향(2026-07-04): "V2 모듈 복제 병행" — 아래 12장 참조.** 기존 모듈은 무손상으로 두고,
> 정규화 테이블을 쓰는 대시보드 모듈을 하나 더 만들어 데이터 이전을 검증한 뒤 짧게 전환하기로 함.

---

## 1. 한눈에 보는 결론

- **현재**: 모든 과제(328개)·성과(432개)가 `dashboard_data` 테이블의 **단 하나의 행**에, `projects`/`performances`
  라는 `JSON` 배열 컬럼으로 통째 저장됨(싱글톤). 과제마다 행이 나뉘지 않음.
- **문제의 본질**: DB가 "터지는" 문제가 아니라, **커질수록 매 저장이 전량 재작성이라 느려지고, 동시 편집 충돌이
  잦아지며, 참조 무결성을 코드로 손수 유지**해야 하는 구조적 부담.
- **개선 방향**: 서버 안쪽만 "과제별 행"으로 정규화하고 **프론트에는 기존 전체 배열 API 계약을 그대로 유지**(서버가
  배열↔행 어댑터 역할)하는 방식이 현실적. 난이도 **중간**. 프론트까지 바꾸는 건 지금 불필요.
- **현황 스캔 판정**: 데이터가 깨끗하고 작아서 **마이그레이션 자체는 쉬운 편**. 진짜 일감은 백엔드 엔드포인트
  재작성. 별개로 **스냅샷 테이블(352개, ~100MB+) 정리**가 즉효성 있는 독립 과제.

---

## 2. 현재 구조

### 저장 위치
- 테이블: `dashboard_data` (파일: `backend/app/modules/digital_twin_dashboard/models.py:225`)
- 핵심 컬럼:
  - `version` (Integer) — 낙관적 락(충돌 감지)용 버전 번호
  - `projects` (**JSON**) — 모든 과제가 하나의 배열로
  - `performances` (**JSON**) — 모든 성과가 하나의 배열로
  - `data_metadata` (JSON)
- 컬럼 타입은 `sqlalchemy.dialects.postgresql.JSON` → **`json`(텍스트 보존) 타입, JSONB 아님**.
- 조회는 항상 `DashboardData.query.first()` (유일한 한 행 = 싱글톤 패턴).

### 과제 = 행이 아니라 배열 원소
- 각 과제는 DB 행이 아니라 `projects` JSON 배열 안의 딕셔너리. 한글 키 사용
  (`과제명`, `uuid`, `id`(예 "VD-1"), `성과목록[]`, `액션아이템목록[]`, `과제참여인력목록[]`, `월간진척현황{}` 등).
- 개별 과제 삭제조차 행 삭제가 아니라, 배열을 순회해 원소에 `_deleted`/`_permanentlyDeleted` 플래그를 세팅한 뒤
  배열 전체를 다시 저장(`flag_modified`로 JSON 변경 강제 인식).

### 프론트엔드 동기화 모델
- **오프라인 우선(offline-first)**: IndexedDB(`utils/storageHelper.js`)가 사실상 주 저장소, 서버는 동기화 허브.
- **전체 스냅샷 방식**: 앱 로드 시 전체 배열 1회 GET, 저장 시 전체 배열을 통째로 upsert(`/data/upsert`).
  과제 1개만 바꿔도 projects + performances 배열 전체를 전송. (델타로 보내는 건 activityLogs뿐)
- 과제/성과의 **생성·수정 개별 API가 없음**. 삭제/복구만 개별 엔드포인트 존재(내부적으론 플래그 세팅).

### 관계(참조) — 실제 FK가 0개
- 과제→성과: 과제의 `성과목록[]`이 성과를 UUID/문자열로 참조(관계 속성 `과제기여도`, `실적수준` 인라인).
- 첨부→과제: `ProjectAttachment.project_id`가 String(과제 uuid). FK 아님, 단순 인덱스.
- 무결성은 코드로 유지: 성과 삭제 시 모든 과제 순회하며 참조 제거, 앱 로드마다 고아 참조 청소.

---

## 3. 현황 스캔 결과 (2026-07-03, 운영 DB)

> DB: `postgresql://postgres@localhost:5432/dxdigitaltwin`

| 항목 | 값 | 의미 |
|---|---|---|
| `dashboard_data` 행 수 | **1** | 싱글톤 확정 |
| 과제 수 | **328** (활성 224 / 삭제 104) | 소~중규모. 삭제분이 ≈1/3 죽은 데이터로 상재 |
| 성과 수 | **432** | 소~중규모 |
| `projects` 크기 | **314 kB** | DB 1GB 한계엔 여유. 옛 localStorage 5MB 벽까진 ~16배 |
| `performances` 크기 | **44 kB** | |
| `version` | **565** | 그동안 565회 전량 덮어쓰기(누적 마모) |

### 키 상태 — 최상 (마이그레이션에 결정적)
- 과제 328개 **전부 uuid·id 보유, 전부 고유(중복 0)**.
- 성과 432개도 **전부 uuid·id 보유, 전부 고유(중복 0)**.
- → **`uuid`를 그대로 PK로 사용 가능.** 키 누락/중복이 없어 최상의 케이스.

### 과제→성과 참조(`성과목록`) 포맷 — 섞였지만 일관됨
- 총 230개 참조, **전부 object 타입**(문자열 참조 0개).
- 신형 `id`: 25개 / 레거시 `성과항목ID`: 205개 / 레거시 `성과항목UUID`: 199개.
- **230개 전부 `과제기여도` 보유** → 조인 테이블로 그대로 이관 가능.
- → "UUID 우선 → 성과항목ID → id" 결정론적 규칙 하나로 100% 해석 가능.

### 무결성 균열 — 2건뿐
- 참조 219개(distinct) 중 217개 정상 매칭, **고아 참조 2개**(존재하지 않는 성과 지목).
- → 마이그레이션 때 버리면 끝. FK가 있었다면 애초에 못 생겼을 것.

### 곁다리 테이블
- `project_attachments`: **1개** (과제 uuid와 정상 매칭) → 이관 부담 없음.
- `dashboard_activity_logs`: 3112개 — 구조 의존 없음, 그대로 유지.
- `dashboard_snapshots`: **352개** ⚠️ — 각 스냅샷이 `{projects, performances}` 전체 JSON 보관.
  352 × ~360kB ≈ **100MB+**. **DB 최대 용량 소비원.** 정규화와 별개로 정리 대상.

---

## 4. 크기 제한에 대한 사실 정리

| 층 | 한계 | 현실성 |
|---|---|---|
| PostgreSQL `json` 값 하드 상한 | ~1 GB / 값 | 거의 안 닿음 (지금 314kB) |
| TOAST(대용량 자동 분리/압축) | 자동 | 물리적으론 안전 |
| **서버 성능(매 저장 전량 직렬화·파싱)** | 수 MB부터 체감 | **여기서 먼저 아픔** |
| **프론트 localStorage** | ~5 MB | **이미 걸려서 IndexedDB로 우회한 이력 있음** |

- 핵심: "크기 때문에 DB가 터진다"가 아니라 **"커질수록 매 저장이 느려지고 충돌이 잦아진다"**가 실제 위험.
- 참고: `json`(현 타입)은 매 조회마다 텍스트 재파싱. `jsonb`로만 바꿔도 압축·파싱은 개선되나, "통짜 한 행"
  구조는 그대로라 근본 통증은 남음.

---

## 5. 개선 옵션 (효과/노력 순)

### Tier 1 — 안 나누고 통증만 완화 (노력: 낮음)
- 참조 포맷(id/성과항목ID/성과항목UUID)을 **하나로 통일하는 일회성 마이그레이션** → 레거시 3중 분기 제거.
- 영구삭제 항목 실제 정리(싱글톤 행 무한 증가 방지), 고아 참조 2건 청소.
- 구조는 그대로. 사용자 수 적으면 가성비 최선.

### Tier 2 — 서버만 정규화, 프론트 계약 유지 (노력: 중간) ⭐ 추천
- 서버에 과제/성과/조인/첨부 테이블 신설, 서버가 "배열↔행" 어댑터.
- 프론트는 지금과 똑같은 전체 배열 GET/upsert 사용 → **프론트 거의 무손상**.
- 효과: FK/CASCADE로 무결성 보장, 과제 1개 수정=1행 write, `flag_modified`·고아청소 코드 소멸.

### Tier 3 — 프론트까지 개별 과제 델타 동기화 (노력: 높음)
- 실시간 다중 편집이 진짜 필요할 때만. 현재 규모엔 과투자. **지금은 비권장.**

---

## 6. Tier 2 정규화 설계 (개념 스키마)

```
projects
  uuid            (PK)              -- 스캔 확인: 328개 전부 고유
  id              (예 "VD-1", UNIQUE)
  과제명, 과제년도, 사업부, 프로세스, 과제영역, 과제구분,
  시작, 종료, 진행상태, 과제PL, 작성자, 과제상세설명,
  PoC과제여부, 중점과제여부,
  액션아이템목록  (JSON)   -- 1차엔 과제 행 내 JSON 유지 (나중에 자식테이블로 분리 가능)
  과제참여인력목록 (JSON)   -- 동상
  담당부서목록    (JSON)   -- 동상
  월간진척현황    (JSON)   -- 동상
  is_deleted, deleted_at, deleted_by, deleted_by_name,   -- 기존 _deleted 플래그 → 컬럼
  is_permanently_deleted, permanently_deleted_at,
  created_at, updated_at

performances
  uuid            (PK)              -- 432개 전부 고유
  id              (예 "perf-xxxx", UNIQUE)
  성과항목, 대분류, 소분류, 단위, 현재수준, 목표수준,
  계산로직(JSON), 월별실적(JSON), 디지털트윈기여도, 설명, isActive,
  is_deleted, ...
  created_at, updated_at

project_performance   -- 과제↔성과 조인 + 관계속성 (현 '성과목록' 인라인을 풀어냄)
  project_uuid       (FK → projects.uuid, ON DELETE CASCADE)
  performance_uuid   (FK → performances.uuid, ON DELETE CASCADE)
  과제기여도                          -- 스캔: 참조 230개 전부 보유
  실적수준
  PRIMARY KEY (project_uuid, performance_uuid)

project_attachments   -- 기존 테이블, project_id(String)를 FK로 승격
  ... 
  project_uuid       (FK → projects.uuid)   -- 스캔: 첨부 1건, uuid 매칭
```

- `액션아이템/참여인력/담당부서/월간진척`은 **1차 정규화에선 과제 행 내 JSON으로 유지**(완전 정규화는 후속).
  과제·성과·조인 3개만 나눠도 통증 대부분 해소.

---

## 7. 엔드포인트 변경 영향 (백엔드 `routes.py`)

`DashboardData` 싱글톤에 의존하는 엔드포인트 **12개**. 정규화 시 변경 요약:

| 엔드포인트 | 현재 | 정규화 후 |
|---|---|---|
| `GET /data` | 배열 통째 반환 | 행들 조회→배열로 조립(어댑터) |
| `POST /data/upload` | version 체크 후 배열 통째 덮어쓰기 | 행 diff 후 변경분만 upsert |
| `POST /data/upsert` (**380줄 머지**) | 두 거대 배열 통짜 머지 | uuid 키 `INSERT ON CONFLICT` **→ 크게 단순화** |
| `GET /data/download` | 배열 조회 | 조립 |
| `POST /project/<uuid>/delete`·`restore`·`permanent-delete` | 배열 순회+플래그+`flag_modified` | 행의 soft-delete 컬럼 UPDATE |
| `DELETE /performance/<id>/delete` | **모든 과제 순회하며 참조 제거** | 조인테이블 삭제 **→ FK CASCADE로 대체** |
| `GET /attachments/all`·`download-all` | 과제명 매핑 위해 배열 읽음 | 조인 |
| `POST /snapshots`, `POST /activity-logs` | version만 읽음 | 거의 무변경 |

- **영향 없음**: 리포트 생성부(PPT/PDF, `routes.py`의 60%+)는 요청 본문으로 데이터를 받으므로 저장구조와 무관.
  `DashboardActivityLog`도 구조 의존 없어 무영향.
- **스냅샷 저장/복원**(`DashboardSnapshot`): 행↔JSON 직렬화/역직렬화로 조정 필요.
- 재작성 핵심 난이도: (1) `upsert` 380줄 로직, (2) 성과삭제 크로스순회. 둘 다 정규화하면 오히려 짧아짐.

---

## 8. 마이그레이션 계획 (초안)

1. **백업**: 운영 DB 전체 덤프(이미 `database_dump_*.sql` 관례 있음). 스냅샷 1건 별도 확보.
2. **새 테이블 생성**: Alembic migration으로 `projects`/`performances`/`project_performance`, `project_attachments` FK 컬럼 추가.
3. **데이터 이관 스크립트**:
   - 과제: 싱글톤 `projects` 배열 → `projects` 행. `_deleted`/`_permanentlyDeleted` → soft-delete 컬럼.
   - 성과: 동일.
   - 참조: 각 과제 `성과목록` 원소 → `project_performance` 행. **해석 규칙: 성과항목UUID → 성과항목ID → id**.
     고아 참조 2건은 스킵(로그 남김).
   - 첨부 1건: `project_id` → `project_uuid` FK 채움.
4. **병행 검증**: 이관 후 어댑터가 만든 `GET /data` 응답을 **기존 싱글톤 JSON과 diff**해 동등성 확인.
5. **엔드포인트 전환**: 어댑터 기반으로 12개 교체. 프론트 계약(요청/응답 형태) 불변 확인.
6. **컷오버**: 싱글톤 컬럼은 당분간 읽기 전용으로 남겨 롤백 여지 확보 → 안정 후 제거.

---

## 9. 리스크 & 안전장치

- **운영 DB 변경**: 반드시 백업 후, 가능하면 `dxdigitaltwin_test`에서 리허설(테스트 DB 이미 존재).
- **롤백 전략**: 컷오버 후에도 기존 싱글톤 행을 일정 기간 보존(양방향 쓰기 또는 읽기전용 보존).
- **동등성 검증**: 어댑터 출력 == 기존 JSON 을 자동 비교하는 일회성 스크립트 필수.
- **주의**: 과제/성과 참조가 `uuid`와 `id` 두 키로 섞여 있으므로 이관 시 **정규화 키를 uuid로 통일**.

---

## 10. 지금 결정해야 할 것 (판단 포인트)

1. **범위**: Tier 1(완화)만? Tier 2(서버 정규화)까지? — *현재 권고: Tier 2, 단 급하지 않으면 Tier 1 먼저.*
2. **트리거**: 지금 실제로 겪는 통증이 무엇인가? (동시편집 충돌 / 느려짐 / 데이터 꼬임 / 아직 없음)
   → 통증이 약하면 **Tier 1 + 스냅샷 정리**만으로 당분간 충분.
3. **스냅샷 정리**(독립 과제): 352개 → 보존 개수 제한(예: 최근 30개)만 걸어도 ~100MB 즉시 회수. **정규화와 무관하게 먼저 해도 이득.**
4. **완전 정규화 여부**: 액션아이템/참여인력 등 자식 테이블까지 쪼갤지는 **후속**으로 미뤄도 됨.

---

## 11. 부록 — 근거 파일 경로

- 백엔드 모델: `backend/app/modules/digital_twin_dashboard/models.py`
  (`DashboardData` L225, `DashboardSnapshot` L262, `ProjectAttachment` L301, `DashboardActivityLog` L345)
- 백엔드 라우트: `backend/app/modules/digital_twin_dashboard/routes.py`
  (`/data` L611, `/data/upload` L650, `/data/upsert` L796, project 삭제 L1250~, performance 삭제 L1467, 리포트 L2495+)
- 프론트 저장소: `frontend/src/modules/digital-twin-dashboard/utils/`
  (`storageHelper.js`, `dataStructure.js`, `projectPerformanceLink.js`)
- 프론트 동기화: `frontend/src/modules/digital-twin-dashboard/services/settingsApi.js`
- 프론트 앱 로직: `frontend/src/modules/digital-twin-dashboard/DigitalTwinDashboardApp.jsx`
- 설정: `backend/app/config.py`, `backend/.env` (DB: PostgreSQL `dxdigitaltwin`, psycopg3)

---

## 12. 최종 채택 방향 — "V2 모듈 복제 병행" (2026-07-04)

### 결정 요지
기존 모듈을 **직접 수정하지 않는다.** 대신 정규화 테이블을 쓰는 **디지털 트윈 대시보드 모듈을 하나 더(V2) 복제**해
같은 서버·같은 DB 안에 나란히 두고, 운영 데이터를 V1→V2로 옮겨 **정확히 이전됐는지 확인한 뒤에만 V1을 닫는다.**
전환 기간은 **최대한 짧게**(divergence 방지).

### 구조 (별도 서버·별도 DB 아님)
```
[모듈 A: digital-twin-dashboard]      기존. 그대로 운영 (코드·테이블 무손상)
   백엔드: dashboard_data (싱글톤 JSON) / 프론트: 기존 메뉴

[모듈 B: digital-twin-dashboard-v2]   복제본. 화면 동일
   백엔드: dt2_projects / dt2_performances / dt2_project_performance (신규 테이블, 이름 충돌 없음)
   경로:   /api/dt-v2/... (별도 blueprint)
   프론트: /dashboard-v2 메뉴 (폴더 복제 후 API 주소만 V2로)
   기능:   "기존(V1) 데이터 가져오기" 버튼 (재실행 안전 = idempotent)
```

### 빌드 순서 — 위험을 앞에 (검증 우선)
1. **V2 테이블 + 복사 로직 + 검증(비교) 로직** — 프론트 없이 헤드리스로 먼저.
   → 여기서 이미 "데이터 이전 성공"을 증명.
2. **V2 API** — 내부는 행 기반, 응답 형태는 V1과 동일(어댑터).
3. **V2 프론트 복제** — 폴더 복제 후 2단계 API로 주소만 변경.
4. **최종 확인 → V1 읽기전용 동결 → 전원 V2 전환.** V1은 롤백용 보존 후 폐기.

- **핵심 검증 게이트**: 1~2단계만으로 `V1 GET /data` 응답과 `V2 GET /data` 응답을 자동 비교해
  **논리적 동일성**을 증명. 프론트 복제(3단계)는 이 증명 이후 진행 → 헛수고 방지.

### 복사(가져오기) 로직 설계 포인트
- **재실행 안전(idempotent)**: 전환 전까지 "V1에서 계속 편집 → V2로 다시 복사"를 반복해 최신 유지.
- **참조 해석 규칙**: `성과항목UUID → 성과항목ID → id` 순. 고아 참조 2건은 건너뛰고 로그.
- **정규화 키는 uuid로 통일** (스캔상 328/432개 전부 고유).
- **삭제분 104개 처리 방침**(결정 필요): 활성 224개만 옮길지 vs soft-delete 컬럼으로 함께 옮길지.

### 이 방식으로 옮길 대상/제외
- **옮김**: 과제(활성, 필요시 삭제분)·성과·과제-성과 연결·첨부(1건).
- **제외 권장**: 스냅샷 352개(과거 백업, V2는 새로 시작), activity_logs(구조 무관).

### 장단점 (솔직)
- **장점**: 운영 무손상·무중단, 언제든 롤백(V2 메뉴만 숨김), 나란히 눈+스크립트로 검증.
- **단점**: 전환 기간 동안 두 벌(백엔드+프론트) 동시 유지 → **전환을 짧게** 가져가는 것이 유일한 방어책.

### 동시편집에 관한 정정 (앞 장 보완)
- **현재 V1에도 "방지"는 없고 "감지"만 있음**: `version` 낙관적 락으로 충돌을 **감지**(`/data/upload`는 409 거절,
  `/data/upsert`는 UUID로 병합). 실시간 잠금/"편집중" 표시/presence는 **없음**.
- **약점**: 같은 과제를 둘이 동시 수정하면 병합 단위가 "과제 통째"라 **나중 저장이 먼저 것을 덮음**(조용한 손실).
  오래된 전체 스냅샷으로 "강제 덮어쓰기" 시 남의 작업이 통째 날아갈 수 있음.
- **정규화(V2)의 실제 효과 범위**: 프론트가 여전히 전체 배열 업로드 + 전역 version을 유지하면 **동시편집 동작은
  거의 그대로**다. 정규화의 진짜 이득은 **무결성(FK)·저장 속도·코드 단순화**이지 동시편집 해결이 아니다.
  동시편집까지 제대로 고치려면 **과제별 version 컬럼**(V2에 얹기 쉬움) 또는 **프론트 개별 과제 델타 저장**(큰 작업)이 필요.
