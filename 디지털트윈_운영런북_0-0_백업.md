# 운영서버 런북 0-0 — 백업 및 staging 복원

> 대상: **운영서버에서 직접 실행** (AI 없음 · 인터넷 없음)
> 근거: [디지털트윈_대시보드_실행계획.md](./디지털트윈_대시보드_실행계획.md) Phase 0-0
> 소요: **약 40분 ~ 2시간** (대부분 백업 대기 시간)
> 서비스 영향: **없음.** 읽기만 하므로 사용자는 평소대로 쓰면 된다. 공지 불필요.

---

## 이 런북을 쓰는 법

- 명령은 **그대로 복사해서 붙여넣는다.**
- 각 단계마다 **"이 결과가 나와야 정상"** 이 적혀 있다. 다르면 **거기서 멈춘다.**
- **판단하지 않는다.** 예상과 다르면 화면을 캡처하고 담당자에게 전달한다.
- 모든 명령은 **PowerShell** 에서 실행한다.

---

## ⚠️ 기존 `export_database.py` 를 쓰지 않는 이유

이번 백업에는 **쓰면 안 된다.** 반드시 실패한다.

| 문제 | 내용 |
|---|---|
| **10분 타임아웃** | `export_database.py:139` 에 `timeout=600` 이 걸려 있다. 이번 DB는 스냅샷만 논리 28.8 GB라 **10분 안에 끝나지 않는다** |
| **무압축 텍스트 형식** | `--format=plain` 이라 덤프 파일이 **30 GB 이상**이 된다 |
| 선택 복원 불가 | 스냅샷만 빼고 복원하는 것이 불가능하다 |

→ 아래 런북은 `pg_dump` 를 **직접** 호출하고 **압축 형식(`-Fc`)** 을 쓴다.

## 백업을 두 개로 나누는 이유

```
A. 핵심 백업  (스냅샷 제외)   → 작고 빠름. 롤백에 실제로 쓰는 것
B. 스냅샷 백업 (스냅샷만)      → 크지만 한 번만 뜨면 된다
```

- 롤백에 필요한 건 `dashboard_data`·`project_attachments`·`users` 등이지 스냅샷이 아니다
- staging 복원도 **A만** 하면 된다 → 훨씬 빠르고 디스크도 아낀다
- 스냅샷 정리(Phase 1-1) 직전에 B를 한 번 떠두면 그걸로 충분하다

---

# 준비

## P-1. 작업 폴더와 접속 정보 설정 — **한 번만 수정**

아래 5줄에서 **경로와 비밀번호만** 실제 값으로 바꾼 뒤 실행한다.
(이 PowerShell 창을 닫으면 초기화되므로, 창을 닫았다면 다시 실행한다.)

```powershell
$PROJ   = "C:\경로를_여기에\52_KnowledgeGraphProject"   # ← 프로젝트 폴더
$BACKUP = "D:\dt_backup"                                # ← 백업 저장 폴더 (여유 큰 드라이브)
$PGBIN  = "C:\Program Files\PostgreSQL\17\bin"          # ← PostgreSQL bin 폴더
$DB     = "dxdigitaltwin"
$env:PGPASSWORD = "여기에_비밀번호"
```

이어서 아래를 그대로 실행한다 (수정 불필요).

```powershell
$STAMP = Get-Date -Format "yyyyMMdd_HHmm"
New-Item -ItemType Directory -Force -Path $BACKUP | Out-Null
Write-Host "작업 스탬프: $STAMP"
Write-Host "백업 폴더  : $BACKUP"
```

**기대 출력**

```
작업 스탬프: 20260728_1830
백업 폴더  : D:\dt_backup
```

---

# 단계 0. 사전 점검

## 0-1. 도구와 서버 버전 확인

```powershell
& "$PGBIN\pg_dump.exe" --version
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -t -c "SHOW server_version;"
```

**기대 출력** — 두 줄이 나오고, **`pg_dump` 버전이 서버 버전보다 같거나 높아야 한다.**

```
pg_dump (PostgreSQL) 17.5
 17.5
```

**실패 시**
- 명령을 못 찾음 → `$PGBIN` 경로가 틀렸다. `C:\Program Files\PostgreSQL\` 아래 실제 버전 폴더를 확인한다
- 접속 실패 → PostgreSQL 서비스가 떠 있는지 확인 (`services.msc` → postgresql-x64-NN)
- **pg_dump 버전이 서버보다 낮음** → 백업이 실패한다. 중단하고 담당자에게 알린다

## 0-2. DB 크기와 디스크 여유 확인

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT pg_size_pretty(pg_database_size(current_database())) AS db_size;"
Get-PSDrive ($BACKUP.Substring(0,1)) | Select-Object Name, @{n='FreeGB';e={[math]::Round($_.Free/1GB,1)}}
```

**기대 출력**

```
 db_size
---------
 26 GB          ← 대략 25~30 GB 예상

Name FreeGB
---- ------
D     455.0    ← DB 크기의 2배 이상이어야 한다
```

**실패 시** — 여유가 DB 크기의 2배 미만이면 중단하고 담당자에게 알린다.

## 0-3. 백업 직전 기준값 기록 ★ 나중에 대조할 숫자다

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d $DB -c "SELECT (SELECT count(*) FROM dashboard_data) AS dashboard_data, (SELECT version FROM dashboard_data LIMIT 1) AS data_version, (SELECT count(*) FROM dashboard_snapshots) AS snapshots, (SELECT count(*) FROM project_attachments) AS attachments, (SELECT count(*) FROM users) AS users, (SELECT count(*) FROM dashboard_activity_logs) AS activity_logs;" | Tee-Object -FilePath "$BACKUP\baseline_$STAMP.txt"
```

**기대 출력** (숫자는 환경마다 다름 — **화면의 숫자를 그대로 적어둔다**)

```
 dashboard_data | data_version | snapshots | attachments | users | activity_logs
----------------+--------------+-----------+-------------+-------+---------------
              1 |          XXX |      4896 |          NN |    NN |          NNNN
```

> 이 값은 `baseline_<스탬프>.txt` 에도 자동 저장된다. **단계 5에서 이 숫자와 대조한다.**

---

# 단계 1. 핵심 백업 (스냅샷 제외) ★ 가장 중요

```powershell
$T0 = Get-Date
& "$PGBIN\pg_dump.exe" -h localhost -U postgres -d $DB `
  --format=custom --compress=6 --no-owner --no-privileges `
  --exclude-table-data=public.dashboard_snapshots `
  -f "$BACKUP\core_$STAMP.dump"
Write-Host "종료코드: $LASTEXITCODE / 소요: $([int]((Get-Date)-$T0).TotalMinutes)분"
Get-Item "$BACKUP\core_$STAMP.dump" | Select-Object Name, @{n='MB';e={[math]::Round($_.Length/1MB,1)}}
```

**기대 출력**

```
종료코드: 0 / 소요: 1분
Name                    MB
----                    --
core_20260728_1830.dump 30.5     ← 수십 MB ~ 수백 MB
```

**판정**
- **종료코드가 0이어야 한다.** 0이 아니면 실패다
- 파일 크기가 **0 MB 이면 실패**다

**실패 시** — 화면 메시지를 캡처하고 중단한다. 아직 아무것도 바뀌지 않았다.

> 참고: `--exclude-table-data` 는 **테이블 구조는 담고 데이터만 뺀다.** 복원하면 `dashboard_snapshots` 는
> 빈 테이블로 만들어진다. 이관 리허설에는 이게 맞다.

---

# 단계 2. 스냅샷 백업 (스냅샷만) — 시간이 오래 걸린다

```powershell
$T0 = Get-Date
& "$PGBIN\pg_dump.exe" -h localhost -U postgres -d $DB `
  --format=custom --compress=6 --no-owner --no-privileges `
  --table=public.dashboard_snapshots `
  -f "$BACKUP\snapshots_$STAMP.dump"
Write-Host "종료코드: $LASTEXITCODE / 소요: $([int]((Get-Date)-$T0).TotalMinutes)분"
Get-Item "$BACKUP\snapshots_$STAMP.dump" | Select-Object Name, @{n='GB';e={[math]::Round($_.Length/1GB,2)}}
```

**기대 출력** — **20분~1시간 이상** 걸릴 수 있다. 화면이 멈춘 것처럼 보여도 **기다린다.**

```
종료코드: 0 / 소요: 35분
Name                         GB
----                         --
snapshots_20260728_1830.dump 2.10    ← 압축되어 논리 크기보다 훨씬 작다
```

**판정** — 종료코드 0, 파일 크기 0 아님.

---

# 단계 3. 백업 파일 무결성 검증 ★ "파일이 있다"와 "복원된다"는 다르다

```powershell
$c = (& "$PGBIN\pg_restore.exe" --list "$BACKUP\core_$STAMP.dump").Count
$s = (& "$PGBIN\pg_restore.exe" --list "$BACKUP\snapshots_$STAMP.dump").Count
Write-Host "core 항목 수      : $c"
Write-Host "snapshots 항목 수 : $s"
```

**기대 출력** — 두 값 모두 **0보다 커야 한다.** (core 는 보통 수백)

```
core 항목 수      : 567
snapshots 항목 수 : 20
```

**실패 시** — 오류가 나거나 0이면 **덤프 파일이 손상된 것**이다. 단계 1·2를 다시 실행한다.

---

# 단계 4. staging DB 생성 및 복원

> 여기서 만드는 `dxdigitaltwin_stg` 는 **이관 리허설 전용**이다.
> 운영 DB(`dxdigitaltwin`)는 **전혀 건드리지 않는다.**

## 4-1. staging DB 생성

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d postgres -c "DROP DATABASE IF EXISTS dxdigitaltwin_stg;"
& "$PGBIN\psql.exe" -U postgres -h localhost -d postgres -c "CREATE DATABASE dxdigitaltwin_stg;"
```

**기대 출력**

```
알림:  "dxdigitaltwin_stg" 데이터베이스 없음, 건너 뜀     ← 처음 실행이면 정상
DROP DATABASE
CREATE DATABASE
```

**실패 시** — `다른 사용자가 사용 중` 오류가 나면, 그 DB에 붙어 있는 세션을 닫고 다시 실행한다.

## 4-2. 핵심 백업 복원

```powershell
$T0 = Get-Date
& "$PGBIN\pg_restore.exe" -h localhost -U postgres -d dxdigitaltwin_stg `
  --no-owner --no-privileges "$BACKUP\core_$STAMP.dump"
Write-Host "종료코드: $LASTEXITCODE / 소요: $([int]((Get-Date)-$T0).TotalMinutes)분"
```

**기대 출력**

```
종료코드: 0 / 소요: 1분
```

> `warning: errors ignored on restore` 같은 경고가 몇 줄 나올 수 있다.
> **종료코드가 0이면 정상**이다. 아래 단계 5의 숫자 대조가 진짜 판정 기준이다.

---

# 단계 5. 복원 검증 ★ 이 단계가 백업의 진짜 증명이다

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d dxdigitaltwin_stg -c "SELECT (SELECT count(*) FROM dashboard_data) AS dashboard_data, (SELECT version FROM dashboard_data LIMIT 1) AS data_version, (SELECT count(*) FROM dashboard_snapshots) AS snapshots, (SELECT count(*) FROM project_attachments) AS attachments, (SELECT count(*) FROM users) AS users, (SELECT count(*) FROM dashboard_activity_logs) AS activity_logs;"
& "$PGBIN\psql.exe" -U postgres -h localhost -d dxdigitaltwin_stg -c "SELECT json_array_length(projects) AS projects, json_array_length(performances) AS performances FROM dashboard_data;"
```

**기대 출력** — **단계 0-3에서 적어둔 숫자와 대조한다.**

| 항목 | 단계 0-3 값과 | 판정 |
|---|---|---|
| `dashboard_data` | **같아야 함** (1) | 다르면 실패 |
| `data_version` | **같아야 함** | 다르면 실패 |
| `attachments` | **같아야 함** | 다르면 실패 |
| `users` | **같아야 함** | 다르면 실패 |
| `activity_logs` | **같아야 함** | 다르면 실패 |
| `snapshots` | **0 이어야 함** | 0이 아니면 이상 — 담당자에게 알림 |
| `projects` / `performances` | 과제·성과 건수가 **0보다 커야 함** | 0이면 실패 |

> `snapshots` 가 **0인 것이 정상**이다. 단계 1에서 의도적으로 데이터를 제외했다.

**실패 시** — 숫자가 하나라도 다르면 **백업이 신뢰할 수 없다는 뜻**이다.
단계 1부터 다시 실행하고, 그래도 다르면 중단하고 담당자에게 알린다.

---

# 단계 6. 코드 스냅샷

```powershell
$zip = "$BACKUP\code_$STAMP.zip"
Compress-Archive -Path "$PROJ\backend\app", "$PROJ\backend\migrations", "$PROJ\backend\scripts", "$PROJ\frontend\src" -DestinationPath $zip -Force
Get-Item $zip | Select-Object Name, @{n='MB';e={[math]::Round($_.Length/1MB,1)}}
```

**기대 출력**

```
Name                  MB
----                  --
code_20260728_1830.zip 12.4
```

---

# 단계 7. 마무리

```powershell
$env:PGPASSWORD = ""
Get-ChildItem $BACKUP -Filter "*$STAMP*" | Select-Object Name, @{n='MB';e={[math]::Round($_.Length/1MB,1)}}, LastWriteTime
```

**기대 출력** — 파일 4개가 보여야 한다.

```
Name                          MB      LastWriteTime
----                          --      -------------
baseline_20260728_1830.txt    0.0     ...
code_20260728_1830.zip        12.4    ...
core_20260728_1830.dump       30.5    ...
snapshots_20260728_1830.dump  2150.0  ...
```

---

# 완료 체크리스트

```
□ 0-1  pg_dump 버전 ≥ 서버 버전 확인
□ 0-2  디스크 여유가 DB 크기의 2배 이상
□ 0-3  기준값 기록 (baseline_*.txt 생성됨)
□ 1    core_*.dump 생성, 종료코드 0, 크기 > 0
□ 2    snapshots_*.dump 생성, 종료코드 0, 크기 > 0
□ 3    pg_restore --list 로 두 파일 모두 항목 수 > 0
□ 4    dxdigitaltwin_stg 생성 및 복원, 종료코드 0
□ 5    복원 검증 — 0-3 기준값과 전부 일치 (snapshots 만 0)
□ 6    code_*.zip 생성
□ 7    파일 4개 확인, PGPASSWORD 해제
```

**8개 항목이 모두 체크되면 Phase 0-0 완료**이며, Phase 1(스냅샷 정리)로 진행할 수 있다.
하나라도 안 되면 **진행하지 않는다.**

---

# 되돌리기

이 런북은 **운영 DB에 아무것도 쓰지 않는다.** 되돌릴 것이 없다.

staging DB만 지우면 흔적이 사라진다.

```powershell
& "$PGBIN\psql.exe" -U postgres -h localhost -d postgres -c "DROP DATABASE IF EXISTS dxdigitaltwin_stg;"
```

---

# 문제가 생겼을 때

1. **더 이상 아무 명령도 실행하지 않는다**
2. 화면 전체를 캡처한다 (오류 메시지 포함)
3. `$BACKUP` 폴더의 파일 목록을 캡처한다
4. 담당자에게 전달한다

> 이 런북의 모든 명령은 **읽기 또는 새 파일 생성**뿐이다.
> 운영 DB의 데이터가 변경되는 명령은 하나도 없다. 중간에 멈춰도 안전하다.

---

# 자주 나오는 상황

| 상황 | 답 |
|---|---|
| 백업 중 사용자가 데이터를 수정해도 되나? | **된다.** `pg_dump` 는 시작 시점의 일관된 상태를 뜬다 |
| 단계 2가 30분째 멈춰 있다 | **정상이다.** 스냅샷이 크다. 종료코드가 나올 때까지 기다린다 |
| `warning:` 가 여러 줄 나온다 | 종료코드가 0이면 정상. 판정 기준은 단계 5의 숫자 대조다 |
| PowerShell 창을 닫아버렸다 | **P-1을 다시 실행**한다. 이미 만든 파일은 그대로 있다 (스탬프만 새로 생김) |
| 스냅샷 백업이 너무 오래 걸린다 | 단계 1·3·4·5(핵심 백업)만 먼저 끝내도 **Phase 1 이전까지는 안전**하다. 단계 2는 Phase 1-1 착수 직전까지만 끝내면 된다 |
| 단계 4-1에서 `데이터베이스 없음, 건너 뜀` 이 뜬다 | **정상.** 처음 실행이라 지울 DB가 없다는 안내다 |
| `snapshots` 가 0으로 나와서 불안하다 | **의도한 결과다.** 단계 1에서 `--exclude-table-data` 로 뺐다. 스냅샷 실물은 단계 2의 별도 파일에 있다 |

---

# 이 런북의 검증 이력

개발 환경(PostgreSQL 17.5)에서 **전 과정을 실제로 실행해 확인함** (2026-07-28).

| 단계 | 결과 |
|---|---|
| 0-1 | `pg_dump 17.5` / 서버 `17.5` — 정상 |
| 0-3 | 기준값 기록: dashboard_data 1, version 571, snapshots 358, attachments 1, users 2, logs 3175 |
| 1 | 종료코드 0, `core_*.dump` 생성 |
| 3 | `pg_restore --list` 항목 567개 |
| 4 | staging 생성·복원 종료코드 0 |
| **5** | **복원값이 기준값과 전부 일치** (snapshots 만 의도대로 0), projects 328 / performances 432 |

> 운영은 데이터가 훨씬 커서 **소요 시간만 다르고 절차는 동일**하다.
